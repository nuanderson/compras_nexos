---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/accounts/views.py
  - apps/accounts/urls.py
  - apps/accounts/templates/accounts/user_list.html
  - apps/accounts/templates/accounts/user_form.html
  - apps/accounts/templates/accounts/user_confirm_deactivate.html
  - apps/accounts/templates/accounts/unit_list.html
  - apps/accounts/templates/accounts/unit_form.html
  - apps/accounts/templates/accounts/unit_confirm_deactivate.html
  - apps/accounts/templates/accounts/partials/user_form.html
  - apps/accounts/templates/accounts/partials/user_row.html
  - apps/accounts/templates/accounts/partials/unit_form.html
  - apps/accounts/templates/accounts/partials/unit_row.html
  - apps/accounts/tests/test_user_mgmt.py
  - apps/accounts/tests/test_unit_mgmt.py
autonomous: true
requirements:
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - UNIT-01
  - UNIT-02
  - UNIT-03

must_haves:
  truths:
    - "Admin can navigate to /admin-panel/usuarios/ and see a table of users"
    - "Admin can create a new user with email, role, and default_unit via /admin-panel/usuarios/novo/"
    - "Admin can deactivate a user — clicking Desativar loads HTMX confirmation card; confirming sets is_active=False"
    - "Admin can navigate to /admin-panel/unidades/ and see a table of organizational units"
    - "Admin can create a new unit with nome, descricao, ativo via /admin-panel/unidades/nova/"
    - "Admin can deactivate a unit — same HTMX confirmation pattern as user deactivation"
    - "Non-admin users accessing /admin-panel/* get HTTP 403 or redirect to login"
    - "All admin panel forms use HTMX — hx-post on submit, hx-target on #form-container, hx-swap innerHTML"
    - "Automated tests for user CRUD and unit CRUD pass"
  artifacts:
    - path: "apps/accounts/views.py"
      provides: "UserListView, UserCreateView, UserUpdateView, UserDeactivateView, UnitListView, UnitCreateView, UnitUpdateView, UnitDeactivateView"
    - path: "apps/accounts/urls.py"
      provides: "All /admin-panel/usuarios/* and /admin-panel/unidades/* URL patterns"
    - path: "apps/accounts/templates/accounts/user_list.html"
      provides: "User management table with Name, Email, Role, Unit, Status, Actions columns"
      contains: "admin-panel/usuarios"
    - path: "apps/accounts/templates/accounts/unit_list.html"
      provides: "Unit management table with Name, Description, Status, Users, Actions columns"
      contains: "admin-panel/unidades"
    - path: "apps/accounts/tests/test_user_mgmt.py"
      provides: "Tests for AUTH-04 (create/deactivate user), AUTH-05 (role assignment), AUTH-06 (unit assignment)"
    - path: "apps/accounts/tests/test_unit_mgmt.py"
      provides: "Tests for UNIT-01 (create unit), UNIT-02 (assign unit to user)"
  key_links:
    - from: "apps/accounts/views.py (UserCreateView)"
      to: "apps/accounts/services.py (create_user)"
      via: "views call services.create_user(form.cleaned_data) — no business logic in view"
      pattern: "services.create_user"
    - from: "apps/accounts/templates/accounts/user_form.html"
      to: "apps/accounts/views.py (UserCreateView)"
      via: "hx-post to accounts:user-create, hx-target #form-container, hx-swap innerHTML"
      pattern: "hx-post"
    - from: "apps/accounts/templates/accounts/user_list.html"
      to: "apps/accounts/views.py (UserDeactivateView)"
      via: "hx-get to accounts:user-deactivate-confirm, hx-target #confirm-container, hx-swap innerHTML"
      pattern: "user-deactivate-confirm"
---

<objective>
Build the admin panel — user management CRUD and unit management CRUD — as HTMX-powered vertical slices. When this plan completes, an Admin-role user can create accounts with role assignments, assign units, deactivate users, and manage organizational units via the custom /admin-panel/ interface. Non-admin users are blocked from the admin panel.

Purpose: AUTH-04 (admin creates/edits/deactivates users), AUTH-05 (roles), AUTH-06 (unit assignment), UNIT-01 (create units), UNIT-02 (link users to units), UNIT-03 (default_unit FK usable as initial value) are all delivered here.

Output:
- User list + create + edit + deactivate views with HTMX inline confirmation
- Unit list + create + edit + deactivate views with HTMX inline confirmation
- All templates matching UI-SPEC (dark theme, data tables, form cards)
- Automated tests for user and unit management
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
</context>

---

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Admin Panel Views — User CRUD + Unit CRUD + URL Wiring</name>
  <files>
    apps/accounts/views.py,
    apps/accounts/urls.py,
    apps/accounts/templates/accounts/user_list.html,
    apps/accounts/templates/accounts/user_form.html,
    apps/accounts/templates/accounts/user_confirm_deactivate.html,
    apps/accounts/templates/accounts/unit_list.html,
    apps/accounts/templates/accounts/unit_form.html,
    apps/accounts/templates/accounts/unit_confirm_deactivate.html,
    apps/accounts/templates/accounts/partials/user_form.html,
    apps/accounts/templates/accounts/partials/unit_form.html
  </files>

  <read_first>
    apps/accounts/views.py — existing login/logout/password reset views (to append to, not replace)
    apps/accounts/urls.py — existing URL patterns (to extend, not replace)
    apps/accounts/forms.py — UserCreateForm, UserEditForm, UnidadeForm
    apps/accounts/services.py — create_user(), deactivate_user(), assign_unit()
    .planning/phases/01-foundation/01-RESEARCH.md — sections "Pattern 5: HTMX Partial Swap for Admin Forms", "Pattern 6: Deactivation with Inline Confirmation"
    .planning/phases/01-foundation/01-UI-SPEC.md — sections "Data Tables", "Forms", "Buttons", "Status Badges", "Copywriting Contract", "HTMX Patterns Used in Phase 1"
  </read_first>

  <action>
    Add all admin panel views to apps/accounts/views.py (append after existing auth views). Add URL patterns to apps/accounts/urls.py. Create all admin panel templates.

    PERMISSION GUARD — Create a decorator/mixin at the top of views.py additions:
      from django.contrib.auth.mixins import LoginRequiredMixin
      from django.core.exceptions import PermissionDenied

      class AdminRequiredMixin(LoginRequiredMixin):
          def dispatch(self, request, *args, **kwargs):
              if not request.user.is_authenticated:
                  return self.handle_no_permission()
              if request.user.role != "admin" and not request.user.is_superuser:
                  raise PermissionDenied
              return super().dispatch(request, *args, **kwargs)

    USER VIEWS — Append to apps/accounts/views.py:

      class UserListView(AdminRequiredMixin, ListView):
        model = User
        template_name = "accounts/user_list.html"
        context_object_name = "users"
        def get_queryset(self): return User.objects.select_related("default_unit").order_by("email")

      class UserCreateView(AdminRequiredMixin, View):
        def get(self, request):
          form = UserCreateForm()
          return render(request, "accounts/user_form.html", {"form": form, "action": "create", "page_title": "Criar Usuário"})
        def post(self, request):
          form = UserCreateForm(request.POST)
          if form.is_valid():
            services.create_user(form.cleaned_data)
            if request.htmx:
              from django_htmx.http import HttpResponseClientRedirect
              return HttpResponseClientRedirect(reverse("accounts:user-list"))
            return redirect("accounts:user-list")
          template = "accounts/partials/user_form.html" if request.htmx else "accounts/user_form.html"
          return render(request, template, {"form": form, "action": "create"})

      class UserUpdateView(AdminRequiredMixin, View):
        def get(self, request, pk):
          user = get_object_or_404(User, pk=pk)
          form = UserEditForm(instance=user)
          return render(request, "accounts/user_form.html", {"form": form, "action": "edit", "target_user": user, "page_title": "Editar Usuário"})
        def post(self, request, pk):
          user = get_object_or_404(User, pk=pk)
          form = UserEditForm(request.POST, instance=user)
          if form.is_valid():
            form.save()
            if request.htmx:
              from django_htmx.http import HttpResponseClientRedirect
              return HttpResponseClientRedirect(reverse("accounts:user-list"))
            return redirect("accounts:user-list")
          template = "accounts/partials/user_form.html" if request.htmx else "accounts/user_form.html"
          return render(request, template, {"form": form, "action": "edit", "target_user": user})

      class UserDeactivateConfirmView(AdminRequiredMixin, View):
        def get(self, request, pk):
          user = get_object_or_404(User, pk=pk)
          return render(request, "accounts/user_confirm_deactivate.html", {"target_user": user})

      class UserDeactivateView(AdminRequiredMixin, View):
        def post(self, request, pk):
          user = get_object_or_404(User, pk=pk)
          services.deactivate_user(user, actor=request.user)
          # Return updated row partial for HTMX outerHTML swap
          return render(request, "accounts/partials/user_row.html", {"user": user})

    UNIT VIEWS — Append to apps/accounts/views.py (same pattern as User views):

      class UnitListView(AdminRequiredMixin, ListView):
        model = UnidadeOrganizacional
        template_name = "accounts/unit_list.html"
        context_object_name = "units"
        def get_queryset(self): return UnidadeOrganizacional.objects.annotate(user_count=Count("default_users")).order_by("nome")

      class UnitCreateView(AdminRequiredMixin, View):
        def get(self, request): form = UnidadeForm(); return render(request, "accounts/unit_form.html", {"form": form, "action": "create", "page_title": "Criar Unidade"})
        def post(self, request):
          form = UnidadeForm(request.POST)
          if form.is_valid():
            form.save()
            if request.htmx:
              from django_htmx.http import HttpResponseClientRedirect
              return HttpResponseClientRedirect(reverse("accounts:unit-list"))
            return redirect("accounts:unit-list")
          template = "accounts/partials/unit_form.html" if request.htmx else "accounts/unit_form.html"
          return render(request, template, {"form": form, "action": "create"})

      class UnitUpdateView(AdminRequiredMixin, View):
        def get(self, request, pk): unit = get_object_or_404(UnidadeOrganizacional, pk=pk); form = UnidadeForm(instance=unit); return render(request, "accounts/unit_form.html", {"form": form, "action": "edit", "unit": unit, "page_title": "Editar Unidade"})
        def post(self, request, pk):
          unit = get_object_or_404(UnidadeOrganizacional, pk=pk)
          form = UnidadeForm(request.POST, instance=unit)
          if form.is_valid():
            form.save()
            if request.htmx:
              from django_htmx.http import HttpResponseClientRedirect
              return HttpResponseClientRedirect(reverse("accounts:unit-list"))
            return redirect("accounts:unit-list")
          template = "accounts/partials/unit_form.html" if request.htmx else "accounts/unit_form.html"
          return render(request, template, {"form": form, "action": "edit", "unit": unit})

      class UnitDeactivateConfirmView(AdminRequiredMixin, View):
        def get(self, request, pk): unit = get_object_or_404(UnidadeOrganizacional, pk=pk); return render(request, "accounts/unit_confirm_deactivate.html", {"unit": unit})

      class UnitDeactivateView(AdminRequiredMixin, View):
        def post(self, request, pk):
          unit = get_object_or_404(UnidadeOrganizacional, pk=pk)
          unit.ativo = False; unit.save()
          return render(request, "accounts/partials/unit_row.html", {"unit": unit})

    Add required imports to views.py: ListView, Count (from django.db.models), get_object_or_404, redirect, services (from . import services), all form classes, UnidadeOrganizacional.

    APPS/ACCOUNTS/URLS.PY — Extend urlpatterns (append, do not remove existing patterns):
      path("admin-panel/usuarios/", UserListView.as_view(), name="user-list"),
      path("admin-panel/usuarios/novo/", UserCreateView.as_view(), name="user-create"),
      path("admin-panel/usuarios/<int:pk>/editar/", UserUpdateView.as_view(), name="user-edit"),
      path("admin-panel/usuarios/<int:pk>/desativar/confirmar/", UserDeactivateConfirmView.as_view(), name="user-deactivate-confirm"),
      path("admin-panel/usuarios/<int:pk>/desativar/", UserDeactivateView.as_view(), name="user-deactivate"),
      path("admin-panel/unidades/", UnitListView.as_view(), name="unit-list"),
      path("admin-panel/unidades/nova/", UnitCreateView.as_view(), name="unit-create"),
      path("admin-panel/unidades/<int:pk>/editar/", UnitUpdateView.as_view(), name="unit-edit"),
      path("admin-panel/unidades/<int:pk>/desativar/confirmar/", UnitDeactivateConfirmView.as_view(), name="unit-deactivate-confirm"),
      path("admin-panel/unidades/<int:pk>/desativar/", UnitDeactivateView.as_view(), name="unit-deactivate"),

    TEMPLATES — All admin panel templates extend base.html. Implement these templates:

    ACCOUNTS/USER_LIST.HTML:
      {% extends "base.html" %}
      {% block page_title %}Usuários{% endblock %}
      {% block page_actions %}<a href="{% url 'accounts:user-create' %}" class="btn btn-primary">Criar usuário</a>{% endblock %}
      {% block content %}
        <div id="confirm-container"></div>
        {% if users %}
          <div class="table-container">
            <table>
              <thead><tr><th>Nome</th><th>E-mail</th><th>Perfil</th><th>Unidade</th><th>Status</th><th>Ações</th></tr></thead>
              <tbody>
                {% for user in users %}
                  {% include "accounts/partials/user_row.html" %}
                {% endfor %}
              </tbody>
            </table>
          </div>
        {% else %}
          <div class="card" style="text-align:center">
            <p style="font-size:20px;font-weight:600;">Nenhum usuário cadastrado</p>
            <p class="text-muted">Crie o primeiro usuário pelo botão acima para começar.</p>
          </div>
        {% endif %}
      {% endblock %}

    ACCOUNTS/PARTIALS/USER_ROW.HTML (partial for HTMX outerHTML swap):
      <tr id="user-row-{{ user.pk }}" {% if not user.is_active %}style="opacity:0.5"{% endif %}>
        <td>{{ user.get_full_name|default:user.username }}</td>
        <td>{{ user.email }}</td>
        <td><span class="badge {% if user.role == 'admin' %}badge-admin{% elif user.is_active %}badge-ativo{% else %}badge-inativo{% endif %}">{{ user.get_role_display }}</span></td>
        <td>{{ user.default_unit.nome|default:"—" }}</td>
        <td><span class="badge {% if user.is_active %}badge-ativo{% else %}badge-inativo{% endif %}">{% if user.is_active %}Ativo{% else %}Inativo{% endif %}</span></td>
        <td>
          <a href="{% url 'accounts:user-edit' user.pk %}" class="action-link" style="color:var(--color-accent);text-decoration:none;font-size:14px;">Editar</a>
          {% if user.is_active %}
          · <button hx-get="{% url 'accounts:user-deactivate-confirm' user.pk %}" hx-target="#confirm-container" hx-swap="innerHTML" class="btn btn-ghost" style="color:var(--color-destructive);font-size:14px;height:auto;padding:0;">Desativar</button>
          {% else %}
          · <span style="color:var(--color-text-muted);font-size:14px;">Inativo</span>
          {% endif %}
        </td>
      </tr>

    ACCOUNTS/USER_CONFIRM_DEACTIVATE.HTML (HTMX partial — no extends):
      <div class="card" style="margin-bottom:16px;border-color:var(--color-destructive);">
        <p style="font-size:14px;margin-bottom:16px;">
          <strong>Desativar usuário:</strong> Esta ação tornará a conta inacessível. O histórico será preservado. Deseja continuar?
        </p>
        <div style="display:flex;gap:8px;">
          <button hx-post="{% url 'accounts:user-deactivate' target_user.pk %}" hx-target="#user-row-{{ target_user.pk }}" hx-swap="outerHTML" class="btn btn-destructive">Confirmar desativação</button>
          <button hx-get="" hx-target="#confirm-container" hx-swap="innerHTML" class="btn btn-secondary">Cancelar</button>
        </div>
      </div>

    ACCOUNTS/USER_FORM.HTML:
      {% extends "base.html" %}
      {% block page_title %}{% if action == "create" %}Criar Usuário{% else %}Editar Usuário{% endif %}{% endblock %}
      {% block content %}
        <div class="card" style="max-width:640px;">
          <div id="form-container">
            {% include "accounts/partials/user_form.html" %}
          </div>
        </div>
      {% endblock %}

    ACCOUNTS/PARTIALS/USER_FORM.HTML (HTMX swap target — no extends):
      {% load django_htmx %}
      <form hx-post="{% if action == 'create' %}{% url 'accounts:user-create' %}{% else %}{% url 'accounts:user-edit' target_user.pk %}{% endif %}" hx-target="#form-container" hx-swap="innerHTML">
        {% csrf_token %}
        {% for field in form %}
          <div class="form-group">
            <label class="form-label" for="{{ field.id_for_label }}">{{ field.label }}{% if field.field.required %} *{% endif %}</label>
            {{ field }}
            {% for error in field.errors %}<p class="form-error">{{ error }}</p>{% endfor %}
          </div>
        {% endfor %}
        <div class="form-actions">
          <button type="submit" class="btn btn-primary">{% if action == 'create' %}Criar usuário{% else %}Salvar alterações{% endif %}</button>
          <a href="{% url 'accounts:user-list' %}" class="btn btn-secondary">Cancelar edição</a>
        </div>
      </form>
      After rendering, add JavaScript to apply .form-input class to all input/select/textarea inside the form:
        <script>document.querySelectorAll('#form-container input, #form-container select, #form-container textarea').forEach(el => { el.classList.add(el.tagName.toLowerCase() === 'select' ? 'form-select' : 'form-input'); });</script>

    ACCOUNTS/UNIT_LIST.HTML — Same pattern as user_list.html but for units. Columns: Nome, Descrição, Status, Usuários vinculados, Ações. Empty state text from UI-SPEC: "Nenhuma unidade cadastrada" / "Cadastre a primeira unidade organizacional para vincular usuários a ela."

    ACCOUNTS/PARTIALS/UNIT_ROW.HTML — Same pattern as user_row.html but for units. Include user_count annotation.

    ACCOUNTS/UNIT_CONFIRM_DEACTIVATE.HTML — Same pattern as user_confirm_deactivate.html. Confirmation text: "Desativar unidade: Usuários vinculados não serão desvinculados automaticamente. Deseja continuar?" (exact copy from UI-SPEC).

    ACCOUNTS/UNIT_FORM.HTML + ACCOUNTS/PARTIALS/UNIT_FORM.HTML — Same pattern as user form but using UnidadeForm. CTA texts: "Criar unidade" and "Salvar alterações" (from UI-SPEC).
  </action>

  <verify>
    <automated>
      docker compose run --rm web python manage.py check 2>/dev/null && curl -s -b /tmp/cookies.txt http://localhost:8000/admin-panel/usuarios/ | grep -c "Criar usuário"
    </automated>
  </verify>

  <acceptance_criteria>
    - `docker compose run --rm web python manage.py check` returns 0 errors
    - GET `/admin-panel/usuarios/` with admin session returns HTTP 200 containing "Criar usuário"
    - GET `/admin-panel/usuarios/` with non-admin user session returns HTTP 403
    - GET `/admin-panel/unidades/` with admin session returns HTTP 200 containing "Criar unidade"
    - POST `/admin-panel/usuarios/novo/` with valid user data returns HTTP 302 (or HTMX redirect) to /admin-panel/usuarios/
    - POST `/admin-panel/unidades/nova/` with valid unit data returns HTTP 302 to /admin-panel/unidades/
    - GET `/admin-panel/usuarios/{pk}/desativar/confirmar/` returns partial HTML containing "Confirmar desativação"
    - POST `/admin-panel/usuarios/{pk}/desativar/` sets user.is_active=False (verifiable via shell)
    - `apps/accounts/templates/accounts/user_confirm_deactivate.html` contains exact string "O histórico será preservado"
    - `apps/accounts/templates/accounts/unit_confirm_deactivate.html` contains exact string "Usuários vinculados não serão desvinculados automaticamente"
    - `apps/accounts/views.py` contains `services.create_user` call in UserCreateView
    - `apps/accounts/views.py` contains `AdminRequiredMixin` class definition
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admin Panel Tests — User CRUD + Unit CRUD</name>
  <files>
    apps/accounts/tests/test_user_mgmt.py,
    apps/accounts/tests/test_unit_mgmt.py
  </files>

  <read_first>
    apps/accounts/tests/conftest.py — existing fixtures: admin_user, solicitante_user, test_unit (use these; do not redefine)
    apps/accounts/views.py — AdminRequiredMixin, view classes added in Task 1
    apps/accounts/urls.py — URL names: user-list, user-create, user-edit, user-deactivate-confirm, user-deactivate, unit-list, unit-create, unit-edit, unit-deactivate-confirm, unit-deactivate
    .planning/phases/01-foundation/01-VALIDATION.md — "Per-Task Verification Map" rows for AUTH-04, UNIT-01, UNIT-02
  </read_first>

  <behavior>
    USER MANAGEMENT TESTS:
    - test_admin_can_access_user_list: Admin client → GET /admin-panel/usuarios/ → status 200
    - test_non_admin_blocked_from_user_list: Solicitante client → GET /admin-panel/usuarios/ → status 403
    - test_admin_create_user: Admin client → POST /admin-panel/usuarios/novo/ with {email, username, first_name, last_name, role="solicitante", password1, password2} → response status 302 → User.objects.filter(email=posted_email).exists() is True
    - test_admin_deactivate_user: Admin client → POST /admin-panel/usuarios/{pk}/desativar/ → user.refresh_from_db() → user.is_active is False
    - test_deactivate_confirm_renders: Admin client → GET /admin-panel/usuarios/{pk}/desativar/confirmar/ → status 200, "Confirmar desativação" in content

    UNIT MANAGEMENT TESTS:
    - test_admin_can_access_unit_list: Admin client → GET /admin-panel/unidades/ → status 200
    - test_admin_create_unit: Admin client → POST /admin-panel/unidades/nova/ with {nome="Nova Unidade", descricao="Desc", ativo=True} → response 302 → UnidadeOrganizacional.objects.filter(nome="Nova Unidade").exists() is True
    - test_admin_assign_unit_to_user: Admin client → POST /admin-panel/usuarios/{pk}/editar/ with {email, username, role, default_unit=unit.pk} → user.refresh_from_db() → user.default_unit_id == unit.pk
    - test_unit_deactivate: Admin client → POST /admin-panel/unidades/{pk}/desativar/ → unit.refresh_from_db() → unit.ativo is False
  </behavior>

  <action>
    Create two test files. All tests use @pytest.mark.django_db and the fixtures from conftest.py.

    APPS/ACCOUNTS/TESTS/TEST_USER_MGMT.PY:
      Import: pytest, django.urls.reverse, apps.accounts.models.User, apps.accounts.models.UnidadeOrganizacional
      All fixtures come from conftest.py — use admin_user, solicitante_user, test_unit.

      @pytest.mark.django_db
      def test_admin_can_access_user_list(client, admin_user):
        client.force_login(admin_user)
        response = client.get(reverse("accounts:user-list"))
        assert response.status_code == 200

      @pytest.mark.django_db
      def test_non_admin_blocked_from_user_list(client, solicitante_user):
        client.force_login(solicitante_user)
        response = client.get(reverse("accounts:user-list"))
        assert response.status_code == 403

      @pytest.mark.django_db
      def test_admin_create_user(client, admin_user, test_unit):
        client.force_login(admin_user)
        post_data = {
          "email": "new@test.com",
          "username": "newuser",
          "first_name": "New",
          "last_name": "User",
          "role": "solicitante",
          "default_unit": test_unit.pk,
          "password1": "securepass123!",
          "password2": "securepass123!",
        }
        response = client.post(reverse("accounts:user-create"), post_data)
        assert response.status_code == 302
        assert User.objects.filter(email="new@test.com").exists()

      @pytest.mark.django_db
      def test_admin_deactivate_user(client, admin_user, solicitante_user):
        client.force_login(admin_user)
        response = client.post(reverse("accounts:user-deactivate", args=[solicitante_user.pk]))
        assert response.status_code == 200  # Returns partial HTML
        solicitante_user.refresh_from_db()
        assert solicitante_user.is_active is False

      @pytest.mark.django_db
      def test_deactivate_confirm_renders(client, admin_user, solicitante_user):
        client.force_login(admin_user)
        response = client.get(reverse("accounts:user-deactivate-confirm", args=[solicitante_user.pk]))
        assert response.status_code == 200
        assert "Confirmar desativação" in response.content.decode("utf-8")

    APPS/ACCOUNTS/TESTS/TEST_UNIT_MGMT.PY:
      Import: pytest, django.urls.reverse, apps.accounts.models.User, UnidadeOrganizacional

      @pytest.mark.django_db
      def test_admin_can_access_unit_list(client, admin_user):
        client.force_login(admin_user)
        response = client.get(reverse("accounts:unit-list"))
        assert response.status_code == 200

      @pytest.mark.django_db
      def test_admin_create_unit(client, admin_user):
        client.force_login(admin_user)
        response = client.post(reverse("accounts:unit-create"), {"nome": "Nova Unidade", "descricao": "Desc teste", "ativo": True})
        assert response.status_code == 302
        assert UnidadeOrganizacional.objects.filter(nome="Nova Unidade").exists()

      @pytest.mark.django_db
      def test_admin_assign_unit_to_user(client, admin_user, solicitante_user, test_unit):
        client.force_login(admin_user)
        new_unit = UnidadeOrganizacional.objects.create(nome="Nova Unidade Atrib", ativo=True)
        post_data = {
          "email": solicitante_user.email,
          "username": solicitante_user.username,
          "role": solicitante_user.role,
          "default_unit": new_unit.pk,
          "is_active": True,
        }
        response = client.post(reverse("accounts:user-edit", args=[solicitante_user.pk]), post_data)
        assert response.status_code == 302
        solicitante_user.refresh_from_db()
        assert solicitante_user.default_unit_id == new_unit.pk

      @pytest.mark.django_db
      def test_unit_deactivate(client, admin_user, test_unit):
        client.force_login(admin_user)
        response = client.post(reverse("accounts:unit-deactivate", args=[test_unit.pk]))
        assert response.status_code == 200  # Returns partial HTML
        test_unit.refresh_from_db()
        assert test_unit.ativo is False
  </action>

  <verify>
    <automated>
      docker compose run --rm web pytest apps/accounts/tests/test_user_mgmt.py apps/accounts/tests/test_unit_mgmt.py -x -q 2>&1
    </automated>
  </verify>

  <acceptance_criteria>
    - `docker compose run --rm web pytest apps/accounts/tests/ -x -q` exits with code 0 (all tests in all test files pass)
    - `test_admin_can_access_user_list` passes — admin gets 200 on /admin-panel/usuarios/
    - `test_non_admin_blocked_from_user_list` passes — solicitante gets 403
    - `test_admin_create_user` passes — User object created in DB after POST
    - `test_admin_deactivate_user` passes — user.is_active is False after deactivation POST
    - `test_admin_create_unit` passes — UnidadeOrganizacional object created in DB
    - `test_admin_assign_unit_to_user` passes — user.default_unit_id updated in DB
    - `test_unit_deactivate` passes — unit.ativo is False after deactivation POST
    - No test uses `time.sleep()` or manual polling
    - Total test count across all test files: at least 15 tests passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /admin-panel/* views | All admin panel views receive untrusted HTTP input |
| Admin role check → view dispatch | AdminRequiredMixin must reject non-admin users before any business logic |
| HTMX POST → deactivate view | Deactivation is a state-changing operation — CSRF required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Elevation of Privilege | /admin-panel/* access by non-admin | mitigate | `AdminRequiredMixin.dispatch()` checks `user.role == "admin" or user.is_superuser` and raises `PermissionDenied` (HTTP 403). Tested by `test_non_admin_blocked_from_user_list`. |
| T-01-13 | Tampering | User deactivation HTMX POST without CSRF | mitigate | `CsrfViewMiddleware` is active. HTMX `htmx:configRequest` injects `X-CSRFToken` header from `<meta name="csrf-token">`. All HTMX POSTs are CSRF-protected. |
| T-01-14 | Elevation of Privilege | Deactivating own admin account | accept | Phase 1 has no guard preventing admin from deactivating themselves. Risk is low — there will be at least one superuser (created via createsuperuser). Superusers bypass `is_active` check. Add guard in Phase 2 if needed. |
| T-01-15 | Tampering | Mass assignment via ModelForm | mitigate | `UserEditForm` explicitly lists allowed fields: [email, username, first_name, last_name, role, default_unit, is_active]. `UserCreateForm` similarly limits fields. No `__all__`. |
| T-01-16 | Information Disclosure | User list exposes all user emails | accept | Admin panel is restricted to Admin role. Internal system with 20 users — admin legitimately needs to see all user data. No PII exposure to non-admin roles. |
| T-01-SC | Tampering | pip package installs | mitigate | No new packages added in this plan — all packages inherited from Plan 01. Legitimacy audit already passed. |
</threat_model>

<verification>
After both tasks complete:

Full suite verification:
  docker compose run --rm web pytest -x -q

Expected results:
  - All tests in apps/accounts/tests/test_auth.py PASSED (5 tests)
  - All tests in apps/accounts/tests/test_models.py PASSED (5 tests)
  - All tests in apps/accounts/tests/test_user_mgmt.py PASSED (5 tests)
  - All tests in apps/accounts/tests/test_unit_mgmt.py PASSED (4 tests)
  - Total: at least 19 tests, all green

Manual spot-check (browser):
  1. Log in as admin at http://localhost:8000/accounts/login/
  2. Navigate to http://localhost:8000/admin-panel/usuarios/ — user list table renders
  3. Click "Criar usuário" — form renders in dark card layout
  4. Create a new user with role "Solicitante" — user appears in list
  5. Click "Desativar" on the new user — HTMX loads confirmation card inline (no page reload)
  6. Click "Confirmar desativação" — row updates inline to show "Inativo" badge
  7. Navigate to http://localhost:8000/admin-panel/unidades/ — unit list renders
  8. Create a new unit — unit appears in list
</verification>

<success_criteria>
Plan 03 is complete when:
- All admin panel URLs respond correctly (200 for admin, 403 for non-admin)
- User create, edit, and deactivate work correctly via both standard HTTP and HTMX
- Unit create, edit, and deactivate work correctly
- HTMX inline deactivation confirmation works (no window.confirm())
- Full pytest suite (all 4 test files) is green with at least 19 tests passing
- AUTH-04, AUTH-05, AUTH-06, UNIT-01, UNIT-02, UNIT-03 are all covered by passing tests
</success_criteria>

## Artifacts This Phase Produces

| Artifact | Path | Consumed By |
|----------|------|-------------|
| Admin panel views | `apps/accounts/views.py` | Admin users immediately (user/unit management) |
| Admin panel URLs | `apps/accounts/urls.py` | Browser navigation, reverse() in templates |
| User list + form templates | `apps/accounts/templates/accounts/` | Admin UI |
| Unit list + form templates | `apps/accounts/templates/accounts/` | Admin UI |
| HTMX partial templates | `apps/accounts/templates/accounts/partials/` | HTMX inline swaps |
| User management tests | `apps/accounts/tests/test_user_mgmt.py` | CI gate / gsd-verify-work |
| Unit management tests | `apps/accounts/tests/test_unit_mgmt.py` | CI gate / gsd-verify-work |
| AdminRequiredMixin | `apps/accounts/views.py` | Phase 2-5 admin panel views (import and reuse) |

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
