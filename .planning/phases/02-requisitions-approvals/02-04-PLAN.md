---
phase: 02-requisitions-approvals
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - apps/aprovacoes/views.py
  - apps/aprovacoes/urls.py
  - templates/base.html
  - apps/aprovacoes/templates/aprovacoes/fila_diretor.html
  - apps/aprovacoes/templates/aprovacoes/partials/fila_diretor_row.html
  - apps/aprovacoes/templates/aprovacoes/partials/modal_reprovar_diretor.html
  - apps/aprovacoes/tests/test_views_diretor.py
autonomous: true
requirements: [APROV-03, APROV-04, APROV-05]
user_setup: []

must_haves:
  truths:
    - "Diretor abre /aprovacoes/fila-diretor/ e vê requisições PENDENTE_DIRETOR de TODAS as unidades (D-06)"
    - "Diretor aprova uma requisição PENDENTE_DIRETOR → status APROVADO (terminal) (APROV-04)"
    - "Diretor reprova com motivo obrigatório → status REPROVADO (terminal, permanente, D-13) (APROV-04, APROV-05)"
    - "Diretor NÃO vê requisições PENDENTE_GESTOR (só 2º nível)"
    - "Gestor/Solicitante não conseguem acessar a fila do Diretor (403)"
    - "Requisição reprovada em qualquer nível é permanente — sem reabertura (D-13)"
  artifacts:
    - path: "apps/aprovacoes/views.py"
      provides: "FilaDiretorView, AprovarDiretorView, ModalReprovarDiretorView, ReprovarDiretorView, DiretorRequiredMixin"
      contains: "class FilaDiretorView"
    - path: "apps/aprovacoes/templates/aprovacoes/fila_diretor.html"
      provides: "Fila do Diretor (todas as unidades) com ações"
      contains: "fila_diretor_row"
    - path: "apps/aprovacoes/templates/aprovacoes/partials/modal_reprovar_diretor.html"
      provides: "Modal HTMX de reprovação do Diretor com motivo + csrf"
      contains: "name=\"motivo\""
  key_links:
    - from: "apps/aprovacoes/views.py"
      to: "apps.aprovacoes.services.aprovar_diretor / reprovar_requisicao"
      via: "chamada do service na view do Diretor"
      pattern: "services\\.(aprovar_diretor|reprovar_requisicao)"
    - from: "apps/aprovacoes/views.py (FilaDiretorView)"
      to: "requisicoes.Requisicao (status=PENDENTE_DIRETOR)"
      via: "queryset sem filtro de unidade"
      pattern: "status=Requisicao.Status.PENDENTE_DIRETOR"
---

<objective>
Construir o slice vertical do Diretor: a fila de requisições aguardando 2º nível (todas as unidades, D-06) e as ações de aprovar (→ APROVADO terminal) e reprovar (→ REPROVADO terminal, permanente). Com este plano, o fluxo de aprovação de 2 níveis está completo end-to-end: requisições acima da alçada do Gestor são roteadas para o Diretor, que aplica a decisão final.

Purpose: Fecha o ciclo de aprovação da fase. Consome `services.aprovar_diretor` e `services.reprovar_requisicao` (criados no Plano 02). Implementa APROV-03 (fila do Diretor), APROV-04 (decisão de 2º nível), APROV-05 (motivo obrigatório), e as decisões D-06 (todas as unidades) e D-13 (reprovação permanente). Depende do Plano 03 por compartilhar `apps/aprovacoes/views.py`, `urls.py` e `templates/base.html` (execução sequencial para evitar conflito de arquivos).

Output: Fila do Diretor funcional (todas as unidades); aprovação/reprovação de 2º nível via HTMX; reprovação permanente; testes de view verdes; fase end-to-end completa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-requisitions-approvals/02-CONTEXT.md
@.planning/phases/02-requisitions-approvals/02-RESEARCH.md
@.planning/phases/02-requisitions-approvals/02-PATTERNS.md
@CLAUDE.md
@apps/accounts/views.py
@apps/aprovacoes/views.py
@apps/aprovacoes/urls.py
@apps/aprovacoes/forms.py
@apps/aprovacoes/templates/aprovacoes/fila_gestor.html
@apps/aprovacoes/templates/aprovacoes/partials/fila_row.html
@apps/aprovacoes/templates/aprovacoes/partials/modal_reprovar.html
@templates/base.html
@apps/accounts/tests/conftest.py
</context>

<artifacts_this_phase_produces>
**Views (`apps/aprovacoes/views.py`):**
- `DiretorRequiredMixin` — role in (diretor, admin) ou superuser
- `FilaDiretorView` (ListView) — fila PENDENTE_DIRETOR de todas as unidades (D-06)
- `AprovarDiretorView` (View) — POST chama `services.aprovar_diretor` → APROVADO
- `ModalReprovarDiretorView` (View) — GET retorna `partials/modal_reprovar_diretor.html`
- `ReprovarDiretorView` (View) — POST chama `services.reprovar_requisicao` → REPROVADO

**URLs (namespace `aprovacoes`):** `fila-diretor`, `aprovar-diretor`, `modal-reprovar-diretor`, `reprovar-diretor`

**Templates:** `fila_diretor.html`, `partials/fila_diretor_row.html`, `partials/modal_reprovar_diretor.html`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fila do Diretor, aprovar (2º nível) e reprovar (modal HTMX), URLs e nav com testes de view</name>
  <files>apps/aprovacoes/views.py, apps/aprovacoes/urls.py, templates/base.html, apps/aprovacoes/templates/aprovacoes/fila_diretor.html, apps/aprovacoes/templates/aprovacoes/partials/fila_diretor_row.html, apps/aprovacoes/templates/aprovacoes/partials/modal_reprovar_diretor.html, apps/aprovacoes/tests/test_views_diretor.py</files>
  <read_first>
    - apps/aprovacoes/views.py (criado no Plano 03 — GestorRequiredMixin, FilaGestorView, AprovarGestorView, ModalReprovarView, ReprovarGestorView; REPLICAR esses padrões para o Diretor alterando role/filtro/service)
    - apps/aprovacoes/urls.py (criado no Plano 03 — adicionar as 4 rotas do Diretor SEM remover as do Gestor)
    - apps/aprovacoes/forms.py (ReprovaForm criado no Plano 03 — reutilizar)
    - apps/aprovacoes/services.py (aprovar_diretor, reprovar_requisicao — criados no Plano 02)
    - apps/aprovacoes/templates/aprovacoes/fila_gestor.html e partials/fila_row.html e partials/modal_reprovar.html (criados no Plano 03 — copiar estrutura, ajustar URLs para rotas do Diretor)
    - .planning/phases/02-requisitions-approvals/02-RESEARCH.md (FilaDiretorView sem filtro de unidade, status=PENDENTE_DIRETOR; DiretorRequiredMixin; Armadilha 4 ValueError→409)
    - templates/base.html (nav — adicionar item "Aprovações (Diretoria)" para diretor/admin; o item de Gestor foi adicionado no Plano 03)
  </read_first>
  <behavior>
    - test_fila_diretor_todas_unidades: requisições PENDENTE_DIRETOR de unidade A e unidade B ambas aparecem para o Diretor (D-06, APROV-03)
    - test_fila_diretor_so_pendente_diretor: requisição PENDENTE_GESTOR ou APROVADO NÃO aparece na fila do Diretor
    - test_gestor_nao_acessa_fila_diretor: GET fila-diretor por gestor → 403
    - test_solicitante_nao_acessa_fila_diretor: GET fila-diretor por solicitante → 403
    - test_aprovar_diretor: POST aprovar requisição PENDENTE_DIRETOR → status APROVADO + AprovacaoLog(evento=APROVACAO_FINAL) (APROV-04)
    - test_aprovar_diretor_estado_invalido_409: POST aprovar requisição PENDENTE_GESTOR via rota do Diretor → 409 (estado não aceita ação)
    - test_modal_reprovar_diretor_get: GET modal-reprovar-diretor retorna 200 com textarea motivo
    - test_reprovar_diretor_sem_motivo_rejeitado: POST reprovar sem motivo → não transiciona (APROV-05)
    - test_reprovar_diretor_com_motivo: POST reprovar com motivo → status REPROVADO + AprovacaoLog com motivo (APROV-04, APROV-05)
    - test_reprovado_permanente: requisição REPROVADA não tem rota de reabertura — POST aprovar nela → 409 (D-13)
  </behavior>
  <action>
    **views.py** — Em `apps/aprovacoes/views.py` ADICIONAR (sem remover as views do Gestor do Plano 03):
    - `DiretorRequiredMixin(LoginRequiredMixin)` replicando GestorRequiredMixin com `role in ("diretor", "admin")` ou superuser.
    - `FilaDiretorView(DiretorRequiredMixin, ListView)`: `template_name="aprovacoes/fila_diretor.html"`, `context_object_name="requisicoes"`. `get_queryset`: `Requisicao.objects.filter(status=Requisicao.Status.PENDENTE_DIRETOR).select_related("criado_por", "categoria", "unidade").order_by("criado_em")` — SEM filtro de unidade (D-06).
    - `AprovarDiretorView(DiretorRequiredMixin, View)`: POST → `try: services.aprovar_diretor(pk, request.user)`; except `(ValueError, PermissionError)` → 409 com mensagem pt-BR (Armadilha 4); sucesso → remover a linha da fila (outerHTML para vazio), mesmo padrão do Plano 03.
    - `ModalReprovarDiretorView(DiretorRequiredMixin, View)`: GET → renderiza `partials/modal_reprovar_diretor.html` com `requisicao` + `ReprovaForm()` (form reutilizado do Plano 03), POST apontando para `aprovacoes:reprovar-diretor`.
    - `ReprovarDiretorView(DiretorRequiredMixin, View)`: POST → `form = ReprovaForm(request.POST)`; inválido → retorna modal com erros (200, sem transicionar); válido → `try: services.reprovar_requisicao(pk, request.user, form.cleaned_data["motivo"])` com tratamento de erro → 409; sucesso → remove a linha e fecha o modal.
    O Diretor não precisa de filtro de unidade nas ações (D-06), mas as views de ação devem verificar `req.pode_diretor_agir()` indiretamente via o service (que levanta ValueError se o estado não for PENDENTE_DIRETOR) — garantindo D-13 (reprovado é terminal: aprovar/reprovar uma requisição REPROVADA cai em ValueError → 409).

    **urls.py** — ADICIONAR a `apps/aprovacoes/urls.py` (preservando as rotas do Gestor): `"fila-diretor/"`→fila-diretor, `"<int:pk>/aprovar-diretor/"`→aprovar-diretor, `"<int:pk>/modal-reprovar-diretor/"`→modal-reprovar-diretor, `"<int:pk>/reprovar-diretor/"`→reprovar-diretor.

    **templates/base.html** — Adicionar no nav, visível para `request.user.role == 'diretor' or request.user.role == 'admin'`, um item "Aprovações (Diretoria)" apontando para `{% url 'aprovacoes:fila-diretor' %}` com marcação de ativo. NÃO remover o item de Gestor adicionado no Plano 03.

    **Templates** (copiar estrutura dos templates do Gestor criados no Plano 03, ajustando rótulos e URLs):
    - `fila_diretor.html`: extends base; `page_title` "Fila de Aprovação — Diretoria"; div `#modal-container`; tabela com colunas Solicitante, Unidade, Descrição, Categoria, Valor Estimado, Data de Criação, Ações; loop incluindo `partials/fila_diretor_row.html`; estado vazio "Nenhuma requisição aguardando aprovação da diretoria".
    - `partials/fila_diretor_row.html`: `<tr id="fila-diretor-row-{{ req.pk }}">`; botão "Aprovar" (`hx-post` aprovar-diretor, target outerHTML na própria linha) e "Reprovar" (`hx-get` modal-reprovar-diretor para `#modal-container`).
    - `partials/modal_reprovar_diretor.html`: igual ao modal do Gestor, mas o form faz `hx-post="{% url 'aprovacoes:reprovar-diretor' requisicao.pk %}"` com `hx-target="#fila-diretor-row-{{ requisicao.pk }}"`; `{% csrf_token %}` OBRIGATÓRIO; `<textarea name="motivo" required>`.

    Toda cópia em pt-BR formal. Todos os forms com `{% csrf_token %}` (CR-04).
  </action>
  <verify>
    <automated>cd . && python -m pytest apps/aprovacoes/tests/test_views_diretor.py -x -q && python manage.py check</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest apps/aprovacoes/tests/test_views_diretor.py -x -q` sai com código 0
    - `python manage.py check` sem erros
    - `FilaDiretorView.get_queryset` filtra `status=PENDENTE_DIRETOR` SEM filtro de unidade (requisições de todas as unidades aparecem — test_fila_diretor_todas_unidades)
    - Gestor e Solicitante recebem 403 na fila do Diretor (test_gestor_nao_acessa_fila_diretor, test_solicitante_nao_acessa_fila_diretor)
    - Aprovação do Diretor leva a APROVADO; reprovação leva a REPROVADO com motivo (APROV-04, APROV-05)
    - Reprovação é permanente: aprovar uma requisição REPROVADA retorna 409 (test_reprovado_permanente, D-13)
    - As 4 rotas do Gestor (Plano 03) continuam presentes em `apps/aprovacoes/urls.py` além das 4 do Diretor
    - `modal_reprovar_diretor.html` contém `{% csrf_token %}` e `name="motivo"` com `required`
    - `templates/base.html` mantém o item de nav de Gestor e adiciona o de Diretoria
  </acceptance_criteria>
  <done>Diretor vê fila de 2º nível de todas as unidades, aprova (→ APROVADO) e reprova com motivo (→ REPROVADO permanente) via HTMX; demais perfis bloqueados; estados terminais retornam 409; fluxo de 2 níveis completo; testes verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → views aprovacoes (Diretor) | Diretor autenticado dispara decisão de 2º nível (identidade e intenção não confiáveis) |
| view → services | Enforcement de estado terminal (D-13) e de role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Gestor/Solicitante acessando fila ou ações de Diretor | mitigate | `DiretorRequiredMixin` em todas as views do Diretor |
| T-04-02 | Tampering | Reprovação do Diretor sem motivo | mitigate | `ReprovaForm` + validação no service `reprovar_requisicao` (APROV-05, defense in depth) |
| T-04-03 | Tampering | Tentativa de reabrir/agir sobre requisição REPROVADA (terminal) | mitigate | Service verifica estado pós-lock e levanta ValueError → view retorna 409 (D-13) |
| T-04-04 | Tampering | Aprovação dupla concorrente no 2º nível | mitigate | `select_for_update()` no service (Plano 02) + verificação de estado pós-lock |
| T-04-05 | Cross-Site Request Forgery | Modal de reprovação do Diretor (form HTMX injetado) | mitigate | `{% csrf_token %}` no form do modal + meta csrf-token + handler em base.html (CR-04) |
| T-04-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo nesta fase (RESEARCH.md §Auditoria de Legitimidade — não aplicável) |
</threat_model>

<verification>
- `python -m pytest apps/aprovacoes/tests/ apps/requisicoes/tests/ -x -q` verde (suíte completa da fase)
- `python manage.py check` sem erros
- Smoke manual (opcional): requisição acima da alçada → Gestor aprova → vai para fila do Diretor → Diretor aprova (APROVADO) ou reprova (REPROVADO permanente)
</verification>

<success_criteria>
- Fila do Diretor com requisições de todas as unidades (D-06, APROV-03)
- Decisão de 2º nível: aprovar → APROVADO, reprovar → REPROVADO permanente (APROV-04, APROV-05, D-13)
- Demais perfis bloqueados (403); estados terminais retornam 409
- APROV-03, APROV-04, APROV-05 satisfeitas; fluxo de aprovação de 2 níveis completo end-to-end
</success_criteria>

<output>
Create `.planning/phases/02-requisitions-approvals/02-04-SUMMARY.md` when done
</output>
