---
phase: 02-requisitions-approvals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/requisicoes/__init__.py
  - apps/requisicoes/apps.py
  - apps/requisicoes/models.py
  - apps/requisicoes/admin.py
  - apps/requisicoes/migrations/__init__.py
  - apps/aprovacoes/__init__.py
  - apps/aprovacoes/apps.py
  - apps/aprovacoes/models.py
  - apps/aprovacoes/admin.py
  - apps/aprovacoes/migrations/__init__.py
  - config/settings/base.py
  - static/css/main.css
  - apps/requisicoes/tests/__init__.py
  - apps/requisicoes/tests/conftest.py
  - apps/aprovacoes/tests/__init__.py
  - apps/aprovacoes/tests/conftest.py
  - apps/aprovacoes/tests/test_models.py
autonomous: true
requirements: [REQ-01, REQ-03, APROV-06]
must_haves:
  truths:
    - "Os apps apps.requisicoes e apps.aprovacoes estão registrados e migram sem erro"
    - "Admin consegue cadastrar CategoriaCompra e configurar ConfiguracaoAlcada via /admin/ sem deploy"
    - "ConfiguracaoAlcada.obter() retorna sempre uma instância singleton (pk=1)"
    - "ConfiguracaoAlcada com valor_maximo_gestor=None exige 2 níveis (fail-safe)"
  artifacts:
    - path: "apps/requisicoes/models.py"
      provides: "CategoriaCompra + Requisicao com FSM helpers e Status TextChoices"
      contains: "class Requisicao"
    - path: "apps/aprovacoes/models.py"
      provides: "AprovacaoLog + ConfiguracaoAlcada singleton"
      contains: "class ConfiguracaoAlcada"
    - path: "apps/requisicoes/admin.py"
      provides: "CategoriaCompra registrado no Django admin"
      contains: "register"
    - path: "apps/aprovacoes/admin.py"
      provides: "ConfiguracaoAlcada + AprovacaoLog registrados no Django admin"
      contains: "ConfiguracaoAlcada"
  key_links:
    - from: "apps/requisicoes/models.py"
      to: "apps.core.models.AuditedModel"
      via: "herança de Requisicao"
      pattern: "Requisicao\\(AuditedModel\\)"
    - from: "apps/aprovacoes/models.py"
      to: "requisicoes.Requisicao"
      via: "FK AprovacaoLog.requisicao"
      pattern: "requisicoes.Requisicao"
    - from: "config/settings/base.py"
      to: "apps.requisicoes / apps.aprovacoes"
      via: "INSTALLED_APPS"
      pattern: "apps.requisicoes"
---

<objective>
Estabelecer a fundação de dados compartilhada da Fase 2: criar os dois apps Django novos (`apps/requisicoes`, `apps/aprovacoes`), os quatro modelos (`CategoriaCompra`, `Requisicao`, `AprovacaoLog`, `ConfiguracaoAlcada`), registrá-los no Django admin, gerar e aplicar as migrations, adicionar os badges CSS faltantes e criar o scaffold de testes (fixtures compartilhadas) que todos os slices verticais subsequentes consomem.

Purpose: Todos os slices verticais (criação de requisição, fila do Gestor, fila do Diretor) dependem destes modelos e migrations. Esta é a única fundação horizontal justificada da fase — sem os modelos, nenhum slice end-to-end pode rodar. Implementa as decisões D-01, D-08, D-19 e os requisitos de cadastro/config administrativos (REQ-01 estrutura de dados, REQ-03 modelo de log, APROV-06 modelo de alçada).

Output: Dois apps registrados, migrations aplicadas, admin funcional para CategoriaCompra e ConfiguracaoAlcada, badges CSS para RASCUNHO/CANCELADO, fixtures de teste prontas.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-requisitions-approvals/02-CONTEXT.md
@.planning/phases/02-requisitions-approvals/02-RESEARCH.md
@.planning/phases/02-requisitions-approvals/02-PATTERNS.md
@CLAUDE.md
@apps/accounts/models.py
@apps/core/models.py
@config/settings/base.py
@apps/accounts/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar apps requisicoes e aprovacoes com os quatro modelos e registrar em INSTALLED_APPS</name>
  <files>apps/requisicoes/__init__.py, apps/requisicoes/apps.py, apps/requisicoes/models.py, apps/requisicoes/migrations/__init__.py, apps/aprovacoes/__init__.py, apps/aprovacoes/apps.py, apps/aprovacoes/models.py, apps/aprovacoes/migrations/__init__.py, config/settings/base.py</files>
  <read_first>
    - apps/core/models.py (AuditedModel, TimestampedModel — bases de herança; comentário sobre DecimalField na linha 1)
    - apps/accounts/models.py (padrão de TextChoices em User.Role linhas 27-32; padrão de FK com on_delete e related_name)
    - config/settings/base.py (INSTALLED_APPS linhas 19-32 — seção "# Local apps")
    - .planning/phases/02-requisitions-approvals/02-RESEARCH.md (Padrão 1 modelo Requisicao com FSM; Padrão 3 ConfiguracaoAlcada singleton; Exemplo de código AprovacaoLog)
  </read_first>
  <action>
    Criar `apps/requisicoes/apps.py` com classe `RequisicoesConfig(AppConfig)` (`default_auto_field="django.db.models.BigAutoField"`, `name="apps.requisicoes"`) e `apps/aprovacoes/apps.py` com `AprovacoesConfig` (`name="apps.aprovacoes"`). Criar `__init__.py` vazios em cada app e em cada pasta `migrations/`.

    Em `apps/requisicoes/models.py`: importar `from apps.core.models import AuditedModel` e `from django.db import models`. Definir:
    - `CategoriaCompra(models.Model)` com `nome = CharField(max_length=100)`, `ativo = BooleanField(default=True)`, Meta com `verbose_name="Categoria de Compra"`, `verbose_name_plural="Categorias de Compra"`, `ordering=["nome"]`, e `__str__` retornando `self.nome`. (D-01)
    - `Requisicao(AuditedModel)` com `Status(models.TextChoices)` interna contendo exatamente: `RASCUNHO`, `PENDENTE_GESTOR` (label "Aguardando Gestor"), `PENDENTE_DIRETOR` (label "Aguardando Diretor"), `APROVADO`, `REPROVADO`, `CANCELADO`. Campos: `descricao = CharField(max_length=500)`, `categoria = ForeignKey("CategoriaCompra", on_delete=models.PROTECT)`, `valor_estimado = DecimalField(max_digits=12, decimal_places=2)`, `justificativa = TextField()`, `unidade = ForeignKey("accounts.UnidadeOrganizacional", on_delete=models.PROTECT, related_name="requisicoes")`, `status = CharField(max_length=20, choices=Status.choices, default=Status.RASCUNHO)`. Atributos de classe `ESTADOS_TERMINAIS = {Status.APROVADO, Status.REPROVADO, Status.CANCELADO}` e `CANCELA_PERMISSOES = {Status.RASCUNHO, Status.PENDENTE_GESTOR}`. Métodos predicado puros (sem efeito colateral): `pode_submeter()` (status==RASCUNHO), `pode_cancelar()` (status in CANCELA_PERMISSOES), `pode_gestor_agir()` (status==PENDENTE_GESTOR), `pode_diretor_agir()` (status==PENDENTE_DIRETOR). Meta com `verbose_name="Requisição de Compra"`, `verbose_name_plural="Requisições de Compra"`, `ordering=["-criado_em"]`. `__str__` retornando `f"{self.descricao[:40]} ({self.get_status_display()})"`.

    Em `apps/aprovacoes/models.py`: importar `from django.conf import settings`, `from decimal import Decimal`, `from apps.core.models import TimestampedModel`. Definir:
    - `AprovacaoLog(TimestampedModel)` com `Evento(models.TextChoices)` contendo: `ENVIO`, `APROVACAO_GESTOR`, `APROVACAO_FINAL`, `REPROVACAO`, `CANCELAMENTO`. Campos: `requisicao = ForeignKey("requisicoes.Requisicao", on_delete=models.CASCADE, related_name="logs")`, `aprovador = ForeignKey(settings.AUTH_USER_MODEL, null=True, on_delete=models.SET_NULL, related_name="aprovacoes_realizadas")`, `evento = CharField(max_length=20, choices=Evento.choices)`, `motivo = TextField(blank=True, default="")`. Meta com `verbose_name="Log de Aprovação"`, `verbose_name_plural="Logs de Aprovação"`, `ordering=["criado_em"]`. (REQ-03, D-19)
    - `ConfiguracaoAlcada(models.Model)` com `valor_maximo_gestor = DecimalField(max_digits=12, decimal_places=2, null=True, blank=True, help_text=...)` (help_text em pt-BR explicando que em branco exige sempre 2 níveis). Meta com `verbose_name="Configuração de Alçada"`, `verbose_name_plural="Configuração de Alçada"`. Classmethod `obter()` que faz `obj, _ = cls.objects.get_or_create(pk=1); return obj`. Método `requer_diretor(self, valor: Decimal) -> bool` que retorna `True` se `valor_maximo_gestor is None` (fail-safe D-10) senão `valor >= self.valor_maximo_gestor`. (D-08, D-09, D-10, APROV-06)

    Em `config/settings/base.py`: adicionar `"apps.requisicoes"` e `"apps.aprovacoes"` na seção `# Local apps` de INSTALLED_APPS, logo após `"apps.accounts"`.

    NÃO escrever lógica de transição de estado aqui (isso vive em services no Plano 02). Os métodos de Requisicao são apenas predicados de leitura.
  </action>
  <verify>
    <automated>python manage.py makemigrations requisicoes aprovacoes --check --dry-run; python manage.py makemigrations requisicoes aprovacoes; python manage.py migrate; python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.dev');django.setup();from apps.aprovacoes.models import ConfiguracaoAlcada;c=ConfiguracaoAlcada.obter();assert c.pk==1;assert c.requer_diretor(__import__('decimal').Decimal('100'))==True"</automated>
  </verify>
  <acceptance_criteria>
    - `python manage.py makemigrations requisicoes aprovacoes` gera migrations sem erro e `python manage.py migrate` aplica sem erro
    - `config/settings/base.py` contém as strings `"apps.requisicoes"` e `"apps.aprovacoes"` em INSTALLED_APPS
    - `apps/requisicoes/models.py` contém `class Requisicao(AuditedModel)` com `class Status(models.TextChoices)` definindo os 6 estados RASCUNHO/PENDENTE_GESTOR/PENDENTE_DIRETOR/APROVADO/REPROVADO/CANCELADO
    - `Requisicao.valor_estimado` é `DecimalField(max_digits=12, decimal_places=2)` (não FloatField)
    - `apps/aprovacoes/models.py` contém `class ConfiguracaoAlcada` com classmethod `obter` e método `requer_diretor`
    - `ConfiguracaoAlcada.obter()` retorna instância com `pk==1`; `requer_diretor(Decimal('100'))` retorna `True` quando `valor_maximo_gestor` é None
  </acceptance_criteria>
  <done>Os dois apps existem, estão em INSTALLED_APPS, migram sem erro; os quatro modelos têm os campos e métodos especificados; o singleton ConfiguracaoAlcada funciona com fail-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Registrar modelos no Django admin e adicionar badges CSS para RASCUNHO/CANCELADO</name>
  <files>apps/requisicoes/admin.py, apps/aprovacoes/admin.py, static/css/main.css</files>
  <read_first>
    - apps/requisicoes/models.py (criado na Task 1 — para importar CategoriaCompra)
    - apps/aprovacoes/models.py (criado na Task 1 — para importar AprovacaoLog, ConfiguracaoAlcada)
    - .planning/phases/02-requisitions-approvals/02-RESEARCH.md (seção "Registro no Django Admin" — has_add_permission, has_delete_permission para singleton e log imutável)
    - static/css/main.css (classes .badge linhas 372-407 — .badge-ativo, .badge-inativo, .badge-aguardando, .badge-em-cotacao já existem; adicionar análogas)
  </read_first>
  <action>
    Em `apps/requisicoes/admin.py`: importar `from django.contrib import admin` e `from .models import CategoriaCompra`. Registrar `@admin.register(CategoriaCompra)` com `CategoriaCompraAdmin(admin.ModelAdmin)` definindo `list_display = ["nome", "ativo"]` e `list_filter = ["ativo"]`. (D-01, D-03)

    Em `apps/aprovacoes/admin.py`: importar `from django.contrib import admin` e `from .models import AprovacaoLog, ConfiguracaoAlcada`.
    - `@admin.register(ConfiguracaoAlcada)` com `ConfiguracaoAlcadaAdmin(admin.ModelAdmin)`: `has_add_permission(self, request)` retorna `not ConfiguracaoAlcada.objects.exists()` (impede 2ª linha) e `has_delete_permission(self, request, obj=None)` retorna `False` (singleton nunca deletado). `list_display = ["valor_maximo_gestor"]`. (D-08, D-11, APROV-06)
    - `@admin.register(AprovacaoLog)` com `AprovacaoLogAdmin(admin.ModelAdmin)`: `list_display = ["requisicao", "aprovador", "evento", "criado_em"]`, `list_filter = ["evento"]`, `readonly_fields = ["requisicao", "aprovador", "evento", "motivo", "criado_em", "atualizado_em"]`, `has_add_permission` retorna `False` e `has_delete_permission` retorna `False` (logs imutáveis, REQ-03). (REQ-03)

    Em `static/css/main.css`: adicionar duas classes de badge novas após a última `.badge-*` existente: `.badge-rascunho` com `background:#374151; color:#9ca3af;` e `.badge-cancelado` com `background:#374151; color:#9ca3af;`. Estas serão usadas pelos templates de status nos planos seguintes para os estados RASCUNHO e CANCELADO (que não têm badge no design system atual).
  </action>
  <verify>
    <automated>python -c "import django,os;os.environ.setdefault('DJANGO_SETTINGS_MODULE','config.settings.dev');django.setup();from django.contrib import admin;from apps.requisicoes.models import CategoriaCompra;from apps.aprovacoes.models import ConfiguracaoAlcada,AprovacaoLog;assert CategoriaCompra in admin.site._registry;assert ConfiguracaoAlcada in admin.site._registry;assert AprovacaoLog in admin.site._registry"; grep -q "badge-rascunho" static/css/main.css && grep -q "badge-cancelado" static/css/main.css && echo "CSS OK"