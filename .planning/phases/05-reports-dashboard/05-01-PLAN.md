---
phase: 05-reports-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/relatorios/__init__.py
  - apps/relatorios/apps.py
  - apps/relatorios/services.py
  - apps/relatorios/tests/__init__.py
  - apps/relatorios/tests/conftest.py
  - apps/relatorios/tests/test_services.py
  - apps/relatorios/tests/test_views.py
  - apps/core/tests/__init__.py
  - apps/core/tests/test_dashboard.py
  - apps/core/views.py
  - apps/core/templates/core/dashboard.html
  - config/settings/base.py
autonomous: true
requirements: [REL-01, UNIT-04]
must_haves:
  truths:
    - "App relatorios is registered in INSTALLED_APPS and importable"
    - "Dashboard displays real KPI values (open requisitions, active RFQs, current-month spend, active suppliers) instead of em-dash stubs"
    - "Solicitante sees KPIs filtered by user.default_unit; comprador/diretor/admin see global KPIs"
    - "Gasto do Mês sums winner preco_unitario for current month AND year only"
    - "Test scaffold exists and fails RED (no implementation in views yet) for the relatorios views"
  artifacts:
    - path: "apps/relatorios/services.py"
      provides: "get_dashboard_kpis, get_gastos_por_categoria, get_requisicoes_painel"
      contains: "def get_dashboard_kpis"
      min_lines: 60
    - path: "apps/relatorios/__init__.py"
      provides: "relatorios app package marker"
    - path: "apps/relatorios/apps.py"
      provides: "RelatoriosConfig"
      contains: "class RelatoriosConfig"
    - path: "apps/relatorios/tests/conftest.py"
      provides: "test fixtures including rfq_com_vencedor and diretor_user"
      contains: "def rfq_com_vencedor"
    - path: "apps/relatorios/tests/test_services.py"
      provides: "RED service-layer tests for KPIs and aggregation"
      contains: "class TestDashboardKpis"
    - path: "apps/relatorios/tests/test_views.py"
      provides: "RED view tests for access control, gastos, requisicoes, PDF"
      contains: "class TestAcesso"
    - path: "apps/core/tests/test_dashboard.py"
      provides: "DashboardView context tests"
      contains: "def test_"
    - path: "apps/core/views.py"
      provides: "DashboardView enriched with get_context_data injecting kpis"
      contains: "get_context_data"
  key_links:
    - from: "apps/core/views.py"
      to: "apps.relatorios.services.get_dashboard_kpis"
      via: "import + call in get_context_data"
      pattern: "get_dashboard_kpis"
    - from: "apps/core/templates/core/dashboard.html"
      to: "kpis context dict"
      via: "template variable rendering"
      pattern: "kpis\\.(req_abertas|cotacoes_andamento|gasto_mes|fornecedores_ativos)"
    - from: "config/settings/base.py"
      to: "apps.relatorios"
      via: "INSTALLED_APPS entry"
      pattern: "apps\\.relatorios"
---

<objective>
Criar a fundação do app `relatorios` (D-08): pacote Python, `RelatoriosConfig`, registro em `INSTALLED_APPS`, e a service layer central (`services.py`) com as três funções de query de toda a fase. Estabelecer o scaffold de testes Wave 0 (RED) e enriquecer a `DashboardView` existente (D-09) para exibir KPIs reais no dashboard (REL-01), com filtro por role (D-02, UNIT-04).

Purpose: Sem o app registrado e a service layer, nenhuma outra parte da fase (relatórios, PDFs) pode importar as queries. O dashboard é a primeira tela visível que entrega valor end-to-end — esta é a fatia vertical inicial (MVP) que faz os 4 KPIs reais aparecerem.
Output: app `relatorios` registrado, `services.py` com `get_dashboard_kpis` / `get_gastos_por_categoria` / `get_requisicoes_painel`, scaffold de testes falhando em RED para as views, dashboard com KPIs reais.
</objective>

<contract_note>
**CONTRATO CANÔNICO DE CHAVES DE KPI (autoritativo — vence RESEARCH.md):**
O dict retornado por `get_dashboard_kpis(user)` DEVE usar exatamente estas 4 chaves:
`req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos`.

Estas chaves são o contrato vinculante definido na `05-VALIDATION.md` (T-05-01) e consumido pelo template `core/dashboard.html` e pelos testes de `apps/core/tests/test_dashboard.py`. O `05-RESEARCH.md` Pattern 1 mostra nomes diferentes (`requisicoes_abertas`, `cotacoes_em_andamento`) — esses nomes da RESEARCH.md estão DESATUALIZADOS e NÃO devem ser usados. Em qualquer divergência entre RESEARCH.md e este contrato, este contrato prevalece.
</contract_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reports-dashboard/05-CONTEXT.md
@.planning/phases/05-reports-dashboard/05-RESEARCH.md
@.planning/phases/05-reports-dashboard/05-PATTERNS.md
@.planning/phases/05-reports-dashboard/05-VALIDATION.md
@CLAUDE.md
</context>

## Phase Goal

**As a** usuário de qualquer perfil, **I want to** ver KPIs reais no dashboard e abrir relatórios de gasto e de requisições filtráveis por período e unidade com exportação em PDF, **so that** eu tenha visibilidade total dos gastos sem depender de planilhas manuais.

<tasks>

<task type="auto">
  <name>Task 1: Scaffold do app relatorios + registro em INSTALLED_APPS</name>
  <files>apps/relatorios/__init__.py, apps/relatorios/apps.py, apps/relatorios/tests/__init__.py, config/settings/base.py</files>
  <read_first>
    - apps/relatorios/apps.py (se já existir — não assumir; criar do zero se ausente)
    - config/settings/base.py (ver INSTALLED_APPS lines 19-37 — entradas seguem o formato "apps.<name>")
    - apps/cotacoes/apps.py (analog de AppConfig — mesma estrutura para todos os apps)
    - .planning/phases/05-reports-dashboard/05-RESEARCH.md §apps.py do novo app relatorios (boilerplate confirmado)
  </read_first>
  <action>
    Criar o pacote Python `apps/relatorios/` com `__init__.py` vazio e `apps/relatorios/tests/__init__.py` vazio.
    Criar `apps/relatorios/apps.py` com a classe `RelatoriosConfig(AppConfig)` definindo:
      - `default_auto_field = "django.db.models.BigAutoField"`
      - `name = "apps.relatorios"`
      - `verbose_name = "Relatórios"`
    O app NÃO tem models (D-08) — não criar `models.py`.
    Em `config/settings/base.py`, adicionar `"apps.relatorios",` na lista `INSTALLED_APPS` imediatamente após `"apps.cotacoes",` (atualmente a última entrada de Local apps na linha 36). Manter a indentação e a vírgula final consistentes com as entradas existentes.
    Não criar `urls.py` nem `views.py` nesta task — eles entram no plano 05-02.
  </action>
  <verify>
    <automated>docker compose exec web python manage.py check</automated>
  </verify>
  <acceptance_criteria>
    - `apps/relatorios/__init__.py` existe (pode estar vazio)
    - `apps/relatorios/tests/__init__.py` existe (pode estar vazio)
    - `apps/relatorios/apps.py` contém exatamente `class RelatoriosConfig(AppConfig):` com `name = "apps.relatorios"` e `verbose_name = "Relatórios"`
    - `config/settings/base.py` contém a string `"apps.relatorios"` dentro de INSTALLED_APPS, posicionada após `"apps.cotacoes"`
    - `python manage.py check` retorna "System check identified no issues"
    - Nenhum arquivo `apps/relatorios/models.py` é criado (D-08 — app sem models)
  </acceptance_criteria>
  <done>App relatorios registrado e reconhecido pelo Django; `manage.py check` passa sem erros.</done>
</task>

<task type="auto">
  <name>Task 2: Service layer — get_dashboard_kpis, get_gastos_por_categoria, get_requisicoes_painel</name>
  <files>apps/relatorios/services.py</files>
  <read_first>
    - apps/cotacoes/services.py (analog exact — imports, type hints, docstrings com exceções, padrão Sum com fallback)
    - apps/aprovacoes/services.py (analog de service layer — toda lógica de query vive aqui)
    - apps/accounts/models.py (campo CORRETO é `user.default_unit`, linha 44 — NUNCA `user.unidade`; role choices linha 27-32)
    - apps/cotacoes/models.py (RFQ.vencedor com related_name="rfqs_vencidos" linha 43; CotacaoFornecedor.preco_unitario linha 82; RFQ.atualizado_em via TimestampedModel)
    - apps/requisicoes/models.py (Requisicao.Status choices linha 35-41: RASCUNHO/PENDENTE_GESTOR/PENDENTE_DIRETOR/APROVADO/REPROVADO/CANCELADO; categoria FK linha 50; unidade FK linha 58)
    - apps/fornecedores/models.py (Fornecedor.ativo)
    - .planning/phases/05-reports-dashboard/05-RESEARCH.md §Pattern 1, §Pattern 2, §Common Pitfalls 1-3
  </read_first>
  <action>
    **ATENÇÃO — CONTRATO DE CHAVES (vence RESEARCH.md):** o dict de `get_dashboard_kpis` DEVE usar EXATAMENTE as chaves `req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos` (contrato T-05-01 da VALIDATION.md). O RESEARCH.md Pattern 1 mostra `requisicoes_abertas` / `cotacoes_em_andamento` — esses nomes estão DESATUALIZADOS; IGNORÁ-LOS e usar as chaves canônicas listadas aqui. Veja `<contract_note>` no topo deste plano.

    Criar `apps/relatorios/services.py` com imports cross-app: `from datetime import date`, `from decimal import Decimal`, `from django.db.models import Count, F, Sum`, e os modelos `CotacaoFornecedor, RFQ` de `apps.cotacoes.models`, `Fornecedor` de `apps.fornecedores.models`, `Requisicao` de `apps.requisicoes.models`.

    Implementar `get_dashboard_kpis(user) -> dict` (REL-01, D-01, D-02):
      - Calcular `hoje = date.today()`, `mes_atual = hoje.month`, `ano_atual = hoje.year`.
      - Filtro de unidade para solicitante: se `user.role == "solicitante"` E `user.default_unit` existir, aplicar `{"unidade": user.default_unit}` nas requisições. Para comprador/diretor/admin não filtrar (global). USAR `user.default_unit` — NUNCA `user.unidade` (causa AttributeError).
      - `req_abertas` = `Requisicao.objects.filter(status__in=[Requisicao.Status.PENDENTE_GESTOR, Requisicao.Status.PENDENTE_DIRETOR], **filtro_unidade).count()` — exclui RASCUNHO (D-02). Chave canônica do dict: `req_abertas`.
      - `cotacoes_andamento` = `RFQ.objects.filter(vencedor_id__isnull=True).count()` — global para todos os roles (D-02). Chave canônica do dict: `cotacoes_andamento`.
      - `gasto_mes` (D-01): para solicitante aplicar `{"rfqs_vencidos__requisicao__unidade": user.default_unit}`; query `CotacaoFornecedor.objects.filter(rfqs_vencidos__atualizado_em__month=mes_atual, rfqs_vencidos__atualizado_em__year=ano_atual, **filtro_gasto).aggregate(total=Sum("preco_unitario"))`. Aplicar fallback obrigatório: `resultado["total"] or Decimal("0")`. FILTRAR POR `__month` E `__year` juntos (Pitfall 2 — sem `__year` acumula meses de anos anteriores). Chave canônica do dict: `gasto_mes`.
      - `fornecedores_ativos` = `Fornecedor.objects.filter(ativo=True).count()` — global (D-02). Chave canônica do dict: `fornecedores_ativos`.
      - Retornar dict com as chaves EXATAS E CANÔNICAS: `{"req_abertas": ..., "cotacoes_andamento": ..., "gasto_mes": ..., "fornecedores_ativos": ...}` (contrato de teste T-05-01). NÃO usar `requisicoes_abertas` nem `cotacoes_em_andamento` (nomes obsoletos do RESEARCH.md Pattern 1).

    Implementar `get_gastos_por_categoria(data_inicio, data_fim, unidade_id=None) -> list` (REL-02, D-05, UNIT-04):
      - `qs = CotacaoFornecedor.objects.filter(rfqs_vencidos__atualizado_em__date__gte=data_inicio, rfqs_vencidos__atualizado_em__date__lte=data_fim, rfqs_vencidos__isnull=False)`.
      - Se `unidade_id`: `qs = qs.filter(rfqs_vencidos__requisicao__unidade_id=unidade_id)`.
      - Retornar `list(qs.values(categoria_nome=F("rfqs_vencidos__requisicao__categoria__nome")).annotate(total=Sum("preco_unitario")).order_by("-total"))`.

    Implementar `get_requisicoes_painel(status=None, unidade_id=None)` (REL-03, UNIT-04):
      - `qs = Requisicao.objects.select_related("categoria", "unidade", "criado_por").order_by("-criado_em")`.
      - Se `status`: `qs = qs.filter(status=status)`.
      - Se `unidade_id`: `qs = qs.filter(unidade_id=unidade_id)`.
      - Retornar `qs`.

    Cada função com type hints e docstring listando o comportamento (padrão de cotacoes/services.py). NENHUMA query de negócio fora deste arquivo.
  </action>
  <verify>
    <automated>docker compose exec web python -c "from apps.relatorios import services; assert hasattr(services, 'get_dashboard_kpis') and hasattr(services, 'get_gastos_por_categoria') and hasattr(services, 'get_requisicoes_painel')"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/relatorios/services.py` define `get_dashboard_kpis`, `get_gastos_por_categoria`, `get_requisicoes_painel`
    - `get_dashboard_kpis` usa `user.default_unit` (string `default_unit` presente; string `user.unidade` ausente)
    - A query de `gasto_mes` contém AMBOS `__month` e `__year` (grep confirma `atualizado_em__month` E `atualizado_em__year` no arquivo, ignorando comentários)
    - Todo `aggregate(total=Sum(...))` é seguido de `or Decimal("0")` (fallback obrigatório)
    - **CONTRATO DE CHAVES (T-05-01):** `get_dashboard_kpis` retorna dict com as chaves canônicas EXATAS `req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos`. Estas chaves vencem o RESEARCH.md Pattern 1 (que usa os nomes obsoletos `requisicoes_abertas`/`cotacoes_em_andamento`). O arquivo NÃO deve conter as chaves `requisicoes_abertas` nem `cotacoes_em_andamento` como chaves do dict de retorno.
    - `req_abertas` filtra `status__in` com PENDENTE_GESTOR e PENDENTE_DIRETOR e NÃO inclui RASCUNHO
    - Import do módulo funciona sem erro (verify automatizado passa)
  </acceptance_criteria>
  <done>Service layer com as 3 funções importável; KPIs calculados via ORM com fallback de None e filtro month+year corretos; dict retornado usa as chaves canônicas `req_abertas`/`cotacoes_andamento`/`gasto_mes`/`fornecedores_ativos` (T-05-01).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scaffold de testes Wave 0 (RED) + enriquecer DashboardView e template</name>
  <files>apps/relatorios/tests/conftest.py, apps/relatorios/tests/test_services.py, apps/relatorios/tests/test_views.py, apps/core/tests/__init__.py, apps/core/tests/test_dashboard.py, apps/core/views.py, apps/core/templates/core/dashboard.html</files>
  <behavior>
    - test_services TestDashboardKpis: gasto_mes soma preco_unitario dos vencedores do mês+ano corrente (usa fixture rfq_com_vencedor); gasto_mes retorna Decimal("0") sem vencedores; solicitante filtra por default_unit, comprador vê global (T-05-03, T-05-04, T-05-05). As asserções leem as chaves canônicas `kpis["gasto_mes"]`, `kpis["req_abertas"]`, etc.
    - test_services TestGastos: get_gastos_por_categoria retorna lista de dicts {categoria_nome, total} com totais corretos no período (T-05-... REL-02).
    - test_services TestRequisicoesPainel: get_requisicoes_painel filtra por status e unidade_id (REL-03).
    - test_services TestFiltroUnidade: filtro de unidade altera o resultado (UNIT-04).
    - test_views TestAcesso: solicitante recebe 403 em /relatorios/gastos/; comprador, diretor e admin recebem 200 (T-05-12). ESTES TESTES FALHAM EM RED porque as views ainda não existem (plano 05-02).
    - test_views TestGastosView / TestRequisicoesPainel / TestPDF: marcados/escritos para falhar RED até 05-02/05-03 (T-05-06..11).
    - test_dashboard: DashboardView injeta `kpis` no contexto com as 4 chaves canônicas `req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos`; req_abertas conta só PENDENTE_GESTOR+PENDENTE_DIRETOR (T-05-01, T-05-02); solicitante filtrado por unidade (T-05-05).
  </behavior>
  <read_first>
    - apps/cotacoes/tests/conftest.py (analog exact — fixtures test_unit, comprador_user, solicitante_user, admin_user, categoria, fornecedor, requisicao_aprovada)
    - apps/cotacoes/tests/test_views.py (analog — estrutura de classes de teste, client.force_login)
    - apps/cotacoes/tests/test_services.py (analog — estrutura de testes de service)
    - apps/core/views.py (stub atual — DashboardView com apenas template_name, linha 5-6)
    - apps/core/templates/core/dashboard.html (stub atual — 4 cards com &mdash;, linha 7-25)
    - .planning/phases/05-reports-dashboard/05-PATTERNS.md §conftest.py (fixtures rfq_com_vencedor, diretor_user), §test_views.py, §DashboardView enrich, §dashboard.html populate
    - .planning/phases/05-reports-dashboard/05-VALIDATION.md (tabela T-05-01..12 — contrato de chaves de KPI e IDs de teste)
  </read_first>
  <action>
    **ATENÇÃO — CONTRATO DE CHAVES (vence RESEARCH.md):** todos os testes e o template DEVEM ler as chaves canônicas `req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos` (contrato T-05-01 da VALIDATION.md). O RESEARCH.md Pattern 1 e os exemplos de template do RESEARCH.md usam os nomes obsoletos `requisicoes_abertas`/`cotacoes_em_andamento` — IGNORÁ-LOS. Veja `<contract_note>` no topo deste plano.

    Criar `apps/relatorios/tests/conftest.py` copiando as fixtures de `apps/cotacoes/tests/conftest.py` (test_unit, comprador_user, solicitante_user, admin_user, categoria, fornecedor, requisicao_aprovada) e ADICIONAR:
      - `diretor_user` (role=User.Role.DIRETOR, default_unit=test_unit).
      - `rfq_com_vencedor(db, requisicao_aprovada, comprador_user, fornecedor)`: cria RFQ, cria CotacaoFornecedor com preco_unitario=Decimal("1500.00"), seta `rfq.vencedor = cotacao`, `rfq.justificativa_selecao = "Menor preço"`, e `rfq.save(update_fields=["vencedor", "justificativa_selecao", "atualizado_em"])`. Retorna o rfq.

    Criar `apps/relatorios/tests/test_services.py` com classes:
      - `TestDashboardKpis`: `test_gasto_mes_soma_vencedores_mes_corrente` (usa rfq_com_vencedor, asserta `kpis["gasto_mes"] == Decimal("1500.00")`); `test_gasto_mes_zero_sem_vencedores` (asserta `kpis["gasto_mes"] == Decimal("0")`); `test_solicitante_filtrado_por_unidade` e `test_comprador_ve_global`. As asserções usam as chaves canônicas `req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos`.
      - `TestGastos`: `test_retorna_total_por_categoria`.
      - `TestRequisicoesPainel`: `test_filtra_por_status`, `test_filtra_por_unidade`.
      - `TestFiltroUnidade`: `test_filtro_unidade_altera_resultado`.
      Os testes de service DEVEM passar GREEN (services.py existe da Task 2).

    Criar `apps/relatorios/tests/test_views.py` com classes `TestAcesso`, `TestGastosView`, `TestRequisicoesPainel`, `TestPDF` seguindo o analog de cotacoes/tests/test_views.py. Estes testes batem em URLs `/relatorios/gastos/`, `/relatorios/requisicoes/`, `/relatorios/gastos/pdf/`, `/relatorios/requisicoes/pdf/` que AINDA NÃO EXISTEM — eles FALHAM EM RED (404/NoReverseMatch). Esse é o comportamento esperado de Wave 0; GREEN vem em 05-02/05-03. Documentar no docstring de cada classe que o RED é intencional.

    Criar `apps/core/tests/__init__.py` (vazio) e `apps/core/tests/test_dashboard.py` testando: DashboardView injeta `kpis` no contexto com as chaves canônicas `req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos` (T-05-01); `req_abertas` conta apenas PENDENTE_GESTOR+PENDENTE_DIRETOR (T-05-02); solicitante filtrado por unidade (T-05-05). Usar `client.force_login` + `client.get("/")` ou `reverse("core:dashboard")` e ler `response.context["kpis"]`. Estes testes PASSAM GREEN após enriquecer a view abaixo. NÃO referenciar `requisicoes_abertas`/`cotacoes_em_andamento`.

    Enriquecer `apps/core/views.py` (D-09): adicionar `from apps.relatorios import services as relatorios_services` e o método `get_context_data(self, **kwargs)` que chama `super().get_context_data(**kwargs)`, injeta `ctx["kpis"] = relatorios_services.get_dashboard_kpis(self.request.user)` e retorna ctx. NÃO criar nova view.

    Atualizar `apps/core/templates/core/dashboard.html`: substituir os quatro `&mdash;` pelos valores `{{ kpis.req_abertas }}`, `{{ kpis.cotacoes_andamento }}`, `R$ {{ kpis.gasto_mes|floatformat:2 }}`, `{{ kpis.fornecedores_ativos }}` (Pattern 5 — formatação BR via floatformat com locale pt-br). USAR as chaves canônicas (NÃO `kpis.requisicoes_abertas` nem `kpis.cotacoes_em_andamento`). Remover o `<p>` de stub "Dashboard em construção".
  </action>
  <verify>
    <automated>docker compose exec web python manage.py test apps.relatorios.tests.test_services apps.core.tests.test_dashboard -v 1</automated>
  </verify>
  <acceptance_criteria>
    - `apps/relatorios/tests/conftest.py` define `rfq_com_vencedor` e `diretor_user`
    - `apps/relatorios/tests/test_services.py` define classes `TestDashboardKpis`, `TestGastos`, `TestRequisicoesPainel`, `TestFiltroUnidade` e os testes de service passam GREEN
    - `apps/relatorios/tests/test_views.py` define `TestAcesso`, `TestGastosView`, `TestRequisicoesPainel`, `TestPDF` (RED esperado — views ainda não existem)
    - **CONTRATO DE CHAVES (T-05-01):** `apps/core/tests/test_dashboard.py` asserta as 4 chaves canônicas `req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos` no contexto e passa GREEN. NÃO usa `requisicoes_abertas` nem `cotacoes_em_andamento`.
    - `apps/core/views.py` contém `get_context_data` e importa `apps.relatorios.services`
    - **CONTRATO DE CHAVES (T-05-01):** `apps/core/templates/core/dashboard.html` contém `kpis.req_abertas`, `kpis.cotacoes_andamento`, `kpis.gasto_mes|floatformat:2`, `kpis.fornecedores_ativos`, NÃO contém mais `&mdash;`, e NÃO referencia `kpis.requisicoes_abertas` nem `kpis.cotacoes_em_andamento`
    - `manage.py test apps.relatorios.tests.test_services apps.core.tests.test_dashboard` passa (estes módulos GREEN); os testes de view RED ficam isolados em test_views.py
  </acceptance_criteria>
  <done>Scaffold de testes Wave 0 criado (test_views RED intencional); service e dashboard tests GREEN com as chaves canônicas; DashboardView exibe KPIs reais com filtro por role.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → DashboardView | Usuário autenticado solicita o dashboard; role determina escopo dos KPIs |
| service → DB (ORM) | Queries de agregação cross-app leem dados de todas as fases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | get_dashboard_kpis (solicitante) | mitigate | Service filtra por `user.default_unit` quando `user.role == "solicitante"`; nunca expõe dados globais a solicitante (D-02) |
| T-05-02 | Elevation of Privilege | DashboardView | mitigate | `LoginRequiredMixin` já aplicado — apenas autenticados acessam |
| T-05-03 | Tampering | gasto_mes query | accept | Query usa `__month` + `__year`; sem input do usuário no dashboard (KPIs derivam do role do request.user, não de params) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase — `reportlab` já em requirements.txt (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `docker compose exec web python manage.py check` — app relatorios reconhecido
- `docker compose exec web python manage.py test apps.relatorios.tests.test_services apps.core.tests.test_dashboard` — GREEN
- `docker compose exec web python manage.py test apps.requisicoes apps.cotacoes apps.fornecedores apps.accounts` — sem regressão
- Dashboard manual: login como comprador → 4 cards exibem números reais; login como solicitante → KPIs filtrados pela unidade
</verification>

<success_criteria>
- App relatorios registrado em INSTALLED_APPS e importável
- services.py expõe as 3 funções de query com `user.default_unit`, fallback `Decimal("0")` e filtro month+year
- `get_dashboard_kpis` retorna dict com as chaves canônicas `req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos` (T-05-01 — vence RESEARCH.md Pattern 1)
- Scaffold de testes Wave 0 existe; test_services e test_dashboard GREEN; test_views RED (esperado)
- DashboardView enriquecida (D-09) exibe KPIs reais com filtro por role (REL-01, D-02, UNIT-04)
</success_criteria>

## Artifacts this phase produces (plano 05-01)

| Symbol | File | Type |
|--------|------|------|
| `RelatoriosConfig` | apps/relatorios/apps.py | AppConfig |
| `get_dashboard_kpis(user)` | apps/relatorios/services.py | function → dict |
| `get_gastos_por_categoria(data_inicio, data_fim, unidade_id=None)` | apps/relatorios/services.py | function → list[dict] |
| `get_requisicoes_painel(status=None, unidade_id=None)` | apps/relatorios/services.py | function → QuerySet |
| `rfq_com_vencedor` (fixture) | apps/relatorios/tests/conftest.py | pytest fixture |
| `diretor_user` (fixture) | apps/relatorios/tests/conftest.py | pytest fixture |
| `DashboardView.get_context_data` | apps/core/views.py | method (enriched) |
| KPI keys `req_abertas`, `cotacoes_andamento`, `gasto_mes`, `fornecedores_ativos` | context contract | dict keys |

<output>
Create `.planning/phases/05-reports-dashboard/05-01-SUMMARY.md` when done
</output>
