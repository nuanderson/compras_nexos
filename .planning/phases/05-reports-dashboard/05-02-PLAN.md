---
phase: 05-reports-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - apps/relatorios/views.py
  - apps/relatorios/urls.py
  - apps/relatorios/templates/relatorios/gastos.html
  - apps/relatorios/templates/relatorios/requisicoes.html
  - config/urls.py
autonomous: true
requirements: [REL-02, REL-03, UNIT-04]
must_haves:
  truths:
    - "Comprador/diretor/admin can open /relatorios/gastos/ and see spending totals by category for the current month"
    - "User can adjust data_inicio/data_fim and unidade filters and the table updates via GET submit"
    - "Solicitante is forbidden (403) from /relatorios/* views"
    - "Comprador/diretor/admin can open /relatorios/requisicoes/ and see all requisitions with status and unit filters"
    - "Diretor (not only comprador/admin) can access the reports"
  artifacts:
    - path: "apps/relatorios/views.py"
      provides: "RelatorioRequiredMixin, GastosView, RequisicoesPainelView, helper _parse_filtros, PDF view stubs"
      contains: "class RelatorioRequiredMixin"
      min_lines: 70
    - path: "apps/relatorios/urls.py"
      provides: "app_name relatorios + url routes for gastos and requisicoes and PDF"
      contains: "app_name = \"relatorios\""
    - path: "apps/relatorios/templates/relatorios/gastos.html"
      provides: "spending report table + date/unit filter form"
      contains: "data_inicio"
    - path: "apps/relatorios/templates/relatorios/requisicoes.html"
      provides: "requisitions status panel + status/unit filter form"
      contains: "name=\"status\""
  key_links:
    - from: "apps/relatorios/views.py"
      to: "apps.relatorios.services.get_gastos_por_categoria"
      via: "service call in GastosView.get"
      pattern: "get_gastos_por_categoria"
    - from: "apps/relatorios/views.py"
      to: "apps.relatorios.services.get_requisicoes_painel"
      via: "service call in RequisicoesPainelView.get"
      pattern: "get_requisicoes_painel"
    - from: "config/urls.py"
      to: "apps.relatorios.urls"
      via: "include() before core catch-all"
      pattern: "apps\\.relatorios\\.urls"
    - from: "apps/relatorios/templates/relatorios/gastos.html"
      to: "relatorios:gastos-pdf"
      via: "Exportar PDF link with request.GET.urlencode"
      pattern: "relatorios:gastos-pdf"
---

<objective>
Construir a fatia vertical dos dois relatórios HTML: `GastosView` (REL-02 — gasto por categoria e período, filtrável por unidade) e `RequisicoesPainelView` (REL-03 — painel de status com filtro por status e unidade). Criar o `RelatorioRequiredMixin` que inclui `diretor` (além de comprador/admin), registrar as URLs do app e ligar em `config/urls.py`, e criar os templates com formulários de filtro via GET (D-03, D-04, UNIT-04).

Purpose: Esta é a segunda fatia vertical — depois do dashboard, o usuário autorizado consegue navegar até relatórios filtráveis end-to-end (URL → view → service → template). Os endpoints HTML são consumidos pelos endpoints PDF do plano 05-03, então a estrutura de filtros e o helper `_parse_filtros` precisam existir primeiro.
Output: views GastosView e RequisicoesPainelView funcionais (200), URLs registradas, templates renderizando tabelas com filtros, acesso restrito a comprador/diretor/admin (403 para solicitante).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reports-dashboard/05-CONTEXT.md
@.planning/phases/05-reports-dashboard/05-RESEARCH.md
@.planning/phases/05-reports-dashboard/05-PATTERNS.md
@.planning/phases/05-reports-dashboard/05-01-PLAN.md
@CLAUDE.md
</context>

## Phase Goal

**As a** usuário de qualquer perfil, **I want to** ver KPIs reais no dashboard e abrir relatórios de gasto e de requisições filtráveis por período e unidade com exportação em PDF, **so that** eu tenha visibilidade total dos gastos sem depender de planilhas manuais.

<tasks>

<task type="auto">
  <name>Task 1: RelatorioRequiredMixin + GastosView + RequisicoesPainelView + helper + PDF stubs</name>
  <files>apps/relatorios/views.py</files>
  <read_first>
    - apps/fornecedores/views.py (analog exact — CompradorRequiredMixin linha 25-41, padrão de GET view com construção de contexto linha 91-109)
    - apps/relatorios/services.py (criado em 05-01 — assinaturas get_gastos_por_categoria(data_inicio, data_fim, unidade_id), get_requisicoes_painel(status, unidade_id))
    - apps/accounts/models.py (User.Role choices linha 27-32 e campo default_unit linha 44)
    - apps/requisicoes/models.py (Requisicao.Status choices linha 35-41 para popular o select de status)
    - .planning/phases/05-reports-dashboard/05-PATTERNS.md §Critical Notes for Planner item 1 (RelatorioRequiredMixin DEVE incluir diretor), §GET Params Parsing
    - .planning/phases/05-reports-dashboard/05-RESEARCH.md §Known Threat Patterns (date injection — validar com strptime + try/except)
  </read_first>
  <action>
    Criar `apps/relatorios/views.py`.

    Imports: `from datetime import date, datetime`, `from django.contrib.auth.mixins import LoginRequiredMixin`, `from django.core.exceptions import PermissionDenied`, `from django.http import HttpResponse`, `from django.shortcuts import render`, `from django.views import View`, `from apps.accounts.models import UnidadeOrganizacional`, `from apps.requisicoes.models import Requisicao`, `from . import services`.

    Definir `RelatorioRequiredMixin(LoginRequiredMixin)` (Critical Note 1 — NÃO reutilizar CompradorRequiredMixin, que exclui diretor): no `dispatch`, se não autenticado → `self.handle_no_permission()`; se NÃO (`request.user.is_superuser` OU `request.user.role in ("comprador", "diretor", "admin")`) → `raise PermissionDenied`; senão `super().dispatch(...)`. INCLUIR "diretor" explicitamente (D-02).

    Definir helper de módulo `_parse_filtros(request)` que retorna `(data_inicio, data_fim, unidade_id)`:
      - Default: `data_inicio = date.today().replace(day=1)` e `data_fim = date.today()` (D-03).
      - `data_inicio_raw = request.GET.get("data_inicio", "")`; se não vazio, tentar `datetime.strptime(data_inicio_raw, "%Y-%m-%d").date()` dentro de try/except ValueError; em falha de parse, manter o default (segurança — date injection, RESEARCH §Known Threat Patterns).
      - Mesma lógica para `data_fim` (`request.GET.get("data_fim", "")`).
      - `unidade_id = request.GET.get("unidade", "") or None`.
      - Retornar `(data_inicio, data_fim, unidade_id)`.

    Definir `GastosView(RelatorioRequiredMixin, View)` com método `get(self, request)` (REL-02, D-05, UNIT-04):
      - `data_inicio, data_fim, unidade_id = _parse_filtros(request)`.
      - `dados = services.get_gastos_por_categoria(data_inicio, data_fim, unidade_id)`.
      - Construir ctx com: `gastos_por_categoria=dados`, `data_inicio=data_inicio.isoformat()`, `data_fim=data_fim.isoformat()`, `unidades=UnidadeOrganizacional.objects.filter(ativo=True)`, `unidade_selecionada=unidade_id or ""`, `pode_filtrar_unidade=True`.
      - `return render(request, "relatorios/gastos.html", ctx)`.

    Definir `RequisicoesPainelView(RelatorioRequiredMixin, View)` com `get(self, request)` (REL-03, UNIT-04):
      - `status = request.GET.get("status", "") or None`, `unidade_id = request.GET.get("unidade", "") or None`.
      - `requisicoes = services.get_requisicoes_painel(status, unidade_id)`.
      - ctx: `requisicoes=requisicoes`, `status_choices=Requisicao.Status.choices`, `status_selecionado=status or ""`, `unidades=UnidadeOrganizacional.objects.filter(ativo=True)`, `unidade_selecionada=unidade_id or ""`.
      - `return render(request, "relatorios/requisicoes.html", ctx)`.

    Definir STUBS de PDF para evitar ImportError em urls.py (corpo real implementado em 05-03):
      - `class GastosPDFView(RelatorioRequiredMixin, View)` com `def get(self, request): return HttpResponse(status=501)` e comentário `# STUB — corpo real do PDF implementado em 05-03`.
      - `class RequisicoesPDFView(RelatorioRequiredMixin, View)` com `def get(self, request): return HttpResponse(status=501)` e o mesmo comentário.

    NENHUMA query ORM de negócio na view — apenas `UnidadeOrganizacional.objects.filter(ativo=True)` para popular o select (consulta de UI, aceitável) e chamadas ao service.
  </action>
  <verify>
    <automated>docker compose exec web python -c "from apps.relatorios import views; assert all(hasattr(views, n) for n in ['RelatorioRequiredMixin','GastosView','RequisicoesPainelView','GastosPDFView','RequisicoesPDFView','_parse_filtros'])"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/relatorios/views.py` define `RelatorioRequiredMixin`, `GastosView`, `RequisicoesPainelView`, `GastosPDFView`, `RequisicoesPDFView`, `_parse_filtros`
    - `RelatorioRequiredMixin.dispatch` verifica `role in ("comprador", "diretor", "admin")` — a string "diretor" está presente
    - `_parse_filtros` usa `datetime.strptime(..., "%Y-%m-%d")` dentro de try/except e mantém default em falha
    - `_parse_filtros` default de data_inicio é `date.today().replace(day=1)` e data_fim é `date.today()` (D-03)
    - `GastosView.get` chama `services.get_gastos_por_categoria` e `RequisicoesPainelView.get` chama `services.get_requisicoes_painel`
    - Os PDF stubs retornam `HttpResponse(status=501)` e carregam comentário indicando implementação em 05-03
    - Import do módulo passa (verify automatizado)
  </acceptance_criteria>
  <done>Views de relatório implementadas com mixin incluindo diretor, helper de filtros com validação de data, delegação ao service e stubs de PDF para destravar o registro de URLs.</done>
</task>

<task type="auto">
  <name>Task 2: URLs do app relatorios + wiring em config/urls.py</name>
  <files>apps/relatorios/urls.py, config/urls.py</files>
  <read_first>
    - apps/cotacoes/urls.py (analog exact — app_name + urlpatterns com path())
    - config/urls.py (estrutura atual — adicionar path antes do catch-all de core.urls, linha 8-17)
    - apps/relatorios/views.py (criado na Task 1 — GastosView, RequisicoesPainelView, GastosPDFView, RequisicoesPDFView)
    - .planning/phases/05-reports-dashboard/05-RESEARCH.md §urls.py do app relatorios (4 rotas, app_name="relatorios")
  </read_first>
  <action>
    Criar `apps/relatorios/urls.py`:
      - `from django.urls import path`
      - `from .views import GastosView, GastosPDFView, RequisicoesPainelView, RequisicoesPDFView`
      - `app_name = "relatorios"`
      - `urlpatterns` com as 4 rotas:
        - `path("gastos/", GastosView.as_view(), name="gastos")`
        - `path("gastos/pdf/", GastosPDFView.as_view(), name="gastos-pdf")`
        - `path("requisicoes/", RequisicoesPainelView.as_view(), name="requisicoes")`
        - `path("requisicoes/pdf/", RequisicoesPDFView.as_view(), name="requisicoes-pdf")`

    Em `config/urls.py`, adicionar `path("relatorios/", include("apps.relatorios.urls")),` IMEDIATAMENTE ANTES da linha `path("", include("apps.core.urls")),` (o catch-all de core deve permanecer por último).
  </action>
  <verify>
    <automated>docker compose exec web python -c "from django.urls import reverse; assert reverse('relatorios:gastos')=='/relatorios/gastos/'; assert reverse('relatorios:requisicoes')=='/relatorios/requisicoes/'; assert reverse('relatorios:gastos-pdf')=='/relatorios/gastos/pdf/'; assert reverse('relatorios:requisicoes-pdf')=='/relatorios/requisicoes/pdf/'"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/relatorios/urls.py` define `app_name = "relatorios"` e as 4 rotas com os nomes `gastos`, `gastos-pdf`, `requisicoes`, `requisicoes-pdf`
    - `config/urls.py` contém `path("relatorios/", include("apps.relatorios.urls"))` ANTES de `path("", include("apps.core.urls"))`
    - `reverse("relatorios:gastos")` resolve para `/relatorios/gastos/` (e análogos para as demais 3 rotas)
  </acceptance_criteria>
  <done>URLs do app registradas e resolvendo; config/urls.py liga o app mantendo o catch-all de core por último.</done>
</task>

<task type="auto">
  <name>Task 3: Templates gastos.html e requisicoes.html com filtros GET</name>
  <files>apps/relatorios/templates/relatorios/gastos.html, apps/relatorios/templates/relatorios/requisicoes.html</files>
  <read_first>
    - apps/core/templates/core/dashboard.html (padrão de extends base.html + blocks title/page_title/content)
    - apps/fornecedores/templates/fornecedores/lista.html (analog role-match — formulário de filtro + tabela)
    - apps/relatorios/views.py (chaves de contexto definidas na Task 1: gastos_por_categoria, data_inicio, data_fim, unidades, unidade_selecionada, pode_filtrar_unidade; requisicoes, status_choices, status_selecionado)
    - .planning/phases/05-reports-dashboard/05-RESEARCH.md §Code Examples (Filtro de período e unidade no template, Card KPI), §Pattern 5 (formatação BR floatformat)
    - .planning/phases/05-reports-dashboard/05-PATTERNS.md §gastos.html (CSS classes em uso: .card, .table-container, .form-group, .form-label, .form-input, .form-select, .btn, .btn-primary, .btn-secondary, .mb-lg, .text-muted, .badge)
  </read_first>
  <action>
    Criar `apps/relatorios/templates/relatorios/gastos.html` estendendo `base.html`:
      - Blocks `title` = "Relatório de Gastos — ComprasNexos" e `page_title` = "Gastos por Categoria".
      - Bloco `content` com um `<form method="get" class="mb-lg">` contendo:
        - input `type="date" name="data_inicio" value="{{ data_inicio }}"` (label "De"), input `type="date" name="data_fim" value="{{ data_fim }}"` (label "Até").
        - `{% if pode_filtrar_unidade %}` um `<select name="unidade" class="form-select">` com `<option value="">Todas</option>` e loop `{% for u in unidades %}<option value="{{ u.pk }}" {% if u.pk|stringformat:"s" == unidade_selecionada %}selected{% endif %}>{{ u.nome }}</option>{% endfor %}` (D-04).
        - `<button type="submit" class="btn btn-primary">Filtrar</button>`.
        - `<a href="{% url 'relatorios:gastos-pdf' %}?{{ request.GET.urlencode }}" class="btn btn-secondary">Exportar PDF</a>` (D-06 — mesmos filtros via GET).
      - Tabela dentro de `.table-container`: cabeçalho "Categoria" / "Total (R$)"; loop `{% for row in gastos_por_categoria %}` exibindo `{{ row.categoria_nome|default:"Sem categoria" }}` e `R$ {{ row.total|floatformat:2 }}` (Pattern 5). `{% empty %}` linha "Nenhum gasto no período."

    Criar `apps/relatorios/templates/relatorios/requisicoes.html` estendendo `base.html`:
      - Blocks `title` = "Painel de Requisições — ComprasNexos" e `page_title` = "Status de Requisições".
      - `<form method="get" class="mb-lg">` com:
        - `<select name="status" class="form-select">` com `<option value="">Todos</option>` e loop `{% for value, label in status_choices %}<option value="{{ value }}" {% if value == status_selecionado %}selected{% endif %}>{{ label }}</option>{% endfor %}`.
        - `<select name="unidade" class="form-select">` com `<option value="">Todas</option>` e loop sobre `unidades` (mesmo padrão de gastos.html).
        - `<button type="submit" class="btn btn-primary">Filtrar</button>`.
        - `<a href="{% url 'relatorios:requisicoes-pdf' %}?{{ request.GET.urlencode }}" class="btn btn-secondary">Exportar PDF</a>` (D-06).
      - Tabela `.table-container`: colunas "Descrição", "Categoria", "Unidade", "Valor Estimado", "Status", "Criado em"; loop `{% for r in requisicoes %}` exibindo `{{ r.descricao }}`, `{{ r.categoria.nome }}`, `{{ r.unidade.nome }}`, `R$ {{ r.valor_estimado|floatformat:2 }}`, `<span class="badge">{{ r.get_status_display }}</span>`, `{{ r.criado_em|date:"d/m/Y" }}`. `{% empty %}` linha "Nenhuma requisição encontrada."
  </action>
  <verify>
    <automated>docker compose exec web python manage.py test apps.relatorios.tests.test_views -v 1</automated>
  </verify>
  <acceptance_criteria>
    - `apps/relatorios/templates/relatorios/gastos.html` contém inputs `name="data_inicio"`, `name="data_fim"`, o select `name="unidade"` sob `{% if pode_filtrar_unidade %}`, e o link `{% url 'relatorios:gastos-pdf' %}?{{ request.GET.urlencode }}`
    - `apps/relatorios/templates/relatorios/requisicoes.html` contém `<select name="status">` populado de `status_choices` e o link `{% url 'relatorios:requisicoes-pdf' %}`
    - Ambos os templates estendem `base.html` e usam as classes CSS existentes (.table-container, .form-select, .btn-primary, .btn-secondary)
    - Total exibido com `|floatformat:2` (formatação BR via locale pt-br)
    - `manage.py test apps.relatorios.tests.test_views` — TestAcesso (403 solicitante / 200 comprador-diretor-admin), TestGastosView e TestRequisicoesPainel passam GREEN (TestPDF ainda RED até 05-03)
  </acceptance_criteria>
  <done>Templates renderizam tabelas e formulários de filtro GET; views de relatório HTML respondem 200 com acesso restrito; testes de acesso e de relatório HTML GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → relatorios views | GET com params de filtro (data_inicio, data_fim, status, unidade) — input não confiável |
| view → service → DB | Params de filtro passam ao ORM via service layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | _parse_filtros (data_inicio/data_fim) | mitigate | `datetime.strptime(..., "%Y-%m-%d")` em try/except; formato inválido cai no default (mês corrente) — não chega input malformado ao ORM |
| T-05-05 | Elevation of Privilege | RelatorioRequiredMixin | mitigate | `raise PermissionDenied` (403) para qualquer role fora de comprador/diretor/admin/superuser; solicitante bloqueado |
| T-05-06 | Injection (Tampering) | unidade_id / status GET params | mitigate | Passados ao ORM via `filter(unidade_id=...)` / `filter(status=...)` — Django ORM parametriza; sem SQL string concatenation |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo instalado nesta fase (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `docker compose exec web python manage.py check`
- `docker compose exec web python manage.py test apps.relatorios.tests.test_views` — TestAcesso/TestGastosView/TestRequisicoesPainel GREEN
- Manual: login comprador → /relatorios/gastos/ exibe tabela + filtros; ajustar datas e clicar Filtrar atualiza; login solicitante → 403
- `docker compose exec web python manage.py test` — sem regressão nas fases anteriores
</verification>

<success_criteria>
- GastosView e RequisicoesPainelView respondem 200 para comprador/diretor/admin e 403 para solicitante (REL-02, REL-03)
- Filtros de período/status/unidade aplicados via GET (D-03, D-04, UNIT-04)
- URLs registradas e resolvendo (gastos, gastos-pdf, requisicoes, requisicoes-pdf)
- Templates renderizam com formatação BR e links de exportar PDF preservando os filtros
</success_criteria>

## Artifacts this phase produces (plano 05-02)

| Symbol | File | Type |
|--------|------|------|
| `RelatorioRequiredMixin` | apps/relatorios/views.py | mixin (inclui diretor) |
| `_parse_filtros(request)` | apps/relatorios/views.py | helper → (date, date, str/None) |
| `GastosView` | apps/relatorios/views.py | View (REL-02) |
| `RequisicoesPainelView` | apps/relatorios/views.py | View (REL-03) |
| `GastosPDFView` (stub) | apps/relatorios/views.py | View stub → 05-03 |
| `RequisicoesPDFView` (stub) | apps/relatorios/views.py | View stub → 05-03 |
| `relatorios` URLconf (app_name) | apps/relatorios/urls.py | URLconf — 4 rotas |
| `gastos.html`, `requisicoes.html` | apps/relatorios/templates/relatorios/ | templates |

<output>
Create `.planning/phases/05-reports-dashboard/05-02-SUMMARY.md` when done
</output>
