---
phase: 05-reports-dashboard
plan: 04
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - templates/base.html
  - README.md
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04, UNIT-04]
must_haves:
  truths:
    - "The 'Relatórios' nav link points to /relatorios/gastos/ (no longer href='#') and shows active state on /relatorios/* pages"
    - "Full test suite passes with no regression across all apps"
    - "README marks Phase 5 complete and documents the reports/dashboard feature"
  artifacts:
    - path: "templates/base.html"
      provides: "working Relatórios nav link"
      contains: "relatorios:gastos"
    - path: "README.md"
      provides: "Phase 5 documented and marked complete"
      contains: "Relatórios"
  key_links:
    - from: "templates/base.html"
      to: "relatorios:gastos"
      via: "nav-item href"
      pattern: "url 'relatorios:gastos'"
---

<objective>
Fechar a fatia de navegação e validar a fase: atualizar o link "Relatórios" no `base.html` (de `href="#"` para `{% url 'relatorios:gastos' %}` com estado ativo), rodar a suíte completa garantindo zero regressão, e cumprir as convenções obrigatórias de encerramento de fase do `CLAUDE.md` (atualizar README marcando a Fase 5 como concluída).

Purpose: O link de navegação é o último elo que torna os relatórios alcançáveis a partir de qualquer página (até agora só via URL direta). A validação da suíte completa confirma que as três fatias verticais (dashboard, relatórios HTML, PDFs) funcionam sem quebrar as fases 1-4. O encerramento de README é mandatório pelo `CLAUDE.md §Conventions`.
Output: nav funcional, suíte verde, README atualizado. (O `git push origin master` — segundo passo obrigatório do encerramento — é executado pelo orquestrador da fase, não por este plano.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-reports-dashboard/05-CONTEXT.md
@.planning/phases/05-reports-dashboard/05-01-PLAN.md
@.planning/phases/05-reports-dashboard/05-02-PLAN.md
@.planning/phases/05-reports-dashboard/05-03-PLAN.md
@CLAUDE.md
</context>

## Phase Goal

**As a** usuário de qualquer perfil, **I want to** ver KPIs reais no dashboard e abrir relatórios de gasto e de requisições filtráveis por período e unidade com exportação em PDF, **so that** eu tenha visibilidade total dos gastos sem depender de planilhas manuais.

<tasks>

<task type="auto">
  <name>Task 1: Atualizar link "Relatórios" na nav (base.html)</name>
  <files>templates/base.html</files>
  <read_first>
    - templates/base.html (bloco "Relatórios" linha 76-82 — atualmente href="#"; comparar com o padrão dos outros nav-items, ex. Fornecedores linha 63-67 e Estoque linha 70-74 que usam {% url %} + is-active)
    - apps/relatorios/urls.py (confirma name="gastos" e app_name="relatorios" → {% url 'relatorios:gastos' %})
    - .planning/phases/05-reports-dashboard/05-PATTERNS.md §base.html update Relatórios nav link
  </read_first>
  <action>
    Em `templates/base.html`, no bloco de navegação "Relatórios" (atualmente linhas 76-82):
      - Manter a condição de visibilidade, adicionando `or request.user.is_superuser` para consistência com os outros itens: `{% if request.user.role == 'comprador' or request.user.role == 'diretor' or request.user.role == 'admin' or request.user.is_superuser %}`.
      - Trocar `<a href="#" class="nav-item">` por:
        `<a href="{% url 'relatorios:gastos' %}" class="nav-item {% if 'relatorios' in request.path %}is-active{% endif %}">`.
      - Manter o texto "Relatórios" e a estrutura `{% endif %}`.
    Não alterar outros itens de navegação. Em particular, garantir que NENHUMA linha do item "Relatórios" mantenha `href="#"` após a edição.
  </action>
  <verify>
    <automated>docker compose exec web python -c "import io; html=io.open('templates/base.html', encoding='utf-8').read(); assert \"url 'relatorios:gastos'\" in html, 'tag {% url relatorios:gastos %} ausente'; rel_lines=[l for l in html.splitlines() if 'Relat' in l or 'relatorios' in l]; assert rel_lines, 'nenhuma linha de nav Relatorios encontrada'; assert all('href=\"#\"' not in l for l in rel_lines), 'href=# ainda presente no item Relatorios'"</automated>
  </verify>
  <acceptance_criteria>
    - `templates/base.html` contém `{% url 'relatorios:gastos' %}` no item de navegação "Relatórios"
    - O link de Relatórios NÃO contém mais `href="#"` (nenhuma linha do bloco "Relatórios"/`relatorios` mantém `href="#"`)
    - O item inclui `{% if 'relatorios' in request.path %}is-active{% endif %}` (estado ativo)
    - A condição de visibilidade inclui comprador, diretor, admin e is_superuser
  </acceptance_criteria>
  <done>Link "Relatórios" aponta para /relatorios/gastos/ com estado ativo; relatórios alcançáveis pela navegação.</done>
</task>

<task type="auto">
  <name>Task 2: Validação da suíte completa (sem regressão)</name>
  <files>(nenhum arquivo modificado — gate de validação; corrigir falhas se surgirem)</files>
  <read_first>
    - .planning/phases/05-reports-dashboard/05-VALIDATION.md (Full suite command; sampling rate; tabela T-05-01..12 — todos devem estar GREEN ao final da fase)
    - apps/relatorios/tests/test_services.py, apps/relatorios/tests/test_views.py, apps/core/tests/test_dashboard.py (todos os testes da fase — devem passar)
  </read_first>
  <action>
    Rodar a suíte completa do projeto. Todos os testes da Fase 5 (T-05-01..12) e das fases anteriores devem passar (GREEN), sem regressão.
    Se algum teste falhar:
      - Diagnosticar a causa raiz (NÃO mascarar com skip/xfail).
      - Corrigir o código de produção responsável (em apps/relatorios, apps/core ou config) — não relaxar a asserção do teste para forçar verde.
      - Reexecutar até a suíte ficar 100% verde.
    Confirmar especificamente que os contadores de Wave 0 viraram GREEN: TestPDF (T-05-10, T-05-11), TestAcesso (T-05-12), TestGastosView (T-05-06/07), TestRequisicoesPainel (T-05-08/09), TestDashboardKpis (T-05-03/04/05), e os testes de dashboard (T-05-01/02).
  </action>
  <verify>
    <automated>docker compose exec web python manage.py test</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose exec web python manage.py test` termina com "OK" (zero falhas, zero erros)
    - Nenhum teste da Fase 5 está em estado RED, skip ou xfail
    - Nenhuma regressão em apps.requisicoes, apps.aprovacoes, apps.fornecedores, apps.estoque, apps.cotacoes, apps.accounts, apps.core
    - Os 12 testes T-05-01..12 da VALIDATION.md estão GREEN
  </acceptance_criteria>
  <done>Suíte completa verde; todos os requisitos REL-01..04 e UNIT-04 verificados por teste automatizado, sem regressão.</done>
</task>

<task type="auto">
  <name>Task 3: Encerramento de fase — atualizar README (CLAUDE.md §Conventions)</name>
  <files>README.md</files>
  <read_first>
    - README.md (estado atual — localizar a seção de fases/roadmap e o status de cada fase; ver como as fases 1-4 foram marcadas como concluídas)
    - CLAUDE.md §Conventions §Encerramento de fase (passo 1 obrigatório: atualizar README marcando a fase como ✅ e documentando funcionalidades/setup)
    - .planning/ROADMAP.md §Phase 5 (success criteria e escopo entregue, para descrever a funcionalidade no README)
  </read_first>
  <action>
    Atualizar `README.md` conforme o `CLAUDE.md §Conventions §Encerramento de fase` (passo 1):
      - Marcar a Fase 5 (Reports & Dashboard) como concluída (✅), seguindo exatamente o mesmo formato usado para as fases 1-4 já concluídas no README.
      - Documentar a nova funcionalidade de relatórios/dashboard: dashboard com KPIs reais (requisições abertas, cotações em andamento, gasto do mês, fornecedores ativos), relatório de gasto por categoria filtrável por período e unidade, painel de status de requisições, e exportação PDF de ambos os relatórios via ReportLab.
      - Se o README documentar URLs ou perfis de acesso, registrar que `/relatorios/*` é acessível a comprador, diretor e admin (não a solicitante) e que o dashboard é acessível a todos os perfis autenticados.
      - NÃO executar `git push` neste plano — o push (passo 2 do encerramento) é responsabilidade do orquestrador da fase após o code review.
  </action>
  <verify>
    <automated>docker compose exec web python -c "import io; txt=io.open('README.md', encoding='utf-8').read(); assert 'Relatórios' in txt or 'Reports' in txt or 'Dashboard' in txt, 'README não documenta a fase 5'"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` marca a Fase 5 como concluída (✅) no mesmo padrão das fases anteriores
    - README documenta dashboard com KPIs, relatório de gastos, painel de requisições e exportação PDF
    - README menciona o controle de acesso dos relatórios (comprador/diretor/admin)
    - Nenhum `git push` é disparado por este plano (responsabilidade do orquestrador)
  </acceptance_criteria>
  <done>README reflete a Fase 5 concluída e documenta as funcionalidades de relatórios — passo 1 do encerramento obrigatório cumprido.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → nav link | Link de navegação renderizado condicionalmente por role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Elevation of Privilege | nav link "Relatórios" | mitigate | O link só aparece para comprador/diretor/admin/superuser; mesmo se forjado, `RelatorioRequiredMixin` (05-02) bloqueia no servidor (403) — defesa em profundidade |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Nenhum pacote novo nesta fase (RESEARCH §Package Legitimacy Audit) |
</threat_model>

<verification>
- `docker compose exec web python manage.py test` — suíte completa verde
- Manual: como comprador, o link "Relatórios" na sidebar leva a /relatorios/gastos/ e fica com estado ativo; como solicitante, o link não aparece
- README revisado mostrando a Fase 5 concluída
</verification>

<success_criteria>
- Link "Relatórios" aponta para /relatorios/gastos/ com estado ativo (relatórios alcançáveis pela navegação)
- Suíte completa 100% verde sem regressão (REL-01..04, UNIT-04 verificados)
- README atualizado marcando a Fase 5 como concluída (CLAUDE.md §Encerramento de fase passo 1)
</success_criteria>

## Artifacts this phase produces (plano 05-04)

| Symbol | File | Type |
|--------|------|------|
| Nav link "Relatórios" → relatorios:gastos | templates/base.html | template patch |
| Fase 5 documentada/concluída | README.md | docs |
| Suíte completa verde (gate) | — | validation |

<output>
Create `.planning/phases/05-reports-dashboard/05-04-SUMMARY.md` when done
</output>
