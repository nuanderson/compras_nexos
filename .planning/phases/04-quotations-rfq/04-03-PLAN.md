---
phase: 04-quotations-rfq
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - apps/cotacoes/forms.py
  - apps/cotacoes/views.py
  - apps/cotacoes/urls.py
  - apps/cotacoes/templates/cotacoes/rfq_detail.html
  - apps/cotacoes/templates/cotacoes/partials/cotacao_row.html
  - apps/cotacoes/templates/cotacoes/partials/modal_selecionar.html
  - apps/cotacoes/tests/test_views.py
autonomous: true
requirements: [COT-02, COT-03, COT-04]
must_haves:
  truths:
    - "Comprador adiciona cotações de fornecedores (preço, prazo, condições) e a página recarrega via HX-Redirect mostrando a nova linha"
    - "Comprador remove uma cotação antes da seleção e a tabela recarrega via HX-Redirect"
    - "A tabela comparativa destaca o menor preço (badge accent #e94560) e mostra o delta % de cada fornecedor vs. o menor"
    - "Comprador seleciona um vencedor via modal com justificativa obrigatória; a seleção é imutável"
    - "Após a seleção, add/remove/selecionar retornam 403 (RFQ encerrado)"
  artifacts:
    - path: "apps/cotacoes/views.py"
      provides: "AdicionarCotacaoView, RemoverCotacaoView, ModalSelecionarVencedorView, SelecionarVencedorView; DetalheRFQView injeta comparativo"
      contains: "class SelecionarVencedorView"
    - path: "apps/cotacoes/templates/cotacoes/partials/modal_selecionar.html"
      provides: "Modal HTMX de seleção de vencedor com justificativa obrigatória"
      contains: "justificativa"
    - path: "apps/cotacoes/forms.py"
      provides: "CotacaoFornecedorForm com fornecedor select + preco_unitario + prazo + condicoes + observacoes"
      contains: "class CotacaoFornecedorForm"
  key_links:
    - from: "apps/cotacoes/views.py"
      to: "apps.cotacoes.services.selecionar_vencedor"
      via: "delegação no service layer"
      pattern: "services\\.selecionar_vencedor"
    - from: "apps/cotacoes/views.py"
      to: "HttpResponseClientRedirect / HX-Redirect"
      via: "reload da página de detalhe após add/remove (D-10)"
      pattern: "HX-Redirect|HttpResponseClientRedirect"
    - from: "apps/cotacoes/templates/cotacoes/rfq_detail.html"
      to: "comparativo"
      via: "loop sobre dicts {cotacao, delta, is_menor}"
      pattern: "is_menor"
---

<objective>
Slice vertical final do ciclo RFQ: entrada de cotações de múltiplos fornecedores (COT-02), tabela comparativa com destaque do menor preço e delta % (COT-03), e seleção do vencedor com justificativa obrigatória imutável (COT-04). Toda a lógica de negócio já existe no service layer (plano 01); aqui ela é exposta via views finas + templates HTMX, seguindo os padrões já estabelecidos na Fase 2 (modal HTMX, retarget/reswap).

Este é o deliverable que completa o ciclo de ponta a ponta: ao fim deste plano, o Comprador vai de um RFQ vazio até um vencedor selecionado e auditável. A tabela comparativa é estática (D-10): após adicionar/remover cotação, a view responde com `HX-Redirect` para `/cotacoes/<pk>/`, recarregando a página inteira e mantendo todos os deltas consistentes (evita o Pitfall 2 — deltas desatualizados).

Purpose: COT-02/03/04 são o núcleo de valor da fase ("dar ao comprador controle total do ciclo de compra"). A imutabilidade do vencedor (D-07/D-08) e o destaque automático do menor preço (D-09) são o diferencial sobre o fluxo manual por planilha.

Output: 4 views (add/remove/modal/selecionar), CotacaoFornecedorForm, 2 partials, comparativo injetado no detalhe, testes COT-02/03/04 GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-quotations-rfq/04-CONTEXT.md
@.planning/phases/04-quotations-rfq/04-RESEARCH.md

# Padrões a reutilizar (LER antes de implementar)
@apps/aprovacoes/views.py
@apps/fornecedores/views.py
@apps/cotacoes/services.py
@apps/cotacoes/views.py
@apps/cotacoes/templates/cotacoes/rfq_detail.html
</context>

<artifacts_this_phase_produces>
Símbolos criados neste plano (consumidos pelo plano 04 apenas para validação de suite):

- **Rotas adicionadas em `apps/cotacoes/urls.py`:**
  - `path("<int:rfq_pk>/cotacoes/adicionar/", AdicionarCotacaoView.as_view(), name="adicionar-cotacao")`
  - `path("<int:rfq_pk>/cotacoes/<int:cotacao_pk>/remover/", RemoverCotacaoView.as_view(), name="remover-cotacao")`
  - `path("<int:rfq_pk>/selecionar-vencedor/<int:cotacao_pk>/modal/", ModalSelecionarVencedorView.as_view(), name="modal-selecionar")`
  - `path("<int:rfq_pk>/selecionar-vencedor/<int:cotacao_pk>/", SelecionarVencedorView.as_view(), name="selecionar-vencedor")`
- **Views (CompradorRequiredMixin, View)** em `apps/cotacoes/views.py`:
  - `AdicionarCotacaoView` — POST: guard `rfq.tem_vencedor`→403; valida CotacaoFornecedorForm; `services.adicionar_cotacao`; responde HX-Redirect para detalhe
  - `RemoverCotacaoView` — POST: guard→403; `services.remover_cotacao`; responde HX-Redirect para detalhe
  - `ModalSelecionarVencedorView` — GET: guard→409; renderiza modal_selecionar.html
  - `SelecionarVencedorView` — POST: `services.selecionar_vencedor` (justificativa obrigatória); ValueError→409; sucesso → HX-Redirect para detalhe
  - `DetalheRFQView` (MODIFICADA) — injeta `comparativo = services.calcular_comparativo(rfq)` e `form = CotacaoFornecedorForm()` no contexto
- **`apps.cotacoes.forms.CotacaoFornecedorForm`** (ModelForm) — fields: fornecedor (ModelChoiceField só ativos), preco_unitario, prazo_entrega, condicoes_pagamento, observacoes
- **Partials:** `cotacao_row.html` (linha da tabela com badge is_menor + delta + botões), `modal_selecionar.html` (form de justificativa)
- **rfq_detail.html** (MODIFICADA) — substitui o placeholder pela tabela comparativa + form inline de adição
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CotacaoFornecedorForm + views add/remove + HX-Redirect + DetalheRFQView com comparativo (COT-02, COT-03, D-08, D-09, D-10, Pitfall 2)</name>
  <files>apps/cotacoes/forms.py, apps/cotacoes/views.py, apps/cotacoes/urls.py, apps/cotacoes/templates/cotacoes/rfq_detail.html, apps/cotacoes/templates/cotacoes/partials/cotacao_row.html</files>
  <read_first>
    - apps/fornecedores/views.py — get_queryset_fornecedores(apenas_ativos=True) para o select de fornecedor; CompradorRequiredMixin
    - apps/aprovacoes/views.py — padrão de view fina que delega ao service e responde HTMX
    - apps/cotacoes/services.py — adicionar_cotacao, remover_cotacao, calcular_comparativo (assinaturas exatas)
    - apps/cotacoes/views.py — DetalheRFQView atual (a modificar — adicionar comparativo + form ao contexto)
    - .planning/phases/04-quotations-rfq/04-RESEARCH.md §Pattern 1/2 (add/remove) e §Pitfall 2 (HX-Redirect para deltas consistentes)
  </read_first>
  <action>
    **forms.py** — adicionar `class CotacaoFornecedorForm(forms.ModelForm)`: `Meta.model = CotacaoFornecedor`, `Meta.fields = ["fornecedor", "preco_unitario", "prazo_entrega", "condicoes_pagamento", "observacoes"]`, labels em pt-BR. Em `__init__`, restringir `self.fields["fornecedor"].queryset` a `Fornecedor.objects.filter(ativo=True).order_by("razao_social")` (reutilizar a intenção de get_queryset_fornecedores). Adicionar classes CSS (`form-select` no fornecedor, `form-input` nos demais). `preco_unitario` já herda MinValueValidator do modelo.

    **views.py** — importar `from django.http import HttpResponse`; usar `from django_htmx.http import HttpResponseClientRedirect` (já disponível — django-htmx instalado) para o HX-Redirect.
    - `AdicionarCotacaoView(CompradorRequiredMixin, View)`: POST(request, rfq_pk) → `rfq = get_object_or_404(RFQ, pk=rfq_pk)`; se `rfq.tem_vencedor` → `return HttpResponse("RFQ encerrado.", status=403)` (T-04-03). `form = CotacaoFornecedorForm(request.POST)`; se válido: `services.adicionar_cotacao(rfq, form.cleaned_data)` e `return HttpResponseClientRedirect(reverse("cotacoes:detalhe", args=[rfq.pk]))` (D-10 — reload mantém deltas consistentes, Pitfall 2). Se inválido: re-render `cotacoes/rfq_detail.html` com erros e `status=422` (com comparativo + form no contexto).
    - `RemoverCotacaoView(CompradorRequiredMixin, View)`: POST(request, rfq_pk, cotacao_pk) → `rfq = get_object_or_404(RFQ, pk=rfq_pk)`; se `rfq.tem_vencedor` → 403; `services.remover_cotacao(rfq, cotacao_pk)`; `return HttpResponseClientRedirect(reverse("cotacoes:detalhe", args=[rfq.pk]))`.
    - MODIFICAR `DetalheRFQView.get`: adicionar ao contexto `"comparativo": services.calcular_comparativo(rfq)` e `"form": CotacaoFornecedorForm()`.

    **urls.py** — adicionar as rotas `adicionar-cotacao` e `remover-cotacao` conforme "Artifacts this phase produces".

    **cotacao_row.html** (partial) — `<tr>` com: fornecedor.razao_social, `R$ {{ item.cotacao.preco_unitario }}`, prazo_entrega, condicoes_pagamento, `{{ item.delta }}%`. Se `item.is_menor`: aplicar badge accent (classe `.badge` com cor `#e94560` / variável `--color-accent`) na célula de preço e texto "Menor preço". Botões (somente se `not rfq.tem_vencedor`): "Remover" (`hx-post` para `cotacoes:remover-cotacao`, `hx-confirm`) e "Selecionar Vencedor" (`hx-get` para `cotacoes:modal-selecionar`, `hx-target="#modal-container"`, `hx-swap="innerHTML"`).

    **rfq_detail.html** — substituir o placeholder do plano 02 por: (a) form inline de adição (`<form hx-post="{% url 'cotacoes:adicionar-cotacao' rfq.pk %}" hx-target="body">` com `{% csrf_token %}` + campos do form, exibido só se `not rfq.tem_vencedor`); (b) `<table>` dentro de `.table-container` com `<tbody>` iterando `comparativo` e incluindo `cotacoes/partials/cotacao_row.html` para cada item; empty state "Adicione cotações de fornecedores para ver o comparativo." (D-10). Manter `<div id="modal-container"></div>`. Se `rfq.tem_vencedor`: exibir card destacando o vencedor (fornecedor + preço + justificativa_selecao).
  </action>
  <verify>
    <automated>cd /c/Users/admre/compras_nexos && python manage.py check && python -m pytest apps/cotacoes/tests/test_views.py::TestAdicionarCotacaoView apps/cotacoes/tests/test_views.py::TestRemoverCotacaoView -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `apps/cotacoes/forms.py` contém `class CotacaoFornecedorForm` e `ativo=True`.
    - `apps/cotacoes/views.py` contém `HttpResponseClientRedirect` e o guard `rfq.tem_vencedor` em AdicionarCotacaoView e RemoverCotacaoView.
    - `apps/cotacoes/views.py` DetalheRFQView contém `services.calcular_comparativo`.
    - `cotacao_row.html` contém `is_menor` e `item.delta`.
    - `python manage.py check` sai 0 e os testes Adicionar/Remover saem 0.
  </acceptance_criteria>
  <done>Comprador adiciona/remove cotações; tabela comparativa com menor preço e delta % renderiza; reload via HX-Redirect.</done>
</task>

<task type="auto">
  <name>Task 2: Modal de seleção + SelecionarVencedorView + imutabilidade + testes COT-04 e bloqueio pós-seleção (COT-04, D-07, D-08, T-04-05/06)</name>
  <files>apps/cotacoes/views.py, apps/cotacoes/urls.py, apps/cotacoes/templates/cotacoes/partials/modal_selecionar.html, apps/cotacoes/tests/test_views.py</files>
  <read_first>
    - apps/aprovacoes/views.py — ModalReprovarView (GET retorna modal) e ReprovarGestorView (POST delega ao service, captura ValueError→409); copiar esse padrão exato
    - apps/aprovacoes/templates/aprovacoes/partials/modal_reprovar.html — estrutura do modal (card, form, csrf, textarea obrigatório, botão cancelar que limpa #modal-container)
    - apps/cotacoes/services.py — selecionar_vencedor (assinatura: rfq_pk, cotacao_pk, justificativa, comprador)
    - apps/cotacoes/tests/test_views.py — remover skips de TestModalSelecionarVencedor e TestBloqueioPosSeletcao
    - .planning/phases/04-quotations-rfq/04-RESEARCH.md §Pattern 3 (modal de seleção)
  </read_first>
  <action>
    **views.py** —
    - `ModalSelecionarVencedorView(CompradorRequiredMixin, View)`: GET(request, rfq_pk, cotacao_pk) → `rfq = get_object_or_404(RFQ, pk=rfq_pk)`; se `rfq.tem_vencedor` → `return HttpResponse("Vencedor já definido.", status=409)`; `cotacao = get_object_or_404(CotacaoFornecedor, pk=cotacao_pk, rfq=rfq)`; render `cotacoes/partials/modal_selecionar.html` com `{"rfq": rfq, "cotacao": cotacao}`.
    - `SelecionarVencedorView(CompradorRequiredMixin, View)`: POST(request, rfq_pk, cotacao_pk) → ler `justificativa = request.POST.get("justificativa", "")`; `try: services.selecionar_vencedor(rfq_pk, cotacao_pk, justificativa, request.user)` `except ValueError as e: return HttpResponse(str(e), status=409)` (T-04-06 — vencedor imutável / justificativa vazia). Em sucesso: `return HttpResponseClientRedirect(reverse("cotacoes:detalhe", args=[rfq_pk]))`.
    Importar `from .models import RFQ, CotacaoFornecedor`.

    **urls.py** — adicionar rotas `modal-selecionar` e `selecionar-vencedor` conforme "Artifacts this phase produces".

    **modal_selecionar.html** (partial) — seguir modal_reprovar.html: card com borda accent; `<p>Selecionar vencedor: {{ cotacao.fornecedor.razao_social }} — R$ {{ cotacao.preco_unitario }}</p>`; `<form hx-post="{% url 'cotacoes:selecionar-vencedor' rfq.pk cotacao.pk %}" hx-target="body">` com `{% csrf_token %}`; `<textarea name="justificativa" rows="3" required class="form-input">`; botões "Confirmar Seleção" (.btn .btn-primary) e "Cancelar" (limpa `#modal-container` via onclick). O `required` no textarea é a 1ª barreira; o service valida defense-in-depth.

    **test_views.py** — remover skips e implementar:
    - `TestModalSelecionarVencedor`: GET como comprador em `cotacoes:modal-selecionar` (com rfq + cotacao_fornecedor) → 200 e o modal contém "Justificativa". POST em `cotacoes:selecionar-vencedor` com justificativa preenchida → resposta com header `HX-Redirect`; `rfq.refresh_from_db()` → `rfq.tem_vencedor is True`. POST com justificativa vazia → 409 e rfq sem vencedor.
    - `TestBloqueioPosSeletcao`: com um RFQ que já tem vencedor (selecionar antes no teste), POST em `cotacoes:adicionar-cotacao` → 403; POST em `cotacoes:remover-cotacao` → 403; GET em `cotacoes:modal-selecionar` → 409 (T-04-03).
    - `TestAdicionarCotacaoView` / `TestRemoverCotacaoView`: confirmar header `HX-Redirect` na resposta de sucesso (caso ainda não coberto na Task 1).
  </action>
  <verify>
    <automated>cd /c/Users/admre/compras_nexos && python -m pytest apps/cotacoes/tests/test_views.py::TestModalSelecionarVencedor apps/cotacoes/tests/test_views.py::TestBloqueioPosSeletcao -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `apps/cotacoes/views.py` contém `class SelecionarVencedorView` e `services.selecionar_vencedor` e `except ValueError`.
    - `apps/cotacoes/urls.py` contém `name="selecionar-vencedor"` e `name="modal-selecionar"`.
    - `modal_selecionar.html` contém `name="justificativa"` e `required`.
    - `test_views.py` não contém mais `skip` nas classes deste plano.
    - `python -m pytest apps/cotacoes/tests/test_views.py::TestModalSelecionarVencedor apps/cotacoes/tests/test_views.py::TestBloqueioPosSeletcao -x -q` sai 0.
  </acceptance_criteria>
  <done>Seleção de vencedor via modal funciona, é imutável; add/remove/selecionar bloqueados após seleção (403/409).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→view | Comprador submete preço/justificativa via POST HTMX — validado por form + service |
| view→service | Seleção de vencedor cruza para o lock select_for_update no service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | AdicionarCotacaoView, RemoverCotacaoView, ModalSelecionarVencedorView | mitigate | Guard `if rfq.tem_vencedor` → 403/409 na view + ValueError no service (defense in depth) |
| T-04-05 | Tampering | SelecionarVencedorView | mitigate | Delega a services.selecionar_vencedor que usa select_for_update + transaction.atomic |
| T-04-06 | Tampering | SelecionarVencedorView | mitigate | `except ValueError` → 409 quando rfq já tem vencedor ou justificativa vazia |
| T-04-CSRF | Spoofing | forms HTMX (add/selecionar) | mitigate | csrf_token nos forms; header X-CSRFToken global já configurado em base.html |
| T-04-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- `python -m pytest apps/cotacoes/tests/test_views.py -x -q` — COT-02/03/04 GREEN
- `python -m pytest apps/cotacoes/ -x -q` — suite do app inteira verde (sem skips restantes)
- Manual: adicionar 2 cotações, confirmar destaque do menor preço (#e94560) e delta %; selecionar vencedor; confirmar imutabilidade
</verification>

<success_criteria>
- Adicionar/remover cotação com reload via HX-Redirect (deltas consistentes)
- Tabela comparativa destaca menor preço e mostra delta %
- Seleção de vencedor com justificativa obrigatória, imutável
- Bloqueio total pós-seleção (403/409)
</success_criteria>

<output>
Create `.planning/phases/04-quotations-rfq/04-03-SUMMARY.md` when done
</output>
