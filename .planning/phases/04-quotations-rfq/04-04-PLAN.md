---
phase: 04-quotations-rfq
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - templates/base.html
  - README.md
autonomous: true
requirements: [COT-01, COT-02, COT-03, COT-04]
must_haves:
  truths:
    - "O link 'Cotações' no nav lateral aponta para /cotacoes/ (não mais href=#) e fica ativo na seção"
    - "O link é visível apenas para Comprador e Admin"
    - "A suite completa do projeto está verde (pytest --tb=short -q)"
    - "README.md marca a Fase 4 como concluída"
  artifacts:
    - path: "templates/base.html"
      provides: "Link de navegação Cotações funcional"
      contains: "url 'cotacoes:lista'"
  key_links:
    - from: "templates/base.html"
      to: "cotacoes:lista"
      via: "url tag no nav-item Cotações"
      pattern: "cotacoes:lista"
---

<objective>
Integração de navegação e validação da fase: ativar o link "Cotações" no nav lateral (`base.html` já tem o item com o guard de role correto, apontando para `href="#"` — basta trocar pela URL real e adicionar o estado ativo), rodar a suite completa do projeto para garantir que nenhuma regressão foi introduzida, e marcar a Fase 4 como concluída no README.md (convenção obrigatória do CLAUDE.md).

Este é o deliverable de fechamento (MVP vertical): após ele, o Comprador descobre a funcionalidade pela navegação e o sistema inteiro continua verde.

Purpose: Sem o link de navegação, o ciclo RFQ existe mas é inacessível pela UI. A validação de suite completa é o phase gate antes de `/gsd-verify-work`. A atualização do README é exigência de encerramento de fase do projeto.

Output: nav funcional, suite verde, README atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-quotations-rfq/04-RESEARCH.md

# Arquivos a modificar (LER antes)
@templates/base.html
</context>

<artifacts_this_phase_produces>
- **Nav ativo:** o item "Cotações" em `templates/base.html` passa de `href="#"` para `href="{% url 'cotacoes:lista' %}"` com classe ativa `{% if 'cotacoes' in request.path %}is-active{% endif %}` (mesmo padrão dos itens Fornecedores/Estoque).
- **README.md:** Fase 4 marcada como ✅ concluída.
- Nenhum símbolo de código novo — apenas wiring de navegação e documentação.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Ativar link "Cotações" no nav lateral (COT-01, D-04)</name>
  <files>templates/base.html</files>
  <read_first>
    - templates/base.html — o bloco do item "Cotações" (guard `request.user.role == 'comprador' or request.user.role == 'admin'`, atualmente `href="#"`); e os itens Fornecedores/Estoque como referência do padrão de `is-active`
    - apps/cotacoes/urls.py — confirmar que `app_name = "cotacoes"` e existe `name="lista"`
  </read_first>
  <action>
    No `templates/base.html`, localizar o item de navegação "Cotações" (dentro do `{% if request.user.role == 'comprador' or request.user.role == 'admin' %}`). Trocar `href="#"` por `href="{% url 'cotacoes:lista' %}"` e adicionar o estado ativo na classe: `class="nav-item {% if 'cotacoes' in request.path %}is-active{% endif %}"`, replicando exatamente o padrão usado no item "Fornecedores" (`{% if 'fornecedores' in request.path %}is-active{% endif %}`). NÃO alterar o guard de role (já está correto: comprador + admin, D-04). NÃO tocar nos outros itens do nav.
  </action>
  <verify>
    <automated>cd /c/Users/admre/compras_nexos && python manage.py check && python -m pytest apps/cotacoes/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `templates/base.html` contém `{% url 'cotacoes:lista' %}` e NÃO contém mais o `href="#"` no item Cotações.
    - `templates/base.html` contém `{% if 'cotacoes' in request.path %}is-active{% endif %}`.
    - `python manage.py check` sai 0.
  </acceptance_criteria>
  <done>Link "Cotações" aponta para /cotacoes/, fica ativo na seção, visível só para Comprador/Admin.</done>
</task>

<task type="auto">
  <name>Task 2: Validação da suite completa + atualizar README (phase gate, convenção CLAUDE.md)</name>
  <files>README.md</files>
  <read_first>
    - README.md — seção de fases/status (localizar a marcação das fases 1-3 concluídas para seguir o mesmo formato ao marcar a Fase 4)
    - .planning/ROADMAP.md — Phase 4 success criteria (para descrever a entrega no README)
  </read_first>
  <action>
    Rodar a suite completa do projeto: `python -m pytest --tb=short -q`. Todos os testes devem passar (phase gate). Se houver qualquer falha em apps anteriores causada por mudança de URL/template, investigar e corrigir antes de prosseguir (não suprimir testes).

    Atualizar `README.md`: marcar a Fase 4 (Quotations / RFQ) como ✅ concluída no mesmo formato das fases anteriores, e atualizar qualquer descrição de funcionalidades para mencionar o ciclo de cotação (criar RFQ, registrar cotações, comparativo de preços, seleção de vencedor). Seguir a convenção de "Encerramento de fase" do CLAUDE.md (atualizar README é passo obrigatório). NÃO fazer `git push` neste plano — o push é responsabilidade do fechamento da fase pelo orquestrador/usuário.
  </action>
  <verify>
    <automated>cd /c/Users/admre/compras_nexos && python -m pytest --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest --tb=short -q` termina sem falhas (exit 0) — suite completa verde.
    - `README.md` contém a Fase 4 marcada como concluída (✅) com menção a cotações/RFQ.
    - Nenhum teste foi marcado como skip/xfail para passar o gate.
  </acceptance_criteria>
  <done>Suite completa verde; README marca Fase 4 concluída.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template→browser | Link de navegação renderizado condicionalmente por role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | nav item Cotações | mitigate | Guard de role no template (`comprador`/`admin`) — link oculto para outros papéis; reforçado pelo CompradorRequiredMixin nas views (plano 02/03) |
| T-04-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo instalado nesta fase |
</threat_model>

<verification>
- `python -m pytest --tb=short -q` — suite completa verde (phase gate)
- `python manage.py check` — sem erros
- Manual: login como Comprador → item "Cotações" visível e clicável; login como Solicitante → item ausente
</verification>

<success_criteria>
- Link "Cotações" funcional e ativo na seção, visível só para Comprador/Admin
- Suite completa do projeto verde
- README.md marca Fase 4 concluída
</success_criteria>

<output>
Create `.planning/phases/04-quotations-rfq/04-04-SUMMARY.md` when done
</output>
