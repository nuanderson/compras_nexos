---
phase: 04-quotations-rfq
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/cotacoes/__init__.py
  - apps/cotacoes/apps.py
  - apps/cotacoes/admin.py
  - apps/cotacoes/models.py
  - apps/cotacoes/services.py
  - apps/cotacoes/migrations/__init__.py
  - apps/cotacoes/migrations/0001_initial.py
  - apps/cotacoes/tests/__init__.py
  - apps/cotacoes/tests/conftest.py
  - apps/cotacoes/tests/test_models.py
  - apps/cotacoes/tests/test_services.py
  - apps/cotacoes/tests/test_views.py
  - config/settings/base.py
autonomous: true
requirements: [COT-01, COT-02, COT-03, COT-04]
must_haves:
  truths:
    - "App apps.cotacoes está em INSTALLED_APPS e migra sem erro"
    - "Modelo RFQ existe com OneToOneField para Requisicao (unicidade no DB)"
    - "Modelo CotacaoFornecedor existe com preco_unitario DecimalField e MinValueValidator(0.01)"
    - "services.calcular_comparativo retorna menor preço com is_menor=True e delta percentual"
    - "services.selecionar_vencedor usa select_for_update + transaction.atomic e é imutável"
    - "Suite de testes Wave 0 existe e roda (RED esperado para comportamento ainda não conectado às views)"
  artifacts:
    - path: "apps/cotacoes/models.py"
      provides: "Modelos RFQ e CotacaoFornecedor"
      contains: "class RFQ"
    - path: "apps/cotacoes/services.py"
      provides: "criar_rfq, adicionar_cotacao, remover_cotacao, calcular_comparativo, selecionar_vencedor"
      contains: "def selecionar_vencedor"
    - path: "apps/cotacoes/tests/conftest.py"
      provides: "Fixtures rfq, cotacao_fornecedor, requisicao_aprovada, comprador_user, fornecedor"
      contains: "def requisicao_aprovada"
  key_links:
    - from: "apps/cotacoes/models.py"
      to: "apps.requisicoes.models.Requisicao"
      via: "OneToOneField"
      pattern: "OneToOneField"
    - from: "apps/cotacoes/services.py"
      to: "RFQ.objects.select_for_update"
      via: "transaction.atomic + select_for_update"
      pattern: "select_for_update"
---

<objective>
Estabelecer a fundação de dados e a camada de serviço do app `apps/cotacoes/`: modelos `RFQ` e `CotacaoFornecedor`, migração inicial, service layer completo (criar RFQ, adicionar/remover cotação, calcular comparativo com delta %, selecionar vencedor imutável) e o scaffold de testes Wave 0 (conftest + test stubs cobrindo COT-01..04).

Este é o primeiro deliverable da Fase 4 (MVP vertical). Como nenhuma view ainda existe, as tarefas seguem a ordem: (1) testes Wave 0 que falham (RED), (2) modelos + migração, (3) service layer + contratos. Os testes de service ficam GREEN ao fim deste plano; os testes de view permanecem RED até os planos 02 e 03 conectarem a UI.

Purpose: Sem modelos e service layer, nenhuma view da Fase 4 pode existir. A unicidade (OneToOneField), a imutabilidade do vencedor (select_for_update) e o guard de divisão por zero no comparativo são responsabilidade exclusiva desta camada.

Output: App `apps/cotacoes/` registrado, migração aplicável, service layer testado, scaffold de testes completo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quotations-rfq/04-CONTEXT.md
@.planning/phases/04-quotations-rfq/04-RESEARCH.md
@.planning/phases/04-quotations-rfq/04-VALIDATION.md

# Padrões a reutilizar (LER antes de implementar)
@apps/core/models.py
@apps/aprovacoes/services.py
@apps/requisicoes/models.py
@apps/fornecedores/tests/conftest.py
</context>

<artifacts_this_phase_produces>
Símbolos criados neste plano (consumidos pelos planos 02, 03, 04):

- **App label:** `cotacoes` (registrado em `config/settings/base.py` INSTALLED_APPS como `"apps.cotacoes"`)
- **Modelo `apps.cotacoes.models.RFQ`** — campos: `requisicao` (OneToOneField→requisicoes.Requisicao, on_delete=PROTECT, related_name="rfq"), `criado_por` (FK→accounts.User, null=True, on_delete=SET_NULL, related_name="rfqs_criados"), `vencedor` (FK→CotacaoFornecedor, null=True, blank=True, on_delete=SET_NULL, related_name="rfqs_vencidos"), `justificativa_selecao` (TextField, blank=True, default=""). Herda TimestampedModel (`criado_em`, `atualizado_em`).
  - propriedade `tem_vencedor` → bool (`vencedor_id is not None`)
  - propriedade `status_display` → "Encerrado" | "Em andamento"
- **Modelo `apps.cotacoes.models.CotacaoFornecedor`** — campos: `rfq` (FK→RFQ, on_delete=CASCADE, related_name="cotacoes"), `fornecedor` (FK→fornecedores.Fornecedor, on_delete=PROTECT, related_name="cotacoes"), `preco_unitario` (DecimalField max_digits=12 decimal_places=2, validators=[MinValueValidator(Decimal("0.01"))]), `prazo_entrega` (CharField max_length=100, blank=True, default=""), `condicoes_pagamento` (CharField max_length=200, blank=True, default=""), `observacoes` (TextField, blank=True, default=""). Herda TimestampedModel.
- **Funções `apps.cotacoes.services`:**
  - `criar_rfq(requisicao_pk: int, comprador) -> RFQ`
  - `adicionar_cotacao(rfq, dados: dict) -> CotacaoFornecedor`
  - `remover_cotacao(rfq, cotacao_pk: int) -> None`
  - `calcular_comparativo(rfq) -> list[dict]` (cada dict: `{"cotacao", "delta", "is_menor"}`)
  - `selecionar_vencedor(rfq_pk: int, cotacao_pk: int, justificativa: str, comprador) -> RFQ`
- **Fixtures de teste** (`apps/cotacoes/tests/conftest.py`): `test_unit`, `comprador_user`, `solicitante_user`, `admin_user`, `categoria`, `fornecedor`, `requisicao_aprovada`, `rfq`, `cotacao_fornecedor`
- **Migração:** `apps/cotacoes/migrations/0001_initial.py`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold de testes Wave 0 — conftest + stubs RED (COT-01..04)</name>
  <files>apps/cotacoes/__init__.py, apps/cotacoes/tests/__init__.py, apps/cotacoes/tests/conftest.py, apps/cotacoes/tests/test_models.py, apps/cotacoes/tests/test_services.py, apps/cotacoes/tests/test_views.py</files>
  <read_first>
    - apps/fornecedores/tests/conftest.py — copiar fixtures test_unit, comprador_user, solicitante_user, admin_user, categoria, fornecedor exatamente (mesma assinatura)
    - .planning/phases/04-quotations-rfq/04-VALIDATION.md — Per-Task Verification Map: nomes EXATOS das classes de teste (TestCriarRFQ, TestCriarRFQDuplicado, TestCalcularComparativo, TestDeltaPercentual, TestDeltaZero, TestSelecionarVencedor, TestVencedorImutavel, TestJustificativaObrigatoria, TestNovaRFQView, TestAdicionarCotacaoView, TestRemoverCotacaoView, TestBloqueioPosSeletcao, TestModalSelecionarVencedor)
    - apps/requisicoes/models.py — Requisicao.Status.APROVADO, campos descricao/categoria/valor_estimado/justificativa/unidade
  </read_first>
  <action>
    Criar o pacote `apps/cotacoes/` (arquivo `__init__.py` vazio) e o pacote de testes `apps/cotacoes/tests/__init__.py` vazio.

    Em `apps/cotacoes/tests/conftest.py`: replicar as fixtures de `apps/fornecedores/tests/conftest.py` (test_unit, comprador_user, solicitante_user, admin_user, categoria, fornecedor) e ADICIONAR três novas fixtures:
    - `requisicao_aprovada(db, test_unit, categoria, comprador_user)` → cria `Requisicao` com status=`Requisicao.Status.APROVADO`, descricao="Compra de notebooks", valor_estimado=Decimal("5000.00"), justificativa="teste", categoria=categoria, unidade=test_unit, criado_por=comprador_user.
    - `rfq(db, requisicao_aprovada, comprador_user)` → cria `RFQ` via `RFQ.objects.create(requisicao=requisicao_aprovada, criado_por=comprador_user)`.
    - `cotacao_fornecedor(db, rfq, fornecedor)` → cria `CotacaoFornecedor.objects.create(rfq=rfq, fornecedor=fornecedor, preco_unitario=Decimal("100.00"), prazo_entrega="30 dias", condicoes_pagamento="à vista")`.

    Em `apps/cotacoes/tests/test_services.py`: escrever os testes REAIS (não placeholders) para as classes de service:
    - `TestCriarRFQ` — criar_rfq vincula a requisição aprovada e define criado_por.
    - `TestCriarRFQDuplicado` — segundo criar_rfq para a mesma requisição levanta IntegrityError (importar `from django.db import IntegrityError`; usar `pytest.raises(IntegrityError)` dentro de `transaction.atomic()`).
    - `TestCalcularComparativo` — com 2 cotações (preços 100 e 150), o item de preço 100 tem `is_menor=True`.
    - `TestDeltaPercentual` — cotação de 150 vs menor 100 → delta == Decimal("50.0") (quantize 0.1).
    - `TestDeltaZero` — guard: se a única cotação tem preco_unitario via construção direta no banco com 0 (criar com `CotacaoFornecedor.objects.create(..., preco_unitario=Decimal("0"))` para burlar o validator de form), `calcular_comparativo` retorna delta Decimal("0") sem ZeroDivisionError.
    - `TestSelecionarVencedor` — selecionar_vencedor define rfq.vencedor e justificativa_selecao; `rfq.tem_vencedor is True`.
    - `TestVencedorImutavel` — segundo selecionar_vencedor levanta ValueError ("Vencedor já foi definido").
    - `TestJustificativaObrigatoria` — selecionar_vencedor com justificativa="   " levanta ValueError.

    Em `apps/cotacoes/tests/test_views.py`: escrever stubs com `@pytest.mark.skip(reason="View conectada no plano 02/03")` para as classes de view (TestNovaRFQView com test_acesso_negado_solicitante e test_select_filtra_aprovadas_sem_rfq, TestAdicionarCotacaoView, TestRemoverCotacaoView, TestBloqueioPosSeletcao, TestModalSelecionarVencedor). O skip evita falsos RED enquanto as views não existem; os planos 02/03 removem os skips.

    Em `apps/cotacoes/tests/test_models.py`: testes de modelo — RFQ.__str__, RFQ.tem_vencedor (False sem vencedor), RFQ.status_display ("Em andamento"), CotacaoFornecedor herda criado_em.

    NÃO usar fenced code dentro deste arquivo de plano; o executor escreve o código real nos arquivos de teste.
  </action>
  <verify>
    <automated>cd /c/Users/admre/compras_nexos && python -m pytest apps/cotacoes/tests/ --co -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest apps/cotacoes/tests/ --co -q` coleta os testes sem ImportError de conftest (modelos ainda podem não existir → coleta pode falhar import; nesse caso a Task 2 resolve. Mínimo aceitável aqui: arquivos criados e fixtures escritas).
    - `apps/cotacoes/tests/conftest.py` contém `def requisicao_aprovada` e `def cotacao_fornecedor`.
    - `apps/cotacoes/tests/test_services.py` contém as classes `TestCriarRFQDuplicado`, `TestDeltaZero`, `TestVencedorImutavel`.
  </acceptance_criteria>
  <done>Scaffold de testes existe com fixtures e testes de service reais; testes de view marcados como skip.</done>
</task>

<task type="auto">
  <name>Task 2: Modelos RFQ + CotacaoFornecedor, admin, migração, registro em INSTALLED_APPS (COT-01, COT-02, D-01..D-07)</name>
  <files>apps/cotacoes/apps.py, apps/cotacoes/models.py, apps/cotacoes/admin.py, apps/cotacoes/migrations/__init__.py, apps/cotacoes/migrations/0001_initial.py, config/settings/base.py</files>
  <read_first>
    - apps/core/models.py — TimestampedModel (herdar, dá criado_em/atualizado_em)
    - apps/requisicoes/models.py — alvo do OneToOneField (Requisicao)
    - apps/fornecedores/models.py — alvo do FK fornecedor (Fornecedor)
    - .planning/phases/04-quotations-rfq/04-RESEARCH.md §Code Examples → Model RFQ e CotacaoFornecedor (assinatura exata dos campos)
  </read_first>
  <action>
    Criar `apps/cotacoes/apps.py` com `class CotacoesConfig(AppConfig)`: `default_auto_field = "django.db.models.BigAutoField"`, `name = "apps.cotacoes"`.

    Criar `apps/cotacoes/models.py` com os modelos `RFQ` e `CotacaoFornecedor` exatamente conforme a seção "Artifacts this phase produces" deste plano. Importante:
    - `RFQ.requisicao = OneToOneField("requisicoes.Requisicao", on_delete=PROTECT, related_name="rfq")` (D-06 — unicidade no DB).
    - `RFQ.vencedor = FK("CotacaoFornecedor", null=True, blank=True, on_delete=SET_NULL, related_name="rfqs_vencidos")` (D-07).
    - `RFQ.justificativa_selecao = TextField(blank=True, default="")` (D-07).
    - propriedade `tem_vencedor` retorna `self.vencedor_id is not None`; propriedade `status_display` retorna "Encerrado"/"Em andamento".
    - `CotacaoFornecedor.preco_unitario = DecimalField(max_digits=12, decimal_places=2, validators=[MinValueValidator(Decimal("0.01"))])` (D-03 + guard divisão por zero T-04-04). Importar `from django.core.validators import MinValueValidator` e `from decimal import Decimal`.
    - `condicoes_pagamento` CharField max_length=200 blank default="" (D-01); `prazo_entrega` CharField max_length=100 blank default="" (D-02); `observacoes` TextField blank default="" (D-03).
    - Meta.ordering: RFQ `["-criado_em"]`, CotacaoFornecedor `["preco_unitario"]`.
    - NÃO adicionar `unique_together = [("rfq","fornecedor")]` — múltiplas cotações do mesmo fornecedor são permitidas (RESEARCH Open Question 1, A3).

    Criar `apps/cotacoes/admin.py` registrando `RFQ` e `CotacaoFornecedor` com `admin.site.register` (list_display básico: RFQ → pk, requisicao, status_display; CotacaoFornecedor → pk, rfq, fornecedor, preco_unitario).

    Adicionar `"apps.cotacoes"` à lista `INSTALLED_APPS` em `config/settings/base.py` (após `"apps.estoque"`).

    Criar `apps/cotacoes/migrations/__init__.py` vazio e gerar a migração com `python manage.py makemigrations cotacoes` (o executor roda o comando; não escrever 0001_initial.py à mão).
  </action>
  <verify>
    <automated>cd /c/Users/admre/compras_nexos && python manage.py makemigrations cotacoes --check --dry-run && python -m pytest apps/cotacoes/tests/test_models.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `apps/cotacoes/models.py` contém `class RFQ(` e `class CotacaoFornecedor(` e a string `OneToOneField`.
    - `apps/cotacoes/models.py` contém `MinValueValidator(Decimal("0.01"))`.
    - `config/settings/base.py` contém `"apps.cotacoes"`.
    - `python manage.py makemigrations cotacoes --check --dry-run` reporta "No changes detected" (migração já criada e aplicada ao state).
    - `python -m pytest apps/cotacoes/tests/test_models.py -x -q` sai 0.
  </acceptance_criteria>
  <done>Modelos migram sem erro, app registrado, testes de modelo verdes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Service layer — criar_rfq, adicionar/remover_cotacao, calcular_comparativo, selecionar_vencedor (COT-01..04, D-06..D-08, T-04-02/04/05/06)</name>
  <files>apps/cotacoes/services.py</files>
  <behavior>
    - criar_rfq(requisicao_pk, comprador) cria RFQ com criado_por=comprador; segundo RFQ para a mesma requisição levanta IntegrityError (OneToOneField, T-04-02).
    - calcular_comparativo(rfq) ordena por preco_unitario ASC; menor preço tem is_menor=True; delta = ((preco-menor)/menor*100).quantize(0.1); se menor<=0 retorna delta Decimal("0") sem ZeroDivisionError (T-04-04).
    - adicionar_cotacao(rfq, dados) cria CotacaoFornecedor; bloqueia (ValueError) se rfq.tem_vencedor (T-04-03 defense in depth).
    - remover_cotacao(rfq, cotacao_pk) deleta a cotação; bloqueia (ValueError) se rfq.tem_vencedor.
    - selecionar_vencedor(rfq_pk, cotacao_pk, justificativa, comprador): justificativa vazia/whitespace → ValueError; usa transaction.atomic + RFQ.objects.select_for_update; se rfq.tem_vencedor → ValueError (T-04-06); salva vencedor + justificativa_selecao (imutável após salvo, COT-04).
  </behavior>
  <read_first>
    - apps/aprovacoes/services.py — padrão EXATO de transaction.atomic + select_for_update + validação ANTES da transação (reprovar_requisicao); copiar essa estrutura
    - apps/cotacoes/tests/test_services.py — os testes escritos na Task 1 são o contrato; implementar para passá-los
    - .planning/phases/04-quotations-rfq/04-RESEARCH.md §Pattern 4 (selecionar_vencedor) e §Code Examples (calcular_comparativo)
  </read_first>
  <action>
    Implementar `apps/cotacoes/services.py` com as cinco funções listadas em "Artifacts this phase produces".

    - `criar_rfq(requisicao_pk, comprador)`: `RFQ.objects.create(requisicao_id=requisicao_pk, criado_por=comprador)` — deixa o IntegrityError propagar (a view do plano 02 captura). Retorna o RFQ.
    - `calcular_comparativo(rfq)`: `list(rfq.cotacoes.select_related("fornecedor").order_by("preco_unitario"))`; se vazio retorna []; `menor = cotacoes[0].preco_unitario`; para cada c: se `menor and menor > 0` → `delta = ((c.preco_unitario - menor) / menor * Decimal("100")).quantize(Decimal("0.1"))` senão `delta = Decimal("0")`; append `{"cotacao": c, "delta": delta, "is_menor": c.preco_unitario == menor}`.
    - `adicionar_cotacao(rfq, dados)`: se `rfq.tem_vencedor` → `raise ValueError("RFQ encerrado — não aceita novas cotações.")`; senão `CotacaoFornecedor.objects.create(rfq=rfq, **dados)` e retorna.
    - `remover_cotacao(rfq, cotacao_pk)`: se `rfq.tem_vencedor` → `raise ValueError("RFQ encerrado — cotações imutáveis.")`; senão buscar `CotacaoFornecedor.objects.get(pk=cotacao_pk, rfq=rfq)` e `.delete()`.
    - `selecionar_vencedor(rfq_pk, cotacao_pk, justificativa, comprador)`: validar `if not justificativa or not justificativa.strip(): raise ValueError("Justificativa é obrigatória para selecionar o vencedor.")` ANTES de abrir a transação (defense in depth, padrão reprovar_requisicao). Dentro de `with transaction.atomic():` → `rfq = RFQ.objects.select_for_update().get(pk=rfq_pk)`; se `rfq.tem_vencedor: raise ValueError("Vencedor já foi definido para este RFQ.")`; `cotacao = CotacaoFornecedor.objects.get(pk=cotacao_pk, rfq=rfq)`; `rfq.vencedor = cotacao`; `rfq.justificativa_selecao = justificativa.strip()`; `rfq.save(update_fields=["vencedor", "justificativa_selecao", "atualizado_em"])`. Retornar rfq.

    Importar `from decimal import Decimal`, `from django.db import transaction`, `from .models import RFQ, CotacaoFornecedor`.

    Remover quaisquer `@pytest.mark.skip` que existam nos testes de service da Task 1 (eles devem rodar GREEN agora).
  </action>
  <verify>
    <automated>cd /c/Users/admre/compras_nexos && python -m pytest apps/cotacoes/tests/test_services.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `apps/cotacoes/services.py` contém `def selecionar_vencedor` e a string `select_for_update`.
    - `apps/cotacoes/services.py` contém `def calcular_comparativo` e o guard `menor > 0`.
    - `python -m pytest apps/cotacoes/tests/test_services.py -x -q` sai 0 (todos os testes de service GREEN).
    - `python -m pytest apps/cotacoes/ -x -q` não tem falhas (testes de view ainda em skip).
  </acceptance_criteria>
  <done>Service layer completo e testado; comportamento de imutabilidade e guard de divisão por zero verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→view (futuro) | Input do Comprador (preço, justificativa) — validado no plano 02/03; nesta camada o service valida defense-in-depth |
| service→DB | Transições de estado (seleção de vencedor) cruzam aqui — race condition possível |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Tampering | RFQ.requisicao OneToOneField | mitigate | OneToOneField força IntegrityError no DB ao tentar segundo RFQ (a view do plano 02 captura) |
| T-04-04 | Tampering | calcular_comparativo | mitigate | Guard `if menor > 0` retorna delta Decimal("0") — evita ZeroDivisionError; MinValueValidator(0.01) no model |
| T-04-05 | Tampering | selecionar_vencedor | mitigate | `transaction.atomic() + RFQ.objects.select_for_update()` — dois Compradores simultâneos serializam |
| T-04-06 | Tampering | selecionar_vencedor | mitigate | Guard `if rfq.tem_vencedor: raise ValueError` dentro da transação (defense in depth além da view) |
| T-04-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo instalado nesta fase (RESEARCH §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `python -m pytest apps/cotacoes/ -x -q` — testes de model e service GREEN; testes de view em skip
- `python manage.py makemigrations cotacoes --check --dry-run` — "No changes detected"
- `python manage.py migrate` — migração 0001 aplica sem erro
</verification>

<success_criteria>
- App `apps.cotacoes` registrado e migrável
- Modelos RFQ + CotacaoFornecedor com unicidade (OneToOneField) e MinValueValidator
- services.calcular_comparativo correto (menor preço, delta %, guard zero)
- services.selecionar_vencedor imutável com select_for_update
- Suite Wave 0 existe; testes de service e modelo verdes
</success_criteria>

<output>
Create `.planning/phases/04-quotations-rfq/04-01-SUMMARY.md` when done
</output>
