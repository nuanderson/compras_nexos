---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - templates/base.html
autonomous: true
requirements:
  - FORN-01
  - FORN-03
  - FORN-04
  - FORN-05
  - EST-01
  - EST-05
  - EST-06

must_haves:
  truths:
    - "Link 'Fornecedores' aparece na navegação apenas para comprador e admin"
    - "Link 'Estoque' aparece na navegação para todos os roles autenticados"
    - "Suite de testes completa da fase 3 passa sem regressões"
    - "django migrate --check não reporta migrações pendentes após ambos os apps registrados"
  artifacts:
    - path: "templates/base.html"
      provides: "links de navegação para fornecedores e estoque"
      contains: "fornecedores:lista"
  key_links:
    - from: "templates/base.html"
      to: "apps/fornecedores/urls.py (namespace=fornecedores)"
      via: "{% url 'fornecedores:lista' %}"
      pattern: "fornecedores:lista"
    - from: "templates/base.html"
      to: "apps/estoque/urls.py (namespace=estoque)"
      via: "{% url 'estoque:lista' %}"
      pattern: "estoque:lista"
---

<objective>
Integrar os apps fornecedores e estoque à navegação global do projeto e verificar ausência de regressões na suite completa.

Purpose: Fechar a fase 3 conectando os dois novos apps ao base.html e confirmando que todos os 11 requisitos (FORN-01..05, EST-01..06) estão cobertos e nenhum teste anterior quebrou.

Output:
- base.html com links de navegação para /fornecedores/ e /estoque/ com controle de visibilidade por role
- Suite completa de testes (pytest -q) verde
</objective>

<execution_context>
@C:/Users/admre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-suppliers-inventory/03-01-SUMMARY.md
@.planning/phases/03-suppliers-inventory/03-02-SUMMARY.md
@.planning/ROADMAP.md

# Base template a modificar — ler antes de editar
@templates/base.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar links de navegação ao base.html</name>
  <files>templates/base.html</files>
  <action>
Ler o arquivo templates/base.html antes de editar para identificar a estrutura do sidebar/navbar.

Localizar os itens de navegação existentes (ex: links para requisicoes, aprovacoes) e adicionar:

1. Link "Fornecedores" — visível APENAS para comprador e admin:
   {% if request.user.role in "comprador,admin" or request.user.is_superuser %}
     <a href="{% url 'fornecedores:lista' %}" ...>Fornecedores</a>
   {% endif %}

2. Link "Estoque" — visível para TODOS os roles autenticados (Solicitante, Gestor, Comprador,
   Diretor, Admin):
   <a href="{% url 'estoque:lista' %}" ...>Estoque</a>

Preservar exatamente o estilo visual existente dos outros itens de navegação (dark theme,
classes CSS, ícones se houver). Não introduzir novos padrões CSS nem frameworks.

Verificar se request.user.role já é disponível via context processor — se não, garantir que
a comparação use o valor da string diretamente conforme User.Role.choices:
  "solicitante", "gestor", "comprador", "diretor", "admin"

Condição correta para Fornecedores:
  request.user.role == "comprador" or request.user.role == "admin" or request.user.is_superuser

NÃO usar request.user.role in "comprador,admin" pois o operador `in` em string verificaria
se o substring existe, não se o valor é igual ao elemento. Usar lógica explícita com `or`.
  </action>
  <verify>
    <automated>cd C:\Users\admre\compras_nexos && python -m pytest apps/fornecedores/ apps/estoque/ -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - templates/base.html contém "{% url 'fornecedores:lista' %}"
    - templates/base.html contém "{% url 'estoque:lista' %}"
    - Link de Fornecedores está dentro de bloco condicional de role comprador/admin
    - Link de Estoque está fora de bloco condicional (visível a todos)
  </done>
</task>

<task type="auto">
  <name>Task 2: Validação final — suite completa e verificação de migrações</name>
  <files></files>
  <action>
1. Executar a suite completa de testes:
   pytest -q
   Se houver falhas, corrigir antes de prosseguir.

2. Verificar migrações pendentes:
   python manage.py migrate --check
   Se falhar, criar as migrações ausentes (não deve ocorrer pois as migrations são manuais nos planos 03-01 e 03-02).

3. Verificar que python-stdnum está instalado (se não estiver, executar pip install python-stdnum==2.2):
   python -c "from stdnum.br import cnpj; print(cnpj.validate('11222333000181'))"
   Deve imprimir "11222333000181" sem exceção.

4. Verificar que TrigramSimilarity está acessível:
   python -c "from django.contrib.postgres.search import TrigramSimilarity; print('OK')"

5. Atualizar ROADMAP.md — alterar a linha de "Plans: TBD" da Phase 3 para:
   Plans: 3 plans
   Plans:
   - [ ] 03-01-PLAN.md — App fornecedores: modelo, CNPJ validation, busca fuzzy pg_trgm, toggle ativo, testes FORN-01..05
   - [ ] 03-02-PLAN.md — App estoque: UnidadeMedida seed, ItemEstoque com isolamento por unidade, select_for_update, testes EST-01..06
   - [ ] 03-03-PLAN.md — Navegação: links fornecedores/estoque no base.html + validação da suite completa
  </action>
  <verify>
    <automated>cd C:\Users\admre\compras_nexos && python -m pytest -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - pytest -q termina com 0 failures (nenhuma regressão)
    - python manage.py migrate --check não reporta pendências
    - ROADMAP.md Phase 3 tem lista de 3 planos
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Template → URL resolver | Links de navegação renderizados para o browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Information Disclosure | base.html — link Fornecedores | mitigate | Condicional de role no template oculta o link para não-compradores; a view ainda exige CompradorRequiredMixin (defesa em profundidade — ocultar no nav não é suficiente sozinho) |
| T-03-11 | Tampering | ROADMAP.md — atualização de estado | accept | Arquivo de planejamento interno; sem impacto de segurança |
</threat_model>

<verification>
- pytest -q deve terminar com 0 failures
- python manage.py migrate --check retorna sucesso
- templates/base.html contém as duas URLs de navegação com condicionais corretos
</verification>

<success_criteria>
1. pytest -q verde — suite completa sem regressões
2. Link "Fornecedores" aparece no nav condicionalmente (comprador + admin)
3. Link "Estoque" aparece no nav para todos os roles autenticados
4. ROADMAP.md Phase 3 lista os 3 planos criados
5. python manage.py migrate --check passa
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-suppliers-inventory/03-03-SUMMARY.md`
</output>
