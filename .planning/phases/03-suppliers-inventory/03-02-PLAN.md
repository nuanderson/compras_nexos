---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - config/settings/base.py
  - config/urls.py
  - apps/estoque/__init__.py
  - apps/estoque/apps.py
  - apps/estoque/models.py
  - apps/estoque/forms.py
  - apps/estoque/views.py
  - apps/estoque/urls.py
  - apps/estoque/admin.py
  - apps/estoque/migrations/__init__.py
  - apps/estoque/migrations/0001_initial.py
  - apps/estoque/migrations/0002_seed_unidades.py
  - apps/estoque/tests/__init__.py
  - apps/estoque/tests/conftest.py
  - apps/estoque/tests/test_models.py
  - apps/estoque/tests/test_forms.py
  - apps/estoque/tests/test_views.py
  - templates/estoque/lista.html
  - templates/estoque/form.html
  - templates/estoque/atualizar_quantidade.html
  - templates/estoque/visao_consolidada.html
  - templates/estoque/partials/item_row.html
autonomous: true
requirements:
  - EST-01
  - EST-02
  - EST-03
  - EST-04
  - EST-05
  - EST-06

must_haves:
  truths:
    - "Solicitante cadastra item de estoque vinculado à própria unidade organizacional — EST-01, D-06"
    - "Item com quantidade_atual < quantidade_minima tem abaixo_do_minimo=True — EST-04"
    - "Solicitante NÃO acessa itens de outra unidade via URL manipulation — EST-05, D-06"
    - "Comprador vê itens de todas as unidades na visão consolidada — EST-06, D-06"
    - "Atualização de quantidade usa select_for_update() dentro de transaction.atomic() — D-05"
    - "Seed migration cria 8 unidades de medida (UN, KG, CX, L, M, PAR, PCT, RES) — D-04"
    - "Items abaixo do mínimo são destacados visualmente na listagem — EST-04"
  artifacts:
    - path: "apps/estoque/models.py"
      provides: "UnidadeMedida + ItemEstoque(TimestampedModel)"
      contains: "class ItemEstoque"
    - path: "apps/estoque/forms.py"
      provides: "ItemEstoqueForm + AtualizarQuantidadeForm"
      contains: "AtualizarQuantidadeForm"
    - path: "apps/estoque/views.py"
      provides: "4 views com isolamento de unidade"
      contains: "select_for_update"
    - path: "apps/estoque/migrations/0001_initial.py"
      provides: "tabelas UnidadeMedida e ItemEstoque"
      contains: "class Migration"
    - path: "apps/estoque/migrations/0002_seed_unidades.py"
      provides: "seed das 8 unidades de medida"
      contains: "seed_unidades"
    - path: "templates/estoque/lista.html"
      provides: "listagem com highlight abaixo do mínimo"
    - path: "templates/estoque/visao_consolidada.html"
      provides: "visão consolidada Comprador/Admin"
  key_links:
    - from: "apps/estoque/views.py"
      to: "ItemEstoque.objects.select_for_update()"
      via: "transaction.atomic()"
      pattern: "select_for_update"
    - from: "apps/estoque/views.py"
      to: "request.user.default_unit"
      via: "filtro unidade_organizacional"
      pattern: "default_unit"
    - from: "templates/estoque/lista.html"
      to: "item.abaixo_do_minimo"
      via: "propriedade do model"
      pattern: "abaixo_do_minimo"
---

<objective>
Criar o app completo apps/estoque com modelo configurável de unidade de medida, itens de estoque por unidade organizacional, isolamento de acesso por unidade e visão consolidada para comprador/admin.

Purpose: Entregar EST-01..06 — cadastro, atualização com lock concorrente, highlight de estoque baixo, isolamento por unidade e visão consolidada.

Output:
- App apps/estoque registrado em INSTALLED_APPS e config/urls.py
- UnidadeMedida configurável via Admin com seed de 8 unidades (D-04)
- ItemEstoque com UniqueConstraint(nome, unidade_organizacional) para evitar duplicatas internas
- Propriedade abaixo_do_minimo no model (EST-04)
- Atualização de quantidade com select_for_update() + transaction.atomic() (D-05)
- Isolamento de unidade via filtro em queryset de Solicitante; Comprador vê tudo (D-06)
- Suite de testes cobrindo EST-01..06 incluindo teste de isolamento de segurança
</objective>

<execution_context>
@C:/Users/admre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Modelos base e accounts para FK
@apps/core/models.py
@apps/accounts/models.py

# Padrão de conftest para testes
@apps/aprovacoes/tests/conftest.py

# Configurações a atualizar
@config/settings/base.py
@config/urls.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold do app estoque — modelos, formulários e migrações</name>
  <files>
    config/settings/base.py,
    config/urls.py,
    apps/estoque/__init__.py,
    apps/estoque/apps.py,
    apps/estoque/models.py,
    apps/estoque/forms.py,
    apps/estoque/admin.py,
    apps/estoque/migrations/__init__.py,
    apps/estoque/migrations/0001_initial.py,
    apps/estoque/migrations/0002_seed_unidades.py,
    apps/estoque/tests/__init__.py,
    apps/estoque/tests/conftest.py,
    apps/estoque/tests/test_models.py,
    apps/estoque/tests/test_forms.py
  </files>
  <behavior>
    - UnidadeMedida(nome="Unidade", sigla="UN") salva e __str__ retorna "Unidade (UN)"
    - ItemEstoque com quantidade_atual=5, quantidade_minima=10 tem abaixo_do_minimo=True
    - ItemEstoque com quantidade_atual=10, quantidade_minima=10 tem abaixo_do_minimo=False
    - ItemEstoque com quantidade_atual=11, quantidade_minima=10 tem abaixo_do_minimo=False
    - Criar dois ItemEstoque com mesmo nome na mesma unidade levanta IntegrityError
    - Criar dois ItemEstoque com mesmo nome em unidades diferentes é permitido
    - Migration 0002 cria exatamente 8 unidades: UN, KG, CX, L, M, PAR, PCT, RES
    - ItemEstoqueForm com quantidade_atual=0 é válido (zero é aceitável)
    - AtualizarQuantidadeForm com quantidade_atual=-1 é inválido (quantidade não pode ser negativa)
  </behavior>
  <action>
1. Adicionar a config/settings/base.py INSTALLED_APPS:
   "apps.estoque",
   (adicionar após "apps.fornecedores" — plano 03-01 já adicionou fornecedores)

2. Adicionar a config/urls.py:
   path("estoque/", include("apps.estoque.urls")),
   (adicionar antes de path("", include("apps.core.urls")))

3. Criar apps/estoque/__init__.py (vazio).

4. Criar apps/estoque/apps.py:
   - AppConfig name="apps.estoque", verbose_name="Estoque", default_auto_field="django.db.models.BigAutoField"

5. Criar apps/estoque/models.py com dois modelos:

   a. UnidadeMedida(models.Model) — per D-04:
      - nome = CharField(max_length=50, unique=True)
      - sigla = CharField(max_length=10, unique=True)
      - ativo = BooleanField(default=True)
      - Meta: ordering=["nome"], verbose_name="Unidade de Medida", verbose_name_plural="Unidades de Medida"
      - __str__: retorna f"{self.nome} ({self.sigla})"

   b. ItemEstoque(TimestampedModel) — per D-06 (FK para UnidadeOrganizacional):
      - nome = CharField(max_length=200)
      - unidade_medida = ForeignKey(UnidadeMedida, on_delete=PROTECT, related_name="itens_estoque")
      - quantidade_atual = IntegerField(default=0)  — per instrução explícita do prompt (inteiros)
      - quantidade_minima = IntegerField(default=0)  — per instrução explícita do prompt (inteiros)
      - unidade_organizacional = ForeignKey("accounts.UnidadeOrganizacional", on_delete=PROTECT,
          related_name="itens_estoque")
      - Meta:
          verbose_name = "Item de Estoque"
          verbose_name_plural = "Itens de Estoque"
          ordering = ["nome"]
          constraints = [
              models.UniqueConstraint(
                  fields=["nome", "unidade_organizacional"],
                  name="unique_item_por_unidade",
              )
          ]  — UniqueConstraint (não unique_together, que é deprecated, per pitfall 8 do research)
      - __str__: f"{self.nome} ({self.unidade_organizacional})"
      - @property abaixo_do_minimo(self) -> bool:
          return self.quantidade_atual < self.quantidade_minima

6. Criar apps/estoque/forms.py:

   a. ItemEstoqueForm(ModelForm):
      - Meta: model=ItemEstoque, fields=["nome", "unidade_medida", "quantidade_atual", "quantidade_minima"]
      - Widget de unidade_medida: ModelChoiceField filtra UnidadeMedida.objects.filter(ativo=True)
      - unidade_organizacional NÃO está no form — é atribuído na view (request.user.default_unit)

   b. AtualizarQuantidadeForm(ModelForm):
      - Meta: model=ItemEstoque, fields=["quantidade_atual"]
      - clean_quantidade_atual: validar que valor >= 0 (raise ValidationError se negativo)

7. Criar apps/estoque/admin.py:
   - UnidadeMedidaAdmin: list_display=["nome","sigla","ativo"], list_editable=["ativo"],
     list_filter=["ativo"], search_fields=["nome","sigla"], ordering=["nome"]
   - ItemEstoqueAdmin: list_display=["nome","unidade_organizacional","quantidade_atual",
     "quantidade_minima","abaixo_do_minimo_display"],
     list_filter=["unidade_organizacional","unidade_medida"],
     search_fields=["nome"], raw_id_fields=["unidade_organizacional"]
   - Método abaixo_do_minimo_display(self, obj): retorna obj.abaixo_do_minimo
     abaixo_do_minimo_display.boolean = True
     abaixo_do_minimo_display.short_description = "Abaixo do mínimo"

8. Criar apps/estoque/migrations/0001_initial.py manualmente:
   - Depende de: [("accounts", "0001_initial")]
   - Criar tabela estoque_unidademedida com nome, sigla (ambos unique), ativo
   - Criar tabela estoque_itemestoque com nome, unidade_medida_id, quantidade_atual,
     quantidade_minima, unidade_organizacional_id, criado_em, atualizado_em
   - Criar UniqueConstraint unique_item_por_unidade (nome + unidade_organizacional_id)

9. Criar apps/estoque/migrations/0002_seed_unidades.py — per D-04:
   - dependencies = [("estoque", "0001_initial")]
   - UNIDADES_INICIAIS = [("Unidade","UN"),("Quilograma","KG"),("Caixa","CX"),
     ("Litro","L"),("Metro","M"),("Par","PAR"),("Pacote","PCT"),("Resma","RES")]
   - Função seed_unidades(apps, schema_editor): usa apps.get_model("estoque","UnidadeMedida")
     e get_or_create por sigla (NÃO importar model diretamente — pitfall de migration reversal)
   - Função unseed_unidades(apps, schema_editor): deleta pelos siglas da lista
   - operations = [RunPython(seed_unidades, unseed_unidades)]

10. Criar apps/estoque/tests/conftest.py com fixtures:
    - test_unit(db): UnidadeOrganizacional(nome="Unidade A")
    - outra_unit(db): UnidadeOrganizacional(nome="Unidade B")
    - solicitante_user(db, test_unit): User(role="solicitante", default_unit=test_unit)
    - comprador_user(db): User(role="comprador", default_unit=None)
    - unidade_medida(db): UnidadeMedida(nome="Unidade", sigla="UN_TEST")
      (usar sigla distinta para não conflitar com seed)
    - item_estoque(db, test_unit, unidade_medida): ItemEstoque(nome="Papel A4",
      quantidade_atual=50, quantidade_minima=10, unidade_organizacional=test_unit,
      unidade_medida=unidade_medida)
    - item_abaixo_minimo(db, test_unit, unidade_medida): ItemEstoque(nome="Caneta",
      quantidade_atual=2, quantidade_minima=20, unidade_organizacional=test_unit,
      unidade_medida=unidade_medida)

11. Criar apps/estoque/tests/test_models.py:
    - test_abaixo_do_minimo_true: qty=5, min=10 → True
    - test_abaixo_do_minimo_false_igual: qty=10, min=10 → False
    - test_abaixo_do_minimo_false_acima: qty=15, min=10 → False
    - test_unique_constraint_mesma_unidade: dois itens mesmo nome + mesma unidade → IntegrityError
    - test_unique_constraint_unidade_diferente: mesmo nome + unidades diferentes → OK
    - test_str: __str__ inclui nome e unidade organizacional

12. Criar apps/estoque/tests/test_forms.py:
    - test_quantidade_negativa_invalida: AtualizarQuantidadeForm com -1 → inválido
    - test_quantidade_zero_valida: AtualizarQuantidadeForm com 0 → válido
    - test_item_form_campos_obrigatorios: nome e unidade_medida obrigatórios
  </action>
  <verify>
    <automated>cd C:\Users\admre\compras_nexos && python -m pytest apps/estoque/tests/test_models.py apps/estoque/tests/test_forms.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - INSTALLED_APPS contém "apps.estoque"
    - Migrações 0001 e 0002 existem e são válidas
    - test_models.py e test_forms.py passam (abaixo_do_minimo, UniqueConstraint, formulários)
    - python manage.py migrate --check não falha
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Views, URLs e templates de estoque</name>
  <files>
    apps/estoque/views.py,
    apps/estoque/urls.py,
    apps/estoque/tests/test_views.py,
    templates/estoque/lista.html,
    templates/estoque/form.html,
    templates/estoque/atualizar_quantidade.html,
    templates/estoque/visao_consolidada.html,
    templates/estoque/partials/item_row.html
  </files>
  <behavior>
    - GET /estoque/ com solicitante retorna 200 — lista apenas itens da própria unidade
    - GET /estoque/ com comprador retorna 200 — lista itens de todas as unidades
    - GET /estoque/<pk>/ com solicitante e pk de outra unidade retorna 404 (isolamento EST-05)
    - POST /estoque/novo/ com solicitante cria item vinculado a request.user.default_unit
    - POST /estoque/<pk>/quantidade/ com select_for_update atualiza quantidade_atual
    - GET /estoque/consolidado/ com solicitante retorna 403
    - GET /estoque/consolidado/ com comprador retorna 200 com itens de todas as unidades
    - Items abaixo do mínimo recebem classe CSS "abaixo-minimo" na listagem
  </behavior>
  <action>
1. Criar apps/estoque/views.py com:

   a. Mixins de acesso — seguindo padrão de GestorRequiredMixin em apps/aprovacoes/views.py:
      - LoginRequiredMixin já é base de todos
      - Verificação de default_unit: se request.user.default_unit is None, redirecionar
        para página de lista com mensagem "Sem unidade configurada" (não 500)

   b. ListaEstoqueView(LoginRequiredMixin, ListView):
      - template_name = "estoque/lista.html", context_object_name = "itens"
      - get_queryset:
        - Comprador ou Admin (role in "comprador","admin" or is_superuser):
            qs = ItemEstoque.objects.select_related("unidade_medida","unidade_organizacional").order_by("nome")
        - Demais roles (Solicitante, Gestor, Diretor):
            if not request.user.default_unit: return ItemEstoque.objects.none()
            qs = ItemEstoque.objects.filter(unidade_organizacional=request.user.default_unit)
               .select_related("unidade_medida").order_by("nome")
        — isolamento D-06: nunca expor itens de outra unidade para Solicitante

   c. CadastrarItemEstoqueView(LoginRequiredMixin, View):
      - GET: render form ItemEstoqueForm
      - POST: form = ItemEstoqueForm(request.POST)
        if form.is_valid():
            item = form.save(commit=False)
            item.unidade_organizacional = request.user.default_unit
            item.save()
            redirect("estoque:lista")
      - unidade_organizacional vem do usuário autenticado, nunca do form (D-06)

   d. EditarItemEstoqueView(LoginRequiredMixin, View):
      - get_object: comprador/admin → get_object_or_404(ItemEstoque, pk=pk)
        solicitante/gestor → get_object_or_404(ItemEstoque, pk=pk,
            unidade_organizacional=request.user.default_unit)  — IDOR guard EST-05
      - GET/POST: mesmo padrão de Cadastrar com instance=item

   e. AtualizarQuantidadeView(LoginRequiredMixin, View):
      - Aceita apenas POST
      - with transaction.atomic():  — obrigatório para select_for_update (pitfall 5 do research)
            item = ItemEstoque.objects.select_for_update().get(
                pk=pk, unidade_organizacional=request.user.default_unit  — IDOR guard
            )   — usar get() dentro do atomic; tratar DoesNotExist como 404
            form = AtualizarQuantidadeForm(request.POST, instance=item)
            if form.is_valid(): form.save()
      - Retorna partial "estoque/partials/item_row.html" para HTMX swap
      - Retorna 404 se pk não existe na unidade do usuário

   f. VisaoConsolidadaView(LoginRequiredMixin, ListView):
      - Bloquear Solicitante/Gestor/Diretor com PermissionDenied se role not in ("comprador","admin")
        e not is_superuser
      - template_name = "estoque/visao_consolidada.html"
      - get_queryset: ItemEstoque.objects.select_related("unidade_medida","unidade_organizacional")
          .order_by("unidade_organizacional__nome","nome")
      - Sem filtro de unidade — visão de todas as unidades (EST-06)

2. Criar apps/estoque/urls.py:
   - app_name = "estoque"
   - urlpatterns:
     - path("", ListaEstoqueView, name="lista")
     - path("novo/", CadastrarItemEstoqueView, name="cadastrar")
     - path("<int:pk>/editar/", EditarItemEstoqueView, name="editar")
     - path("<int:pk>/quantidade/", AtualizarQuantidadeView, name="atualizar-quantidade")
     - path("consolidado/", VisaoConsolidadaView, name="consolidado")

3. Criar templates/estoque/lista.html:
   - Estende base.html, título "Meu Estoque" com link "Novo Item"
   - Tabela com colunas: Nome, Unidade de Medida, Quantidade Atual, Mínimo, Status, Ações
   - Para cada item: se item.abaixo_do_minimo → classe CSS "abaixo-minimo" ou background
     vermelho/accent (#e94560 em 20% opacidade) na linha
   - Botão "Atualizar Qtd" por linha com hx-post="{% url 'estoque:atualizar-quantidade' item.pk %}"
     hx-target="#item-{{ item.pk }}", hx-swap="outerHTML", abrindo modal ou inline form
   - Dark theme: #1a1a2e, #16213e, accent #e94560
   - Link "Visão Consolidada" visível apenas para comprador/admin via {% if request.user.role in "comprador,admin" or request.user.is_superuser %}

4. Criar templates/estoque/partials/item_row.html:
   - Linha da tabela com id="item-{{ item.pk }}"
   - Inclui classe condicional para abaixo_do_minimo
   - Formulário inline (ou mini modal via HTMX) para AtualizarQuantidadeForm

5. Criar templates/estoque/form.html:
   - Estende base.html, título dinâmico ("Novo Item" ou "Editar Item")
   - Campos: nome, unidade_medida, quantidade_atual, quantidade_minima
   - Exibição de erros por campo
   - Botões Submit + Cancelar

6. Criar templates/estoque/atualizar_quantidade.html:
   - Template minimal para fallback sem HTMX
   - Form com apenas o campo quantidade_atual + submit

7. Criar templates/estoque/visao_consolidada.html:
   - Estende base.html, título "Estoque Consolidado — Todas as Unidades"
   - Agrupa itens por unidade_organizacional (ordenação no queryset garante agrupamento visual)
   - Destaca itens abaixo do mínimo com mesmo padrão visual da lista

8. Criar apps/estoque/tests/test_views.py:
   - test_lista_solicitante_ve_apenas_propria_unidade: solicitante com test_unit vê item_estoque
     mas não vê item de outra_unit
   - test_lista_comprador_ve_todas_unidades: comprador vê itens de test_unit e outra_unit
   - test_isolamento_idor_solicitante: solicitante não acessa item de outra unidade via pk URL
     (espera 404)
   - test_cadastrar_item_vincula_unidade_usuario: POST cria item com unidade=request.user.default_unit
   - test_atualizar_quantidade: POST /estoque/<pk>/quantidade/ atualiza quantidade_atual
   - test_atualizar_quantidade_negativa_invalida: POST com -1 retorna form com erro
   - test_visao_consolidada_comprador_ok: comprador acessa /estoque/consolidado/ com 200
   - test_visao_consolidada_solicitante_403: solicitante recebe 403 em /estoque/consolidado/
  </action>
  <verify>
    <automated>cd C:\Users\admre\compras_nexos && python -m pytest apps/estoque/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pytest apps/estoque/ passa completamente (0 failures)
    - Solicitante vê apenas itens da própria unidade
    - Solicitante recebe 404 ao tentar acessar item de outra unidade pelo pk
    - Comprador vê todos os itens em /estoque/consolidado/
    - Atualização de quantidade usa select_for_update dentro de transaction.atomic
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Browser → /estoque/* | Inputs não confiáveis: pk na URL, quantidade_atual no POST |
| Solicitante → ItemEstoque | Acesso por pk na URL pode expor itens de outras unidades |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure / Tampering | EditarItemEstoqueView, AtualizarQuantidadeView | mitigate | get_object_or_404 sempre com filtro unidade_organizacional=request.user.default_unit para Solicitante — nunca só pk (D-06, pitfall 7 do research) |
| T-03-06 | Elevation of Privilege | VisaoConsolidadaView | mitigate | PermissionDenied se role not in ("comprador","admin") e not is_superuser (EST-06) |
| T-03-07 | Tampering | AtualizarQuantidadeView | mitigate | select_for_update() dentro de transaction.atomic() previne race condition (D-05, pitfall 5 do research) |
| T-03-08 | Tampering | AtualizarQuantidadeView | mitigate | AtualizarQuantidadeForm.clean valida quantidade >= 0 — rejeita valores negativos |
| T-03-09 | Tampering | CadastrarItemEstoqueView | mitigate | unidade_organizacional definida por request.user.default_unit, nunca por campo de formulário — impede Solicitante de criar item em outra unidade |
</threat_model>

<verification>
- python -m pytest apps/estoque/ -q deve terminar com 0 failures
- python manage.py migrate --check não deve reportar migrações pendentes
- UnidadeMedida.objects.count() deve ser 8 após migrate (seed migration)
- Solicitante autenticado em /estoque/consolidado/ recebe 403
</verification>

<success_criteria>
1. "apps.estoque" está em INSTALLED_APPS
2. Migrations 0001 e 0002 existem e são válidas
3. Seed cria 8 unidades de medida (UN, KG, CX, L, M, PAR, PCT, RES)
4. pytest apps/estoque/ passa (todos os testes verde)
5. item.abaixo_do_minimo retorna True corretamente
6. Solicitante não acessa itens de outra unidade (404 por IDOR guard)
7. Comprador acessa visão consolidada de todas as unidades
8. Atualização de quantidade usa select_for_update() dentro de transaction.atomic()
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-suppliers-inventory/03-02-SUMMARY.md`
</output>
