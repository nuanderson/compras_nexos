---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config/settings/base.py
  - config/urls.py
  - apps/fornecedores/__init__.py
  - apps/fornecedores/apps.py
  - apps/fornecedores/models.py
  - apps/fornecedores/forms.py
  - apps/fornecedores/views.py
  - apps/fornecedores/urls.py
  - apps/fornecedores/admin.py
  - apps/fornecedores/templatetags/__init__.py
  - apps/fornecedores/templatetags/fornecedor_tags.py
  - apps/fornecedores/migrations/__init__.py
  - apps/fornecedores/migrations/0001_initial.py
  - apps/fornecedores/tests/__init__.py
  - apps/fornecedores/tests/conftest.py
  - apps/fornecedores/tests/test_models.py
  - apps/fornecedores/tests/test_forms.py
  - apps/fornecedores/tests/test_views.py
  - templates/fornecedores/lista.html
  - templates/fornecedores/form.html
  - templates/fornecedores/partials/fornecedor_list.html
autonomous: true
requirements:
  - FORN-01
  - FORN-02
  - FORN-03
  - FORN-04
  - FORN-05

must_haves:
  truths:
    - "Comprador pode cadastrar fornecedor com CNPJ válido (numérico e alfanumérico Jul/2026) — D-02"
    - "Formulário rejeita CNPJ inválido com mensagem clara e rejeita CNPJ duplicado"
    - "Comprador pode buscar fornecedores por nome (fuzzy pg_trgm) ou CNPJ (exato) — D-03"
    - "Comprador pode filtrar fornecedores por categoria — D-01"
    - "Comprador pode inativar/reativar fornecedor via HTMX sem perder o registro — D-04"
    - "CNPJ é exibido formatado (XX.XXX.XXX/XXXX-XX) nos templates — template filter cnpj_format"
    - "Solicitante recebe 403 ao tentar acessar qualquer URL de fornecedores — CompradorRequiredMixin"
  artifacts:
    - path: "apps/fornecedores/models.py"
      provides: "Fornecedor(TimestampedModel)"
      contains: "class Fornecedor"
    - path: "apps/fornecedores/forms.py"
      provides: "FornecedorForm com clean_cnpj via stdnum.validate"
      contains: "clean_cnpj"
    - path: "apps/fornecedores/views.py"
      provides: "CompradorRequiredMixin + 4 views"
      contains: "CompradorRequiredMixin"
    - path: "apps/fornecedores/templatetags/fornecedor_tags.py"
      provides: "filtro cnpj_format"
      contains: "cnpj_format"
    - path: "apps/fornecedores/migrations/0001_initial.py"
      provides: "migration do modelo Fornecedor"
      contains: "class Migration"
    - path: "templates/fornecedores/lista.html"
      provides: "página de listagem com HTMX live search"
    - path: "templates/fornecedores/partials/fornecedor_list.html"
      provides: "partial retornada pelo HTMX"
  key_links:
    - from: "apps/fornecedores/forms.py"
      to: "stdnum.br.cnpj.validate"
      via: "import direto"
      pattern: "from stdnum.br import cnpj"
    - from: "apps/fornecedores/views.py"
      to: "TrigramSimilarity"
      via: "django.contrib.postgres.search"
      pattern: "TrigramSimilarity"
    - from: "templates/fornecedores/partials/fornecedor_list.html"
      to: "fornecedor_tags"
      via: "{% load fornecedor_tags %}"
      pattern: "cnpj_format"
---

<objective>
Criar o app completo apps/fornecedores com modelo, formulário, views, templates, template tag, admin e testes.

Purpose: Entregar o requisito FORN-01..05 — cadastro, busca fuzzy, filtragem por categoria e toggle ativo de fornecedores, com validação de CNPJ via python-stdnum (incluindo formato alfanumérico Jul/2026, D-02).

Output:
- App apps/fornecedores registrado em INSTALLED_APPS e config/urls.py
- python-stdnum==2.2 adicionado a requirements.txt
- Modelo Fornecedor com CNPJ compactado de 14 chars (D-02), FK CategoriaCompra PROTECT (D-01)
- Busca fuzzy TrigramSimilarity em razao_social + busca exata em cnpj (D-03)
- Toggle ativo via HTMX POST sem deleção do registro (FORN-04)
- Suite de testes cobrindo FORN-01..05
</objective>

<execution_context>
@C:/Users/admre/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/admre/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

# Modelos existentes reutilizados neste plano
@apps/core/models.py
@apps/requisicoes/models.py
@apps/accounts/models.py

# Padrão de Mixin a seguir (GestorRequiredMixin → CompradorRequiredMixin)
@apps/aprovacoes/views.py

# Padrão de namespace e urls
@apps/aprovacoes/urls.py

# Padrão de fixtures para testes
@apps/aprovacoes/tests/conftest.py

# Configurações a atualizar
@config/settings/base.py
@config/urls.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold do app fornecedores — modelo, formulário e migração</name>
  <files>
    requirements.txt,
    config/settings/base.py,
    config/urls.py,
    apps/fornecedores/__init__.py,
    apps/fornecedores/apps.py,
    apps/fornecedores/models.py,
    apps/fornecedores/forms.py,
    apps/fornecedores/admin.py,
    apps/fornecedores/templatetags/__init__.py,
    apps/fornecedores/templatetags/fornecedor_tags.py,
    apps/fornecedores/migrations/__init__.py,
    apps/fornecedores/migrations/0001_initial.py
  </files>
  <behavior>
    - Fornecedor com cnpj="11222333000181" salva corretamente (14 chars, unique)
    - Fornecedor com cnpj="12ABC34501DE35" salva corretamente (CNPJ alfanumérico Jul/2026)
    - FornecedorForm.clean_cnpj rejeita "00000000000000" com ValidationError
    - FornecedorForm.clean_cnpj rejeita CNPJ duplicado excluindo o próprio pk em edição
    - FornecedorForm.clean_cnpj aceita "11.222.333/0001-81" e retorna "11222333000181" (compactado)
    - cnpj_format("11222333000181") retorna "11.222.333/0001-81"
    - cnpj_format("12ABC34501DE35") retorna "12.ABC.345/01DE-35"
    - cnpj_format("") retorna "" sem exceção
    - FK categoria: deletar CategoriaCompra com fornecedor vinculado levanta ProtectedError
  </behavior>
  <action>
1. Adicionar ao requirements.txt (se ausente — checar primeiro com grep): python-stdnum==2.2

2. Adicionar a config/settings/base.py INSTALLED_APPS (antes de apps.estoque que virá no plano 03-02):
   "apps.fornecedores",

3. Adicionar a config/urls.py antes do path("", include("apps.core.urls")):
   path("fornecedores/", include("apps.fornecedores.urls")),

4. Criar apps/fornecedores/__init__.py (vazio).

5. Criar apps/fornecedores/apps.py:
   - AppConfig com name="apps.fornecedores", verbose_name="Fornecedores", default_auto_field="django.db.models.BigAutoField"

6. Criar apps/fornecedores/models.py — classe Fornecedor(TimestampedModel):
   - cnpj = CharField(max_length=14, unique=True)
   - razao_social = CharField(max_length=200)
   - email = EmailField()
   - telefone = CharField(max_length=20, blank=True, default="")  — opcional (A2 do research)
   - categoria = ForeignKey("requisicoes.CategoriaCompra", on_delete=PROTECT, related_name="fornecedores")
   - ativo = BooleanField(default=True)
   - Meta: verbose_name="Fornecedor", verbose_name_plural="Fornecedores", ordering=["razao_social"]
   - __str__: retorna razao_social
   Importar: from apps.core.models import TimestampedModel

7. Criar apps/fornecedores/forms.py — classe FornecedorForm(ModelForm):
   - Meta: model=Fornecedor, fields=["cnpj", "razao_social", "email", "telefone", "categoria"]
   - Widget de categoria: ModelChoiceField filtra CategoriaCompra.objects.filter(ativo=True)
   - clean_cnpj(self):
     a. valor = self.cleaned_data.get("cnpj", "").strip()
     b. Tentar: compactado = cnpj_lib.validate(valor)   # from stdnum.br import cnpj as cnpj_lib
     c. Except StdnumValidationError (from stdnum.exceptions import ValidationError as StdnumValidationError):
        raise forms.ValidationError("CNPJ inválido. Verifique os dígitos.")
     d. Unicidade: qs = Fornecedor.objects.filter(cnpj=compactado); excluir self.instance.pk se existir
     e. Se qs.exists(): raise forms.ValidationError("Já existe um fornecedor cadastrado com este CNPJ.")
     f. return compactado
   NUNCA usar except Exception bare — capturar apenas StdnumValidationError (per RESEARCH.md pitfall 1).

8. Criar apps/fornecedores/admin.py:
   - FornecedorAdmin: list_display=["razao_social","cnpj","categoria","ativo"], list_filter=["ativo","categoria"],
     search_fields=["razao_social","cnpj"], list_editable=["ativo"]

9. Criar apps/fornecedores/templatetags/__init__.py (vazio).

10. Criar apps/fornecedores/templatetags/fornecedor_tags.py:
    - Registrar filtro @register.filter com nome "cnpj_format"
    - Importar: from stdnum.br import cnpj as cnpj_lib
    - Se value vazio: return value
    - try: return cnpj_lib.format(value)
    - except Exception: return value   — fallback seguro para exibição

11. Criar apps/fornecedores/migrations/__init__.py (vazio).

12. Criar apps/fornecedores/migrations/0001_initial.py manualmente (não via makemigrations):
    - Depende de: [("accounts", "0001_initial"), ("requisicoes", "0001_initial")]
    - NÃO adicionar TrigramExtension — já está em accounts/0001_initial.py (verificado)
    - Criar tabela fornecedores_fornecedor com todos os campos do modelo
    - Criar índice em ativo e em categoria_id
  </action>
  <verify>
    <automated>cd C:\Users\admre\compras_nexos && python -m pytest apps/fornecedores/tests/test_models.py apps/fornecedores/tests/test_forms.py -x -q 2>&1 | head -40</automated>
  </verify>
  <done>
    - requirements.txt contém python-stdnum==2.2
    - INSTALLED_APPS contém "apps.fornecedores"
    - Migration 0001_initial.py existe e é válida (python manage.py migrate --check não falha)
    - test_models.py e test_forms.py passam (CNPJ válido/inválido/alfanumérico, unicidade, PROTECT)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Views, URLs e templates de fornecedores</name>
  <files>
    apps/fornecedores/views.py,
    apps/fornecedores/urls.py,
    apps/fornecedores/tests/conftest.py,
    apps/fornecedores/tests/test_views.py,
    templates/fornecedores/lista.html,
    templates/fornecedores/form.html,
    templates/fornecedores/partials/fornecedor_list.html
  </files>
  <behavior>
    - GET /fornecedores/ com role=comprador retorna 200
    - GET /fornecedores/ com role=solicitante retorna 403
    - GET /fornecedores/?q=empresa retorna partial HTML com HTMX (request.htmx=True)
    - GET /fornecedores/?q= (query vazia) retorna lista completa, nunca 404
    - POST /fornecedores/novo/ com dados válidos cria Fornecedor e redireciona para lista
    - POST /fornecedores/<pk>/toggle-ativo/ inverte ativo e retorna partial de linha atualizada
    - GET /fornecedores/<pk>/editar/ com pk de outro fornecedor é acessível ao comprador (sem isolamento)
  </behavior>
  <action>
1. Criar apps/fornecedores/views.py com:

   a. CompradorRequiredMixin(LoginRequiredMixin) — seguindo exatamente o padrão de GestorRequiredMixin
      em apps/aprovacoes/views.py (D-02 de context, A3 de research):
      - dispatch: verifica is_authenticated → PermissionDenied se role not in ("comprador", "admin")
        e not is_superuser

   b. Função auxiliar get_queryset_fornecedores(q=None, categoria_pk=None, apenas_ativos=False):
      - qs = Fornecedor.objects.select_related("categoria")
      - if categoria_pk: qs = qs.filter(categoria_id=categoria_pk)
      - if q:
          q = q[:100]  — truncar a 100 chars (DoS guard, T-03-04 do research)
          cnpj_c = q.replace(".",  "").replace("/", "").replace("-", "").strip().upper()
          if len(cnpj_c) == 14 and cnpj_c.isalnum():
              qs = qs.filter(cnpj=cnpj_c)  — busca exata por CNPJ (D-03)
          else:
              qs = qs.annotate(sim=TrigramSimilarity("razao_social", q)).filter(sim__gt=0.1).order_by("-sim")
      - else: qs = qs.order_by("razao_social")
      - return qs
      Importar: from django.contrib.postgres.search import TrigramSimilarity
      NUNCA aplicar TrigramSimilarity com q vazio (pitfall 2 do research).

   c. ListaFornecedoresView(CompradorRequiredMixin, View):
      - get_method: q = request.GET.get("q","").strip(); categoria_pk = request.GET.get("categoria","")
      - ctx = {fornecedores: qs, categorias: CategoriaCompra.objects.filter(ativo=True), q: q, categoria_pk: categoria_pk}
      - if request.htmx: return render(request, "fornecedores/partials/fornecedor_list.html", ctx)
      - return render(request, "fornecedores/lista.html", ctx)

   d. CadastrarFornecedorView(CompradorRequiredMixin, View):
      - GET: render form vazio
      - POST: FornecedorForm(request.POST); if valid → save() → redirect("fornecedores:lista")
      - Falha: render form com erros

   e. EditarFornecedorView(CompradorRequiredMixin, View):
      - get_object: get_object_or_404(Fornecedor, pk=pk)  — sem filtro de unidade (fornecedor global)
      - GET/POST: mesmo padrão de CadastrarFornecedorView com instance=fornecedor

   f. ToggleAtivoView(CompradorRequiredMixin, View):
      - Aceita apenas POST (http_method_not_allowed para GET — pitfall do research)
      - fornecedor = get_object_or_404(Fornecedor, pk=pk)
      - fornecedor.ativo = not fornecedor.ativo
      - fornecedor.save(update_fields=["ativo"])
      - return render(request, "fornecedores/partials/fornecedor_row.html", {"fornecedor": fornecedor})
      - hx-swap="outerHTML" na linha da tabela

2. Criar apps/fornecedores/urls.py:
   - app_name = "fornecedores"
   - urlpatterns:
     - path("", ListaFornecedoresView, name="lista")
     - path("novo/", CadastrarFornecedorView, name="cadastrar")
     - path("<int:pk>/editar/", EditarFornecedorView, name="editar")
     - path("<int:pk>/toggle-ativo/", ToggleAtivoView, name="toggle-ativo")

3. Criar templates/fornecedores/lista.html:
   - Estende base.html
   - Cabeçalho com título "Fornecedores" e botão "Novo Fornecedor" (link para fornecedores:cadastrar)
   - Barra de busca com input type="search" name="q":
     hx-get="{% url 'fornecedores:lista' %}", hx-trigger="input delay:300ms, search",
     hx-target="#fornecedores-list", hx-swap="innerHTML",
     hx-include="[name='categoria']"
   - Select de categoria com name="categoria" com hx-get e hx-trigger="change"
     incluindo a mesma target e swap
   - Div id="fornecedores-list" contendo a partial fornecedor_list.html (include)
   - Dark theme: bg #1a1a2e, cards #16213e, accent #e94560

4. Criar templates/fornecedores/partials/fornecedor_list.html:
   - {% load fornecedor_tags %}
   - Tabela com colunas: Razão Social, CNPJ (via {{ f.cnpj|cnpj_format }}), Categoria, Status, Ações
   - Cada linha renderizada como partial via include "fornecedores/partials/fornecedor_row.html" OU
     inline com id="fornecedor-{{ f.pk }}" para o outerHTML swap do toggle
   - Se queryset vazio: "Nenhum fornecedor encontrado." (nunca 404)

5. Criar template inline fornecedor_row (pode ser parte do partial_list ou arquivo separado
   templates/fornecedores/partials/fornecedor_row.html) — linha da tabela com:
   - id="fornecedor-{{ fornecedor.pk }}"
   - Botão Toggle Ativo: hx-post="{% url 'fornecedores:toggle-ativo' fornecedor.pk %}",
     hx-target="#fornecedor-{{ fornecedor.pk }}", hx-swap="outerHTML",
     hx-confirm="Confirmar alteração de status?"
   - Exibe "Ativo" com destaque verde ou "Inativo" com texto apagado (#e94560 como accent)

6. Criar templates/fornecedores/form.html:
   - Estende base.html
   - Formulário POST com {% csrf_token %}
   - Campos: cnpj, razao_social, email, telefone, categoria
   - Mensagens de erro inline por campo ({{ form.field.errors }})
   - Botão Submit + link Cancelar

7. Criar apps/fornecedores/tests/conftest.py com fixtures:
   - test_unit(db): UnidadeOrganizacional
   - comprador_user(db, test_unit): User com role="comprador", default_unit=test_unit
   - solicitante_user(db, test_unit): User com role="solicitante"
   - admin_user(db): User com is_superuser=True, role="admin"
   - categoria(db): CategoriaCompra(nome="Informática", ativo=True)
   - fornecedor(db, categoria): Fornecedor com cnpj="11222333000181", razao_social="Empresa Teste Ltda",
     email="teste@empresa.com", categoria=categoria, ativo=True

8. Criar apps/fornecedores/tests/test_views.py cobrindo:
   - test_lista_exige_comprador: solicitante recebe 403
   - test_lista_comprador_ok: comprador recebe 200
   - test_busca_htmx_retorna_partial: request.htmx=True + q="Empresa" retorna HTML sem base.html
   - test_busca_vazia_retorna_lista: q="" não retorna 404, retorna 200
   - test_busca_cnpj_exato: q="11.222.333/0001-81" encontra fornecedor pelo CNPJ compactado
   - test_toggle_ativo_post: POST toggle-ativo inverte ativo=True → False
   - test_toggle_ativo_get_nao_aceito: GET toggle-ativo retorna 405
   - test_cadastrar_fornecedor_valido: POST com CNPJ válido cria Fornecedor e redireciona
   - test_cadastrar_cnpj_duplicado: POST com CNPJ já cadastrado retorna form com erro
  </action>
  <verify>
    <automated>cd C:\Users\admre\compras_nexos && python -m pytest apps/fornecedores/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pytest apps/fornecedores/ passa completamente (0 failures)
    - GET /fornecedores/ com comprador retorna 200
    - POST /fornecedores/novo/ com CNPJ válido cria registro no banco
    - POST /fornecedores/<pk>/toggle-ativo/ inverte ativo e retorna partial HTML
    - Solicitante recebe 403 em qualquer URL de fornecedores
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| Browser → /fornecedores/* | Inputs não confiáveis: q, cnpj, razao_social, categoria |
| URL param → DB | pk na URL pode ser manipulado para acessar qualquer fornecedor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | /fornecedores/* (todas as URLs) | mitigate | CompradorRequiredMixin bloqueia solicitante/gestor/diretor; PermissionDenied levanta 403 |
| T-03-02 | Tampering | ToggleAtivoView | mitigate | Aceita apenas POST; GET retorna 405 via http_method_not_allowed |
| T-03-03 | Tampering | FornecedorForm.clean_cnpj | mitigate | stdnum.validate() garante formato ^[\dA-Z]+$ antes de salvar; ORM parameterizado previne SQL injection |
| T-03-04 | Denial of Service | TrigramSimilarity — query muito longa | mitigate | Truncar q a 100 chars antes de passar ao ORM: q = q[:100] |
| T-03-SC | Tampering | pip install python-stdnum==2.2 | mitigate | Package Legitimacy Audit OK (research — slopcheck [OK], 14 anos, arthurdejong/python-stdnum) |
</threat_model>

<verification>
- python -m pytest apps/fornecedores/ -q deve terminar com 0 failures
- python manage.py migrate --check não deve reportar migrações pendentes
- curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/fornecedores/ (sem login) retorna 302 (redirect para login)
- Solicitante autenticado em /fornecedores/ recebe 403
</verification>

<success_criteria>
1. python-stdnum==2.2 está em requirements.txt
2. "apps.fornecedores" está em INSTALLED_APPS
3. Migration 0001_initial.py existe e é válida
4. pytest apps/fornecedores/ passa (todos os testes verde)
5. CNPJ alfanumérico "12ABC34501DE35" é aceito pelo FornecedorForm
6. CNPJ "00000000000000" é rejeitado com mensagem de erro
7. Busca fuzzy por nome retorna fornecedores similares
8. Toggle ativo via POST HTMX atualiza o campo sem reload
</success_criteria>

<output>
Ao concluir, criar `.planning/phases/03-suppliers-inventory/03-01-SUMMARY.md`
</output>
